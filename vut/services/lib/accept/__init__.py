# SPDX-License: MIT; Project VUT; (C) Frank-Rene Schaefer
"""
'hwut.accept's second face, on the same brick.

    propose.py   'hwut.accept.propose': BLESSES NOTHING. Writes the
                 differing cases as a file to read -- difference
                 first, target after.
    apply.py     'hwut.accept.apply': blesses what such a file names,
                 with '--force' implicit -- the reading was the
                 consent. The word is the tree's own: 'hwut.sanitize'
                 reports and acts on '--apply', and this is the same
                 relation.

ONE BRICK, NOT TWO ('services/accept.py'). Selection, provision and
the difference are accept's; this package holds a FACE over them and
no logic of its own. What it adds is a name: proposing and blessing
are different acts, and a reader who wants the first should not have
to remember a flag on the second.
"""
