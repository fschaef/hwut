STIMULUS  hwut.diff --console s.txt n.txt --plain --side-by-side --width 80
REACTION  stdout {
              
              =[ s.txt ]========================================== round 1
              setup | numeric=exact | analogy=on '((..))' | whitespace=tolerant | ignored '##..##'
              --[ LINE_SEQUENCE/LINE_SEQUENCE ]--
                 1 alpha                                   1 alpha
                 2 value [3.140]                      |    2 value [3.141]
                 3 [only in subject]                  >
                 4 same ~((token))~ here              ~    3 same ~((other))~ here
                   ^ analogy '((token))' ~ '((other))' -- established at S:4/N:3
                                                      <    4 [inserted in nominal]
                 5 a line wide enough to wrap around       5 a line wide enough to wrap around
                   the column it is given, so the con        the column it is given, so the con
                   tinuation rows show                       tinuation rows show
              ============================================================
          }
          exit code : 1

STIMULUS  the same, --numeric 0.01: the pair is tolerated, gutter '~'
              --[ LINE_SEQUENCE/LINE_SEQUENCE ]--
                 1 alpha                                   1 alpha
                 2 value ~3.140~                      ~    2 value ~3.141~
                 3 [only in subject]                  >
                 4 same ~((token))~ here              ~    3 same ~((other))~ here
                   ^ analogy '((token))' ~ '((other))' -- established at S:4/N:3
                                                      <    4 [inserted in nominal]
                 5 a line wide enough to wrap around       5 a line wide enough to wrap around
                   the column it is given, so the con        the column it is given, so the con
                   tinuation rows show                       tinuation rows show
              ============================================================

Subject left, nominal right: what the author reads is what he
will merge, left into right. ' ' equal, '~' tolerated, '|'
differing, '>' subject only, '<' nominal only.
SUCCESS: one row per aligned pair, two columns, the gutter speaks.
<hwut-end>
