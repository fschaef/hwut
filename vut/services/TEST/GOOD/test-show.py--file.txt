
--- resolved ---
$ hwut.show test-a.py
    test-a.py {
        title = "A"
        choices {                                 # app
            one {
                caps {
                    timeout_sec = 300.0           # default
                    cpu_sec = 300                 # default
                    memory_mb = 512               # default
                    file_size_mb = 10             # default
                    child_process_max_n = 32      # default
                    network = true                # default
                }
                tolerance {
                    numeric_ratio = 0.01          # app
                    slash = true                  # default
                    whitespace = true             # default
                    eq_pattern = []               # default
                    nothing = []                  # default
                    analogy = ["((", "))"]        # default
                    constraints = []              # default
                    comment = ["##", "##"]        # default
                    regions = true                # default
                }
                same = false                      # default
                interactive = false               # default
                output = ["<stdout>"]             # default
            }
            two {
                caps {
                    timeout_sec = 300.0           # default
                    cpu_sec = 300                 # default
                    memory_mb = 512               # default
                    file_size_mb = 10             # default
                    child_process_max_n = 32      # default
                    network = true                # default
                }
                tolerance {
                    numeric_ratio = 0.01          # app
                    slash = true                  # default
                    whitespace = true             # default
                    eq_pattern = []               # default
                    nothing = []                  # default
                    analogy = ["((", "))"]        # default
                    constraints = []              # default
                    comment = ["##", "##"]        # default
                    regions = true                # default
                }
                same = false                      # default
                interactive = false               # default
                output = ["<stdout>"]             # default
            }
        }
    }
    [status 0]

--- --no-default: what somebody chose ---
$ hwut.show test-a.py --no-default
    test-a.py {
        title = "A"
        choices {                                 # app
            one {
                tolerance {
                    numeric_ratio = 0.01          # app
                }
            }
            two {
                tolerance {
                    numeric_ratio = 0.01          # app
                }
            }
        }
    }
    [status 0]
<hwut-end>
