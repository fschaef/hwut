--- a target NO directory binds:
STATUS: 2
STDOUT {
    SANITIZE, in 'tree': lock, orphans, out, session
    
      nothing to sanitize
    
    TARGETS:
      target 'clean':
        no directory below '<work>/tree' binds any target.
        REFUSED: no directory binds a target 'clean'
}
--- a target one directory binds:
STATUS: 0
STDOUT {
    SANITIZE, in 'tree': lock, orphans, out, session
    
      nothing to sanitize
    
    TARGETS:
      target 'clean':
        :: suite/TEST: ./clean.sh [exit 0]
        the project cleaned itself
        directories: 1  executed: 1  skipped: 0
}
--- and without '--apply' it only says so:
STATUS: 0
STDOUT {
    SANITIZE, in 'tree': lock, orphans, out, session  (reporting only; '--apply' acts)
    
      nothing to sanitize
    
    TARGETS:
      target 'clean': would run in every directory that binds it ('--apply' runs it)
}
