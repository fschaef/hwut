
--- one app, every choice of it ---
$ hwut.plan test-a.py
    WISH: --glob "test-a.py"
    TEST PLAN: 3 node(s), 2 link(s), 0 exclusion set(s)
    NODES
        build[make test-a.py]  BUILD
        test-a.py one          TEST
        test-a.py two          TEST
    LINKS
        build[make test-a.py] ==> test-a.py one
        build[make test-a.py] ==> test-a.py two
    EXCLUSIONS
        (none)
    [status 0]

--- app and choice ---
$ hwut.plan test-a.py one
    WISH: --glob "test-a.py one"
    TEST PLAN: 2 node(s), 1 link(s), 0 exclusion set(s)
    NODES
        build[make test-a.py]  BUILD
        test-a.py one          TEST
    LINKS
        build[make test-a.py] ==> test-a.py one
    EXCLUSIONS
        (none)
    [status 0]

--- the same, spelled as the wish ---
$ hwut.plan --glob "test-a.py one"
    WISH: --glob "test-a.py one"
    TEST PLAN: 2 node(s), 1 link(s), 0 exclusion set(s)
    NODES
        build[make test-a.py]  BUILD
        test-a.py one          TEST
    LINKS
        build[make test-a.py] ==> test-a.py one
    EXCLUSIONS
        (none)
    [status 0]

--- a globbed app ---
$ hwut.plan test-*.py
    WISH: --glob "test-*.py"
    TEST PLAN: 5 node(s), 3 link(s), 1 exclusion set(s)
    NODES
        build[make test-a.py]  BUILD
        test-a.py one          TEST
        test-a.py two          TEST
        test-b.py              TEST
        test-net.py            TEST
    LINKS
        test-a.py one -> test-b.py
        build[make test-a.py] ==> test-a.py one
        build[make test-a.py] ==> test-a.py two
    EXCLUSIONS
        { test-net.py, test-a.py two }
    [status 0]
<hwut-end>
