STIMULUS  the flag spellings, one flag at a time:

REACTION  -r vs --raw      : identical
          coverage line, default      : coverage: none recorded
          coverage line, --no-coverage: coverage: left out ('--no-coverage')
          COVERAGE sections, default      : 0
          COVERAGE sections, --no-coverage: 0

One flag, two spellings; and where the book knows nothing of
coverage, no section pretends there was anything to show.
SUCCESS: the default is the useful one, and absence is spoken.
<hwut-end>
