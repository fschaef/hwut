==============================================================================
review-r6 -- THE DUMP IS INCOMPLETE, AND ONE EARLIER VERIFICATION FALLS
==============================================================================

VERSION: 3

COMPONENTS: engine/coverage (the gap); adm (the cause)
PLAN:       adm/WORK/plan/01-work-structure.txt   (fork r-5f, answered here)

Raised while answering r-5f -- 'coverage's text names a layout that no
longer exists'. It does not. THE LAYOUT EXISTS; THE DUMP DOES NOT
CARRY IT.

______________________________________________________________________________

1  WHAT IS MISSING

Six modules are IMPORTED by code that is present, and are themselves
absent from the tree:

    engine/coverage/database/api.py       <- coverage/api.py,
                                             coverage/affected.py
    engine/coverage/database/record.py    <- readers/gcov.py,
                                             readers/ghdl_psl.py,
                                             readers/go.py, and others
    engine/coverage/database/binary.py    <- TEST/test-affected.py
    engine/coverage/database/measure.py   <- readers/ucis.py,
                                             readers/verilator.py
    engine/coverage/readers/api.py        <- coverage/api.py,
                                             coverage/core.py
    engine/coverage/readers/reader.py     <- coverage/core.py,
                                             readers/__init__.py,
                                             readers/cobertura.py

'engine/coverage' therefore CANNOT IMPORT as the dump stands.

THE GAP IS BOUNDED. Every relative and absolute 'vut.*' import in the
whole tree was resolved against the tree. Exactly these six fail. The
only other unresolved name is 'vut.artifact', imported by three
'TEST/config.py' files, which is a test-side helper and a separate
question.

______________________________________________________________________________

2  THE CAUSE, AND WHY IT IS NOT A DESIGN PROBLEM

'adm/bundle.sh' selects with 'git ls-files' by default -- TRACKED
FILES ONLY. '--complete'/-a adds 'git ls-files --others', i.e. the
untracked ones. A file created and not yet committed is therefore
SILENTLY ABSENT from a default-mode dump: no warning, no manifest
entry, nothing to notice.

The six modules are exactly what a fresh 'database/readers' split
would produce: new files, not yet committed. So

    coverage's todo-13, dated 2026-09-04, IS CORRECT AND CURRENT.
    It names 'coverage/database/TEST/' and 'coverage/readers/TEST/'
    because those directories EXIST in Frank-Rene's working tree.

r-5f is ANSWERED: coverage's prose is not describing a superseded
layout. The dump is describing an incomplete one.

______________________________________________________________________________

2b  CONFIRMED BY A SECOND DUMP (2026-09-04, 'dump-vut-text-only.txt')

A documentation-only dump -- every '.txt'/'.md' EXCEPT under 'GOOD/'
and 'TEST/', 112 members -- settles section 2 by observation:

    vut/engine/coverage/database/FORMAT.txt          EXISTS (317 lines)
    vut/engine/coverage/readers/TEST/REAL_PARSING_INPUT/
                                     PROVENANCE.txt  EXISTS

So 'coverage/database/' and 'coverage/readers/TEST/' are real
directories in the working tree, and coverage's todo-13 is CORRECT AND
CURRENT. The first dump's gap was selection, not design, exactly as
section 2 supposed.

THE .py FILES ARE STILL NOT REACHABLE. This dump is text-only, so the
six missing modules remain unread and todo-9 (section 3) remains
UNVERIFIED. r-6a stands.

A FOURTH DISCUSSIONS FILE EXISTS, and is NEW -- created the same day,
absent from the first dump:

    services/DISCUSSIONS.txt   70 lines, one entry

Its todo-1 is 'hwut.rename' / 'hwut.rename-choice' ACROSS A DIRECTORY
BOUNDARY -- ruled during the database/readers split, and precisely the
blocker coverage's todo-13 names ("Waits on 'services' todo-1"). The
entry is careful: it states no fork ("no disc-N stands yet because no
fork has been found, only labour"), inventories the primitives that
already exist ('TestIdDb.register()', 'hwut.remove''s retirement,
'rename.move_tuple()', 'record.seated()'), and names what is actually
new -- the command shape, which today binds ONE Store per call and
would need a source and a target.

review-r5's count of THREE DISCUSSIONS.txt files was right when it was
written. It is now four.

Every strike of r-5a was re-examined against the gap. Five stand; ONE
DOES NOT.

  SAFE -- verified against files that ARE present:

    disc-8   verified against D-15, D-17 (coverage/RATIONALE.txt) and
             orchestrator D-7, D-8. All four entries present.
    todo-10  verified against D-25 and 'services/cov.py'. Present.
    todo-11  RETIRED; a self-declaration with no code claim to check.
    todo-12  verified against 'services/cov.py' VERB_TUPLE. Present.
    (operations todo-12 likewise: 'compare/configuration.py' and
     'compare/TEST/test-configuration.py' are both present.)

  WEAKENED -- verified only indirectly:

    todo-1   'the homogeneous record, its merge, and the index'. The
             record IS the missing 'database/record.py'. The strike
             rested on the record FORMAT being in use (version 2 in
             'services/TEST') and on 'services/cov.py' standing, not
             on reading the module. The conclusion is probably right
             and the evidence is thinner than stated.

  FALLS -- the verification was against a missing file:

    todo-9   The entry claims "what remains of the old bridge -- the
             'resolve' seam and '_local_resolver' -- is deleted", and
             review-r5 recorded them "verified ABSENT from the tree".
             THAT VERIFICATION IS WORTHLESS: a grep over
             'engine/coverage' cannot find a symbol that would live in
             'database/', and 'database/' is not here. The absence
             measured was the absence of the FILE, not of the symbol.

             The strike may still be correct -- D-18 stands as an
             entry, and the entry's own claim is dated -- but it was
             not verified, and this review says so rather than leaving
             a false 'verified' standing.

______________________________________________________________________________

OPEN -- FOR FRANK-RENE

  r-6a  CLOSED (2026-09-04). The complete dump arrived: 631 members,
        5.2 MB. All six modules are there --
        'coverage/database/{api,binary,gather,index,measure,present,
        record}.py' and 'coverage/readers/{api,reader,registry}.py'.
        The component imports. TODO-9 IS VERIFIED: '_local_resolver'
        occurs in zero '.py' files, only in RATIONALE prose recording
        its deletion, so the strike of r-5a stands on measurement and
        not on D-18 alone. Section 3's 'FALLS' is lifted.

        WHAT THE COMPLETE TREE ALSO OVERTURNED: three of the five
        README drift findings (README-INDEX 1.2, 1.4) were FALSE
        POSITIVES of measuring against the incomplete tree, and one
        (1.3, 'adm/census.py') was correctly found but WRONGLY
        DIAGNOSED here as untracked. It is a retired tool: 'census' is
        a VERB in this project and 'hwut.run' is what does it --
        'adm/TEST/hwut-info.dat' and 'bin/TEST/hwut-info.dat' exist,
        so the framework censuses itself.

  r-6b  Should 'bundle.sh' WARN when a default-mode selection omits
        untracked files? Today the omission is silent. A one-line
        count ('N untracked file(s) not selected; --complete includes
        them') would have made this visible at the moment the dump was
        made rather than four bundles later. It is the same principle
        as O-19: a kill names its cap.

  r-6c  'vut.artifact', imported by 'compare/TEST/config.py',
        'compare/core/edit_operations/TEST/config.py' and
        'compare/feeder/TEST/config.py', resolves to nothing here
        either. Untracked too, or genuinely external?

  r-6d  services/DISCUSSIONS.txt IS A RE-SEEDING, and it is the live
        instance of gate s-3. 'services/DISCUSSIONS/' was emptied and
        removed by G-2; a 'services/DISCUSSIONS.txt' was then created
        on the same day. Under the work structure, new work of this
        kind belongs in 'adm/WORK/' as an intend/spec/plan triple, not
        in a component's DISCUSSIONS. Two readings:
          (a) THE ENTRY IS WELL-FORMED WHERE IT STANDS and the
              structure should accept a component-local DISCUSSIONS
              for pure labour that carries no fork. It says so itself:
              "no disc-N stands yet ... only labour".
          (b) IT SHOULD BE AN adm/WORK ISSUE, gathered like the other
              forty, and DISCUSSIONS stays retired.        <- I have
              no recommendation here: this is what s-3 exists to
              decide, and it is a question about the discipline
              rather than about this entry.
