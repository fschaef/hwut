diff --git a/vut/adm/WORK/plan/01-work-structure.txt b/vut/adm/WORK/plan/01-work-structure.txt
--- a/vut/adm/WORK/plan/01-work-structure.txt
+++ b/vut/adm/WORK/plan/01-work-structure.txt
@@ -2,7 +2,7 @@
 plan/01-work-structure -- THE MIGRATION, IN ORDERED STEPS
 ==============================================================================
 
-VERSION: 5
+VERSION: 6
 
 COMPONENTS: all.
 INTEND:     adm/WORK/intend/01-work-structure.txt
@@ -86,10 +86,20 @@
              owner -- its COMPONENTS line says display, its subject
              is pype and the oracle.
         r-4b 0q: INTERNALS prose, or a GENERATED check under s-2.
-        r-5  THE GATHER IS INCOMPLETE -- three DISCUSSIONS.txt FILES
+        r-5  THE GATHER WAS INCOMPLETE -- three DISCUSSIONS.txt FILES
              (orchestrator 121, coverage 563, operations 721 lines)
-             were missed by a directory-only search. Each holds MANY
-             entries, so gathering them is a SPLIT, not a move.
+             were missed by a directory-only search. RULED
+             (2026-09-04): treat them BEFORE phase S, and an
+             ACCOMPLISHED entry needs no split -- it may enter README
+             or RATIONALE directly, or simply go.
+             TRIAGED in 'adm/WORK/review-r5.txt'.
+             engine/orchestrator/DISCUSSIONS.txt is DONE and DELETED:
+             every entry verified built against the tree, and the two
+             rulings of its todo-4 that no RATIONALE carried (R1, R2)
+             are now an AMENDED block on D-7.
+             Coverage is MIXED (six accomplished, nine discussions and
+             several todos open); operations is LARGELY OPEN (nothing
+             accomplished). Both wait on r-5a..r-5d.
 
     Sorting into a substrate whose census is a floor rather than a
     total would carry the gap forward, so nothing moves until r-5 at
diff --git a/vut/adm/WORK/review-r5.txt b/vut/adm/WORK/review-r5.txt
new file mode 100644
--- /dev/null
+++ b/vut/adm/WORK/review-r5.txt
@@ -0,0 +1,174 @@
+==============================================================================
+review-r5 -- THE THREE 'DISCUSSIONS.txt' FILES, TRIAGED BY ACCOMPLISHMENT
+==============================================================================
+
+VERSION: 1
+
+COMPONENTS: engine/orchestrator, engine/coverage, engine/operations
+PLAN:       adm/WORK/plan/01-work-structure.txt   (fork r-5)
+
+RULED (2026-09-04): these three files are treated BEFORE phase S, and
+an entry that is ACCOMPLISHED needs no split -- it may enter README or
+RATIONALE directly, or simply go.
+
+Each file is ONE document holding MANY entries, not the
+one-issue-per-file disc-N/todo-N form gathered in G-2. They were missed
+by G-1, which searched for DISCUSSIONS DIRECTORIES.
+
+Every claim of 'built' below was checked AGAINST THE TREE, not read off
+the file's own marker.
+
+______________________________________________________________________________
+
+1  engine/orchestrator/DISCUSSIONS.txt   121 lines      ** DONE, DELETED **
+
+    Every entry accomplished. Verified:
+
+      todo-1  test_run rewiring        'FOOTPRINT_FILE_NAME',
+                                       '_write_footprint' and
+                                       'footprint_facts' have ZERO
+                                       occurrences in the tree
+      todo-2  the services             'services/rename.py',
+                                       'rename_choice.py',
+                                       'remove.py', 'remove_choice.py',
+                                       'report.py' all stand
+      todo-3  configuration parsers    'exploration/hwut_parse.py' and
+                                       'test_writing_support/python/
+                                       hwut_hocon.py' stand (R-9, R-29)
+      todo-4  the test register        'bookkeeper/test_id_db.py' with
+                                       'roster()' (t-5, consumed by
+                                       'services/sanitize.py') and
+                                       'vanished()' (t-4, consumed by
+                                       'services/show.py'); record
+                                       format v2 in use
+                                       ('##VUT-COVERAGE 2', '##run:');
+                                       t-11 'services/sanitize.py'
+                                       stands
+      disc-1  the explorer             PARKED as 'not built yet' -- but
+                                       'exploration/explorer.py',
+                                       'tree_explorer.py', 'finder.py'
+                                       and 'hwut_info_interview.py' all
+                                       stand. The entry was STALE: the
+                                       question it parks was answered
+                                       by R-69 and R-76.
+
+    BEFORE DELETING, two rulings of todo-4 were found NOT fully carried
+    by any RATIONALE entry. R3 was carried whole by D-8 ('hwut.rename'
+    touches no record content, ever). R1 and R2 were not, and are now
+    an AMENDED block on D-7 in 'engine/orchestrator/RATIONALE.txt':
+
+      R1  what a vanishment costs each face -- 'hwut.run' notes
+          MISSING/FAIL and THE SUITE CONTINUES; source-needing services
+          REFUSE; every other face MENTIONS and proceeds.
+      R2  an explicit name outranks the roster -- the roster restricts
+          THE WALK and nothing else, so a file named on a command line
+          is interviewed regardless. This is how a new test runs before
+          it is registered, and why no registration step is owed.
+
+    The file is then DELETED, by its own header's law: 'an entry is
+    deleted the moment it is settled'.
+
+______________________________________________________________________________
+
+2  engine/coverage/DISCUSSIONS.txt      563 lines            ** MIXED **
+
+    ACCOMPLISHED -- may go, on your word:
+
+      todo-1   the homogeneous record, merge, index    self-marked BUILT
+      todo-6   'hwut.affected' over the index          self-marked BUILT
+      todo-9   is the orchestrator's t-9               BUILT, D-18 stands
+      todo-10  'hwut.cov gather'                       BUILT, D-25 stands
+      todo-11  the two refusals                        RETIRED
+      todo-12  'hwut.cov <wish>' runs, gather folds    BUILT
+
+      'services/cov.py' stands, and D-18 and D-25 are entries in
+      'coverage/RATIONALE.txt'. So these six are recorded where they
+      belong and their entries are residue.
+
+    STILL OPEN -- nine discussions and three part-built todos:
+
+      disc-1   text or binary (explicitly: not to be ruled without
+               numbers -- and todo-7 is the measurement that would rule
+               it, itself unbuilt)
+      disc-2   the session road: what is a coverage measurement OF
+      disc-3   in-directory parallelism during a coverage run
+      disc-4   cross-directory aggregation
+      disc-5   a merge meets hit counts
+      disc-6   'system/helper.py' Interval: unify or leave
+      disc-7   what consumes the index
+      disc-8   whose table is the test id -- SUPERSEDED, per the
+               orchestrator file's t-10, which says 'coverage disc-8
+               CLOSED' by D-7/D-8. Reads as accomplished; the entry
+               itself does not say so.
+      disc-9   hardware description languages
+      todo-2   the per-language readers            [11 built, not all]
+      todo-8   measurements beside the line axis   [part]
+      todo-3   'hwut.convert_to_lcov'
+      todo-4   the pack stops reserving
+      todo-5   '--global-conf' on the services
+      todo-7   the measurement that rules disc-1
+      todo-13  the three 'TEST/' books are stale -- DATED TODAY
+               (2026-09-04, the database/readers split). Blocked on
+               'services' todo-1: 'hwut.rename' carrying a book entry
+               ACROSS a directory boundary, not only re-keying within
+               one. Hand-editing was refused, correctly (never
+               fabricated).
+
+______________________________________________________________________________
+
+3  engine/operations/DISCUSSIONS.txt    721 lines   ** LARGELY OPEN **
+
+    Two parked discussions and seventeen todos, most of them live
+    design rather than residue. Nothing here is self-marked BUILT.
+
+      disc-8   the greater map                     parked by ruling
+      disc-9   the configuration file              parked, OUTSIDE this
+                                                   component
+      todo-2   re-association, what is still open        (compare)
+      todo-12  a misspelt tolerance is accepted silently (compare)
+      todo-15  the channel                         parked, after todo-16
+      todo-16  the merge loop does not loop
+      todo-17  a second IDE
+      todo-18  a browser client
+      todo-19  THE NUMERIC TOLERANCE IS UNSIGNED AND ASYMMETRIC
+               A MEASURED DEFECT, not a wish: 'compare/engine/input/
+               line_element.py' computes 'epsilon = number * ratio'
+               with NO abs(), so for a negative number the epsilon is
+               negative and the tolerance works BACKWARDS. This
+               violates the standing law that Eq must not be a
+               function of b0. It is the one item in these three files
+               that is a BUG REPORT.
+      todo-20  down carries the measurement             (compare feeder)
+      todo-21  the explain seam
+      todo-22  coverage in the pack
+      todo-23  proxy provision -- what remains open
+      todo-23  README deep-section polish     <- NUMBER COLLIDES with
+                                                 the entry above; two
+                                                 different todos both
+                                                 called todo-23
+      todo-24  the staleness law (s-4b, second half)
+      todo-25  the face of the second question
+      todo-26  renovate and the stderr survey
+
+______________________________________________________________________________
+
+OPEN -- FOR FRANK-RENE
+
+  r-5a  COVERAGE: strike the six accomplished entries (todo-1, 6, 9,
+        10, 11, 12) and disc-8 if its supersession holds, leaving the
+        file with only what is open? Nothing needs to enter RATIONALE
+        for these -- D-18 and D-25 already carry them.
+
+  r-5b  OPERATIONS: nothing is accomplished, so nothing can be struck.
+        Does the file stand as it is until its items are built, or is
+        it split into issues now? Splitting seventeen live todos is
+        the largest single piece of work in this migration, and its
+        value is not obvious while they remain in one component.
+
+  r-5c  todo-19 is a MEASURED DEFECT sitting in a discussion file. Does
+        it stay a discussion, or become work in its own right? It is
+        the only item across the three files that reports something
+        WRONG rather than something owed.
+
+  r-5d  operations has TWO todo-23s. Renumber, or leave until the file
+        is split?
diff --git a/vut/engine/orchestrator/DISCUSSIONS.txt b/vut/engine/orchestrator/DISCUSSIONS.txt
deleted file mode 100644
--- a/vut/engine/orchestrator/DISCUSSIONS.txt
+++ /dev/null
@@ -1,121 +0,0 @@
-============================================================================
-VUT-ORCHESTRATOR -- DISCUSSIONS
-============================================================================
-
-What is OPEN. Nothing settled belongs here; the settled design is
-README.txt and RATIONALE.txt. An entry is deleted the moment it is
-settled.
-
-    disc-N   a CHOICE is owed -- Frank-Rene rules, then it is built
-    todo-N   the LABOUR is owed -- the choice is made, the work is not
-
-============================================================================
-
-----------------------------------------------------------------------------
-todo-1  THE TEST_RUN REWIRING  (pass 2; the choice is made, D-1/D-5/D-6)
-----------------------------------------------------------------------------
-Owed, in test_run:
-    - every mode is HANDED a Bookkeeper (made above; tests construct
-      their own at the top)
-    - store.py loses the naming (delegates to the Bookkeeper) and the
-      footprint face entirely; FOOTPRINT_FILE_NAME and the footprint
-      file are deleted
-    - session.py loses _write_footprint, compare_setup_delta and
-      _OPERATION_NAME; records through bookkeeper.record()
-    - the outcome's '.footprint' field: renamed or retired -- the GOOD
-      files that print it move; a DESIGNED change, re-recorded and said
-      so
-    - result.footprint_facts(): retired (the Bookkeeper derives)
-    - test-store.py, test-session.py: footprint choices become
-      Bookkeeper-backed or move to bookkeeper/TEST
-
-----------------------------------------------------------------------------
-todo-2  THE SERVICES  (the surface is ruled, D-4)
-----------------------------------------------------------------------------
-Owed, in services/: hwut.rename, hwut.rename-choice, hwut.remove,
-hwut.remove-choice -- each edits the base AND moves/removes the record
-files the naming names, in one holding of the directory. The report
-service moves here from test_run/services/report.py and asks the
-Bookkeeper instead of globbing (find_records retires).
-
-----------------------------------------------------------------------------
-todo-4  THE TEST REGISTER, END TO END  (D-7; t-1..t-3 built 2026-08-25)
-----------------------------------------------------------------------------
-BUILT: t-1/t-2 'bookkeeper/test_id_db.py' -- relational register,
-one write-protected file, seven printing choices in
-'TEST/test-test_id_db.py'. Its allocation law changed the same day
-(bookkeeper B-2): ids are issued once, from 0, mark persisted,
-ceiling 2**32; the group table is the bookkeeper's too (B-3). t-3: 'hwut.accept' allocates on
-bless. t-4 MECHANISM: 'vanished()'. t-5 SEAM: 'roster()'. t-6:
-'hwut.show --show-ids' prints the register, VANISHED marks included.
-
-RULED (2026-08-25), all three open points:
-  R1   'hwut.run' notes MISSING/FAIL for a registered run whose test
-       or choice is no longer there (a renamed choice among them) and
-       THE SUITE CONTINUES. Services that need the test SOURCE
-       ('hwut.show <file>' among them) REFUSE; every other face
-       MENTIONS the vanishment and proceeds.
-  R2   No 'hwut.register': the FIRST 'hwut.accept' creates the entry.
-       The roster restricts the WALK; a file named EXPLICITLY on a
-       command line is interviewed regardless -- that is how a new
-       test runs before it exists in the register.
-  R3   'hwut.rename' touches no record content, ever: VUT-COVERAGE
-       records will refer to RUN IDS, and the guard is D-8 (ids
-       persist only in files the naming names).
-
-OWED, in order:
-  t-5  EXPLORATION READS THE ROSTER  [MUST BE DONE]: the directory
-       walk interviews registered files only; explicit names are
-       interviewed regardless (R2); unregistered candidates in a walk
-       are MENTIONED, never executed.
-  t-4  WIRING per R1: run's MISSING/FAIL note; source-needing
-       services refuse; the rest mention.
-  t-8  hwut.remove / hwut.remove-choice: base entry out, files the
-       naming names out -- record files among them (D-8) -- id freed,
-       ONE holding.
-       OWED WITH t-9, AND NOT BEFORE: every reference to the freed id
-       inside COVERAGE FILES must go in that same holding. The
-       obligation is DORMANT today and unmet only in appearance --
-       records carry NAMES, not ids, so nothing stale can exist yet.
-       It ARMS the day the record format names run ids (t-9), and
-       until then t-8 may be built but is NOT DONE: the last claim it
-       owes cannot be proved against a format that does not exist.
-       THE ORDER IS THEREFORE t-8 BEFORE t-9 BY CONVENIENCE, t-9
-       BEFORE t-8'S CLOSURE BY NECESSITY -- and t-9's design must
-       state where a record's ids stand, or removal cannot find them.
-  t-7  hwut.rename / hwut.rename-choice: base entry, file moves,
-       register rename -- one holding; record contents untouched (R3).
-  t-9  THE RECORD FORMAT REVISION  [BUILT 2026-08-25, coverage D-18]
-       Format version 2: '##run:' names RUN IDS, a SET of them, '-'
-       where the record names none. 'merge' unions the set. The
-       resolve seam is gone -- a record carries its ids. Accept hands
-       an id back when it aborts (bookkeeper B-4). What t-7 owes is
-       now SMALLER: 'hwut.rename' moves files and renames the register
-       entry and touches NO record content, as R3 ruled.
-  t-11 'hwut.sanitize', the housekeeping face -- options, each a
-       report first and a repair only on request:
-           --unregistered   source files in the directory that hold
-                            no entry and match no ignore glob (the
-                            forgotten first accept)
-           --stale          GOOD or OUT files no registered run
-                            names (candidates of the removed)
-           --orphans        test-runs registered without tests --
-                            the VANISHED, offered for removal
-  t-10 coverage disc-8 CLOSED; coverage identity doctrine notes the
-       supersession (D-7, D-8 here).
-
-----------------------------------------------------------------------------
-todo-3  THE CONFIGURATION PARSERS
-----------------------------------------------------------------------------
-The Bookkeeper's second half: parsing the user's configuration
-statements (what a test IS, from the source) as opposed to the recorded
-copy (what the base holds). The statement syntax is not designed yet;
-nothing here binds it.
-
-----------------------------------------------------------------------------
-disc-1  THE EXPLORER  (parked -- "something we focus on in isolation
-                       and calm")
-----------------------------------------------------------------------------
-Which directories hold tests: a task file naming them, or a walk for a
-marker (HWUT 1.0: 'hwut-info.dat'). orchestrator.py has nothing to bind
-until this is taken up; it is therefore not built yet (no orphans).
diff --git a/vut/engine/orchestrator/RATIONALE.txt b/vut/engine/orchestrator/RATIONALE.txt
--- a/vut/engine/orchestrator/RATIONALE.txt
+++ b/vut/engine/orchestrator/RATIONALE.txt
@@ -175,6 +175,27 @@
           the coordination dir-locality (coverage D-14) exists to
           remove.
 
+AMENDED (2026-09-04, on retiring 'orchestrator/DISCUSSIONS.txt', whose
+         todo-4 carried these two rulings of 2026-08-25 and whose every
+         entry is now built):
+
+         WHAT A VANISHMENT COSTS EACH FACE. 'hwut.run' NOTES
+         MISSING/FAIL for a registered run whose test or choice is no
+         longer there -- a renamed choice among them -- AND THE SUITE
+         CONTINUES. A vanished test does not abort a run: the other
+         tests still have something to say. Services that need the
+         test SOURCE ('hwut.show <file>' among them) REFUSE, because
+         there is nothing to read; every other face MENTIONS the
+         vanishment and proceeds.
+
+         AN EXPLICIT NAME OUTRANKS THE ROSTER. There is no
+         'hwut.register' face: the FIRST 'hwut.accept' creates the
+         entry, as above. The roster restricts THE WALK, and nothing
+         else -- a file named EXPLICITLY on a command line is
+         interviewed regardless of whether it stands in the register.
+         That is how a new test runs before it exists in the register,
+         and why no registration step is owed to the author.
+
 
 D-8  IDS IN RECORDS; ONE HOLDING FREES AND DELETES               (2026-08-25)
 ------------------------------------------------------------------------------
