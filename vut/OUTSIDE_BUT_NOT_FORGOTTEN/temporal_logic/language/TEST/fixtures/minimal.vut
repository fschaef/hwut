## minimal rule file: one reactor, causalities and a guarded causality inside
Polling : reactor {
    fuel: float;
    watch : behavior {
        button_pressed => light_on;
        MOTOR_ON when: .fuel == 0 => lighten_fuel_missing;
        ~ENTRY       => poll_sensors();
    }
}
