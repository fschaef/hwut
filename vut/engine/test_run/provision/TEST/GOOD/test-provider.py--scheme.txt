INSPECT: every role roots in I_Provider: True
         MultiExecute  is an I_MultiProvider: True
         its hand-out  is I_ProxyProvider and I_ExecuteProvider: True, True
         the proxy names its multi: True
         the queue sees I_Provider, uniformly: True
         a local stage is NO proxy: True
  OK  : one root under every role
  OK  : a multi-provider holds the means -- its PROXIES fill slots, not itself
  OK  : a hand-out is a proxy IN a role, and names its multi
  OK  : one queue consumes both shapes, and provenance stays visible only to who asks
SUCCESS: queue a provider, or a multi's proxies -- the seam carries both.
