INSPECT: with choice = GOOD/parse--basic.stdout
         no choice   = GOOD/parse.stdout
         candidate   = OUT/parse--basic.stdout
  OK  : the choice is part of the key, so choices never collide
  OK  : a test without choices carries no choice part
  OK  : nominals and candidates live in SEPARATE key spaces
SUCCESS: one key per (test, choice, subject); two spaces.
