--- what the fill allocated ---
INSPECT: [1] test-parse.py
         [1.1] basic
         [1.2] deep
INSPECT: [2] test-other.py
--- asking without allocating ---
         test-ghost.py -> None
         test-parse.py deep -> 1.2
  OK  : the first app's first choice is 1.1
  OK  : its second choice is 1.2 -- choice ids are scoped per app
  OK  : the choice-less run spells the app id alone
  OK  : an unknown run answers None where allocation was not asked
  OK  : allocating a standing run returns the standing id
  OK  : the register is on disk the moment the first id is born
SUCCESS: an id is born at first sight, lowest unused, in its own scope.
<hwut-end>
