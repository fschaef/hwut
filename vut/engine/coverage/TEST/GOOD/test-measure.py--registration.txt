
--- what is registered ---
         branch   tag 'BR'   mergeable False
         mcdc     tag 'MC'   mergeable False

--- a second measure claiming a taken name, or a taken tag ---
         name 'branch' again -> MeasureFault
         tag 'BR' again      -> MeasureFault
  OK  : two measures are registered, and the record writes them in this order so the bytes are stable
  OK  : each owns a record tag
  OK  : and answers to both its name and its tag
  OK  : what is not registered answers None -- the caller decides whether that is a fault
  OK  : a second measure of one NAME is refused
  OK  : and a second behind one TAG: two truths, and the later import would win
SUCCESS: a measure is registered once, under one name and one tag.
<hwut-end>
