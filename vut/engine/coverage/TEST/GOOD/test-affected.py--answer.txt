
--- a change in the region both parse runs executed ---
REACTION  status 0   stderr 0 byte(s)
          | ==[ HWUT AFFECTED ]==========================================================
          | change:  1 file(s), 2 range(s)
          | index:   3 run(s), 1 source file(s)
          | groups:  5 distinct
          |
          | runs to perform:
          |   parser/TEST:0.0
          |   parser/TEST:0.1
          |
          | NOTE: these are the runs that CERTAINLY executed the changed lines.
          |       A run ABSENT here may still be affected -- by a branch that was
          |       deleted, by a data file, by a dynamic dispatch, by an absence.
          |       This is a SUGGESTION, never a clearance: running only these and
          |       calling the suite green is a false green.

--- a change at line 51 -- a different region, a different run ---
REACTION  status 0   stderr 0 byte(s)
          | ==[ HWUT AFFECTED ]==========================================================
          | change:  1 file(s), 1 range(s)
          | index:   3 run(s), 1 source file(s)
          | groups:  5 distinct
          |
          | runs to perform:
          |   other/TEST:0
          |
          | NOTE: these are the runs that CERTAINLY executed the changed lines.
          |       A run ABSENT here may still be affected -- by a branch that was
          |       deleted, by a data file, by a dynamic dispatch, by an absence.
          |       This is a SUGGESTION, never a clearance: running only these and
          |       calling the suite green is a false green.
  OK  : a selection was made
  OK  : both runs that executed lines 7..8 are named
  OK  : the run that reached only 50..52 is NOT named -- the change touched 7..8 and 42
  OK  : and a change at line 51 names IT alone: the selection follows the lines, not the file
  OK  : the index says how many runs it was built from
  OK  : and the LIMIT is printed, always
SUCCESS: the answer names who executed the change, and what it does not mean.
<hwut-end>
