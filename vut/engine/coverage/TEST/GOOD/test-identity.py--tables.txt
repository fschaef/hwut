
--- the group table, as it is stored ---
         | ##VUT-TEST-GROUPS 2
         | G:0
         | G:1 1.1
         | G:2 1.1,1.2
         | G:3 1.2

--- read back ---
         0 -> {}
         1 -> 1.1
         2 -> 1.1, 1.2
         3 -> 1.2

--- the next allocation stands ABOVE the highest group read ---
INSPECT: -> 4

--- a GATHERED key has no place in a stored table ---
         raised IdentityFault
  OK  : format(parse(format(x))) == format(x) -- the bytes are stable
  OK  : a group decodes to the run ids it was made of
  OK  : the empty group survives the round trip
  OK  : reading a table allocates onward from ABOVE the highest group it saw
  OK  : storing a gathered key is refused: it belongs to ONE aggregation and would freeze that view as identity
SUCCESS: one table, stable bytes, and nothing ephemeral in it.
<hwut-end>
