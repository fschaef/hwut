
--- the empty group exists by construction ---
INSPECT: group of the empty set -> 0
         group 0 decodes to {}

--- three distinct sets, three groups ---
INSPECT: {1} -> 1   {1,2} -> 2   {2} -> 3

--- the SAME set, whatever order or type it arrives in ---
INSPECT: [2,1] -> 2   frozenset({2,1}) -> 2
         the table holds 4 group(s)
  OK  : the empty set is group 0 -- a value, not a hole
  OK  : and it decodes to nothing
  OK  : distinct sets get distinct groups, monotonically
  OK  : one set is one group, whatever order or container it came in
  OK  : three sets plus the empty one -- nothing was allocated twice
  OK  : an unknown set can be asked without being allocated
  OK  : a group decodes to its ids
  OK  : a group that was never allocated is refused, never answered with the empty set -- that would read as 'nobody ran this'
SUCCESS: pay for the distinct set, not for every occurrence.
<hwut-end>
