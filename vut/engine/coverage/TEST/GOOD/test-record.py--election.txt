
--- the language is derived where it is not stated ---
INSPECT: 'test-parse.py'  -> python
         'engine.cpp'     -> c++
         stated wins      -> lua

--- candidates by glob, in preference order ---
INSPECT: python  -> ('coverage', 'slipcover', 'trace')
         python3 -> ('coverage', 'slipcover', 'trace')
         c++     -> ('gcov', 'llvm-cov', 'kcov')

--- the FIRST available candidate is elected ---
INSPECT: python on this stated machine -> coverage
         c      on this stated machine -> gcov

--- the three refusals ---
         no derivation   -> CoverageRefused
         no tool for it  -> CoverageRefused
         none available  -> CoverageRefused
  OK  : '.py' derives python
  OK  : '.cpp' derives c++
  OK  : a stated language wins over the derivation
  OK  : the glob 'python*' answers for 'python', readable tool first
  OK  : and for 'python3' -- one language under two names
  OK  : vhdl reaches gcov, because GHDL's gcc backend writes it -- a language served by a table entry and no code
  OK  : and a language this build vouches for no tool on has an EMPTY list: saying so beats inventing a default
  OK  : the first available candidate is elected
  OK  : and the preference order decides which
  OK  : an underivable language is refused, never guessed
  OK  : a language with no configured tool is refused
  OK  : a machine with no candidate is refused, not silently skipped
SUCCESS: election is stated at every step, and refuses rather than guesses.
<hwut-end>
