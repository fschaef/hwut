
--- rename the test file ---
INSPECT: id 1 now names 'test-parser.py--basic'
         the id is unchanged: True

--- rename the choice ---
INSPECT: id 1 now names 'test-parser.py--quick'

--- the OLD name no longer resolves forward ---
         test-parse.py--basic             -> None

--- renaming onto a run another live id holds ---
         raised IdentityFault
  OK  : THE ID IS UNCHANGED -- every record ever written still attributes to the right run
  OK  : both parts can be renamed, one call at a time
  OK  : the old name resolves forward no more
  OK  : a rename onto a LIVE run's name is refused: two live ids for one run would make 'which id is this' unanswerable
  OK  : renaming an id that was never allocated is refused
  OK  : and no rename ever allocated an id
SUCCESS: a rename touches one table; the records are untouched.
<hwut-end>
