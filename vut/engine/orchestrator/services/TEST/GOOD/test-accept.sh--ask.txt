STATUS: 0
    
    ==============================================================================
    ACCEPT  test-ok.sh .stdout
    ------------------------------------------------------------------------------
        steady line
        <hwut-end>
    ------------------------------------------------------------------------------
        candidate: test-ok.stdout
        nominal:   test-ok.stdout
    accept this as the nominal? [y/N] 
    
    ==============================================================================
    ACCEPT  test-two.sh a .stdout
    ------------------------------------------------------------------------------
        choice a
        <hwut-end>
    ------------------------------------------------------------------------------
        candidate: test-two--a.stdout
        nominal:   test-two--a.stdout
    accept this as the nominal? [y/N] 
    
    ==============================================================================
    ACCEPT  test-two.sh b .stdout
    ------------------------------------------------------------------------------
        choice b
        <hwut-end>
    ------------------------------------------------------------------------------
        candidate: test-two--b.stdout
        nominal:   test-two--b.stdout
    accept this as the nominal? [y/N] 
    
    ==============================================================================
    ACCEPTED  1 of 3
    ------------------------------------------------------------------------------
        blessed        test-ok.sh .stdout
        left alone     test-two.sh a .stdout
        left alone     test-two.sh b .stdout
    ==============================================================================
GOOD holds (first blessed, second left alone):
    test-ok.stdout
