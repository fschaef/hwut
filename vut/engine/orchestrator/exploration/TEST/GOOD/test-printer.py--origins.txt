
--- every provenance in one directory ---
gen.c {
    title = "generated"
    choices {                                 # hwut.conf
        - {
            caps {
                timeout_sec = 60.0            # default
                network = true                # default
            }
            tolerance {
                numeric_ratio = 0.0           # default
                slash = true                  # hwut.conf:2
                whitespace = true             # default
            }
            eq-pattern = []                   # default
            nothing = []                      # default
            analogy = ["((", "))"]            # default
            constraints = []                  # default
            comment = ["//", "//"]            # hwut.conf:5
            same = false                      # default
            interactive = false               # default
            output = ["<stdout>"]             # default
        }
    }
}

legacy.bas {
    title = "Iron and Blood"
    choices {                                 # --hwut-info
        one {
            caps {
                timeout_sec = 60.0            # default
                network = true                # default
            }
            tolerance {
                numeric_ratio = 0.0           # default
                slash = true                  # hwut.conf:2
                whitespace = true             # default
            }
            eq-pattern = []                   # default
            nothing = []                      # default
            analogy = ["((", "))"]            # default
            constraints = []                  # default
            comment = ["##", "##"]            # default
            same = true                       # --hwut-info
            interactive = false               # default
            output = ["<stdout>"]             # default
        }
    }
}

test-own.py {
    title = "own"
    choices {                                 # app
        two {
            caps {
                timeout_sec = 60.0            # default
                network = true                # default
            }
            tolerance {
                numeric_ratio = 0.05
                slash = true                  # hwut.conf:2
                whitespace = true             # default
            }
            eq-pattern = []                   # default
            nothing = []                      # default
            analogy = ["((", "))"]            # default
            constraints = []                  # default
            comment = ["##", "##"]            # default
            same = false                      # default
            interactive = false               # default
            output = ["<stdout>"]             # default
        }
    }
}

<hwut-end>
