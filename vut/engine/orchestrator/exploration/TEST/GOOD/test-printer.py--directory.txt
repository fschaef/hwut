
--- the whole directory ---
hwut.conf
    on_entry             setup.sh
    collision            test-b.py
    dependency           test-a.py one <- test-b.py  test-b.py <- test-a.py one

test-a.py {
    title = "A"
    choices {                                 # app
        one {
            caps {
                timeout_sec = 300.0           # default
                cpu_sec = 300                 # default
                memory_mb = 512               # default
                file_size_mb = 10             # default
                child_process_max_n = 32      # default
                network = true                # default
            }
            tolerance {
                numeric_ratio = 0.0           # default
                slash = true                  # default
                whitespace = true             # default
                eq_pattern = []               # default
                nothing = []                  # default
                analogy = ["((", "))"]        # default
                constraints = []              # default
                comment = ["##", "##"]        # default
                regions = true                # default
            }
            same = false                      # default
            interactive = false               # default
            output = ["<stdout>"]             # default
        }
        two {
            caps {
                timeout_sec = 300.0           # default
                cpu_sec = 300                 # default
                memory_mb = 512               # default
                file_size_mb = 10             # default
                child_process_max_n = 32      # default
                network = true                # default
            }
            tolerance {
                numeric_ratio = 0.0           # default
                slash = true                  # default
                whitespace = true             # default
                eq_pattern = []               # default
                nothing = []                  # default
                analogy = ["((", "))"]        # default
                constraints = []              # default
                comment = ["##", "##"]        # default
                regions = true                # default
            }
            same = false                      # default
            interactive = false               # default
            output = ["<stdout>"]             # default
        }
    }
}

test-b.py {
    title = "B"
    choices {                                 # app
        - {
            caps {
                timeout_sec = 300.0           # default
                cpu_sec = 300                 # default
                memory_mb = 512               # default
                file_size_mb = 10             # default
                child_process_max_n = 32      # default
                network = true                # default
            }
            tolerance {
                numeric_ratio = 0.0           # default
                slash = true                  # default
                whitespace = true             # default
                eq_pattern = []               # default
                nothing = []                  # default
                analogy = ["((", "))"]        # default
                constraints = []              # default
                comment = ["##", "##"]        # default
                regions = true                # default
            }
            same = false                      # default
            interactive = false               # default
            output = ["<stdout>"]             # default
        }
    }
}

test-a.py one   [MISDEP]
test-b.py -   [MISDEP]
FAULT hwut.conf:1:7: DIRECTORY: dependency cycle: test-a.py one -> test-b.py -> test-a.py one
<hwut-end>
