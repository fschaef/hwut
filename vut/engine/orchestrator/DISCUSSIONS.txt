============================================================================
VUT-ORCHESTRATOR -- DISCUSSIONS
============================================================================

What is OPEN. Nothing settled belongs here; the settled design is
README.txt and RATIONALE.txt. An entry is deleted the moment it is
settled.

    disc-N   a CHOICE is owed -- Frank-Rene rules, then it is built
    todo-N   the LABOUR is owed -- the choice is made, the work is not

============================================================================

----------------------------------------------------------------------------
todo-1  THE TEST_RUN REWIRING  (pass 2; the choice is made, D-1/D-5/D-6)
----------------------------------------------------------------------------
Owed, in test_run:
    - every mode is HANDED a Bookkeeper (made above; tests construct
      their own at the top)
    - store.py loses the naming (delegates to the Bookkeeper) and the
      footprint face entirely; FOOTPRINT_FILE_NAME and the footprint
      file are deleted
    - session.py loses _write_footprint, compare_setup_delta and
      _OPERATION_NAME; records through bookkeeper.record()
    - the outcome's '.footprint' field: renamed or retired -- the GOOD
      files that print it move; a DESIGNED change, re-recorded and said
      so
    - result.footprint_facts(): retired (the Bookkeeper derives)
    - test-store.py, test-session.py: footprint choices become
      Bookkeeper-backed or move to bookkeeper/TEST

----------------------------------------------------------------------------
todo-2  THE SERVICES  (the surface is ruled, D-4)
----------------------------------------------------------------------------
Owed, in services/: hwut.rename, hwut.rename-choice, hwut.remove,
hwut.remove-choice -- each edits the base AND moves/removes the record
files the naming names, in one holding of the directory. The report
service moves here from test_run/services/report.py and asks the
Bookkeeper instead of globbing (find_records retires).

----------------------------------------------------------------------------
todo-4  THE TEST REGISTER, END TO END  (D-7; t-1..t-3 built 2026-08-25)
----------------------------------------------------------------------------
BUILT: t-1/t-2 'bookkeeper/test_id_db.py' -- relational register,
one write-protected file, seven printing choices in
'TEST/test-test_id_db.py'. Its allocation law changed the same day
(bookkeeper B-2): ids are issued once, from 0, mark persisted,
ceiling 2**32; the group table is the bookkeeper's too (B-3). t-3: 'hwut.accept' allocates on
bless. t-4 MECHANISM: 'vanished()'. t-5 SEAM: 'roster()'. t-6:
'hwut.show --show-ids' prints the register, VANISHED marks included.

RULED (2026-08-25), all three open points:
  R1   'hwut.run' notes MISSING/FAIL for a registered run whose test
       or choice is no longer there (a renamed choice among them) and
       THE SUITE CONTINUES. Services that need the test SOURCE
       ('hwut.show <file>' among them) REFUSE; every other face
       MENTIONS the vanishment and proceeds.
  R2   No 'hwut.register': the FIRST 'hwut.accept' creates the entry.
       The roster restricts the WALK; a file named EXPLICITLY on a
       command line is interviewed regardless -- that is how a new
       test runs before it exists in the register.
  R3   'hwut.rename' touches no record content, ever: VUT-COVERAGE
       records will refer to RUN IDS, and the guard is D-8 (ids
       persist only in files the naming names).

OWED, in order:
  t-5  EXPLORATION READS THE ROSTER  [MUST BE DONE]: the directory
       walk interviews registered files only; explicit names are
       interviewed regardless (R2); unregistered candidates in a walk
       are MENTIONED, never executed.
  t-4  WIRING per R1: run's MISSING/FAIL note; source-needing
       services refuse; the rest mention.
  t-8  hwut.remove / hwut.remove-choice: base entry out, files the
       naming names out -- record files among them (D-8) -- id freed,
       ONE holding.
       OWED WITH t-9, AND NOT BEFORE: every reference to the freed id
       inside COVERAGE FILES must go in that same holding. The
       obligation is DORMANT today and unmet only in appearance --
       records carry NAMES, not ids, so nothing stale can exist yet.
       It ARMS the day the record format names run ids (t-9), and
       until then t-8 may be built but is NOT DONE: the last claim it
       owes cannot be proved against a format that does not exist.
       THE ORDER IS THEREFORE t-8 BEFORE t-9 BY CONVENIENCE, t-9
       BEFORE t-8'S CLOSURE BY NECESSITY -- and t-9's design must
       state where a record's ids stand, or removal cannot find them.
  t-7  hwut.rename / hwut.rename-choice: base entry, file moves,
       register rename -- one holding; record contents untouched (R3).
  t-9  THE RECORD FORMAT REVISION: the VUT-COVERAGE header names a
       RUN ID where it named test/choice; the register decodes;
       version bump; index folds over TestRunIds. UNBLOCKED by
       bookkeeper B-2 (an id may now be persisted); design pending.
       Its precondition 'identity.py reduces to the gather shapes' is
       done: 'identity.py' is dissolved (coverage D-17).
  t-11 'hwut.sanitize', the housekeeping face -- options, each a
       report first and a repair only on request:
           --unregistered   source files in the directory that hold
                            no entry and match no ignore glob (the
                            forgotten first accept)
           --stale          GOOD or OUT files no registered run
                            names (candidates of the removed)
           --orphans        test-runs registered without tests --
                            the VANISHED, offered for removal
  t-10 coverage disc-8 CLOSED; coverage identity doctrine notes the
       supersession (D-7, D-8 here).

----------------------------------------------------------------------------
todo-3  THE CONFIGURATION PARSERS
----------------------------------------------------------------------------
The Bookkeeper's second half: parsing the user's configuration
statements (what a test IS, from the source) as opposed to the recorded
copy (what the base holds). The statement syntax is not designed yet;
nothing here binds it.

----------------------------------------------------------------------------
disc-1  THE EXPLORER  (parked -- "something we focus on in isolation
                       and calm")
----------------------------------------------------------------------------
Which directories hold tests: a task file naming them, or a walk for a
marker (HWUT 1.0: 'hwut-info.dat'). orchestrator.py has nothing to bind
until this is taken up; it is therefore not built yet (no orphans).
