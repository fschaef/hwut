todo-1  THE DISPATCHER INTO test_run                      ** LABOUR OWED **
______________________________________________________________________________

The seam is built ('I_Dispatcher', S-1); the implementation that hands
work to test_run is not.

Owed:
    run_test(node)        one test run through test_run's pipeline;
                          the verdict is the answer, the record goes
                          to the Bookkeeper
    run_build(node)       through test_run's multi-builder, whose
                          waves the build interview already asked about
    open_session(node)    one MultiExecute per interactive file; its
                          per-choice providers serve the TEST nodes
    close_session(node)   MultiExecute.close(); the sinks leave with
                          the visit
    run_script(role, cmd) the frame scripts, procsitter-supervised,
                          under the directory's caps

Also owed: the scheduler's own report reaching the Bookkeeper.
