==============================================================================
display -- RATIONALE
==============================================================================

Settled decisions, read-only. Ruled by Frank-Rene, 2026-08-19.

----------------------------------------------------------------------------
D-1  THE FLOW LINE FORMAT
----------------------------------------------------------------------------
PROBLEM   Tests run in parallel; the reader needs to know what is
          happening NOW and how many threads are live, without reading
          a clock of their own or polling.

DECISION  One line per event:

              hh:mm:ss | NNN | [EVENT] NICK:base [choice] ...

          'hh:mm:ss' relative to the stream's first 'when'; unparseable
          'when' prints verbatim, right-aligned to 8. 'NNN' is the
          parallel count AFTER the event. '[SKIP ]' for a 'run-ended'
          that never had a 'run-begun'. Failing lines carry the phrase
          inline. Cause appended with '<-'. Width fixed 78 (constructor
          arg; COLUMNS honoured only on a tty, at the face, once).

          DIRECTORIES roll-call after the flow; FAILURES last. Per-
          directory dotted block from HWUT 1.0: GRAVEYARD (confusing,
          no longer needed -- the roll-call and FAILURES carry all of it).

COST      One line per event means a long run produces a long log; the
          QUIET tier is the answer for callers that want the summary only.

GRAVEYARD Per-directory dotted progress block (HWUT 1.0) -- retired in
          favour of the linear flow + closing roll-call.

----------------------------------------------------------------------------
D-2  THE COLOUR DECISION
----------------------------------------------------------------------------
PROBLEM   Colour helps on a terminal; it is noise in a file, a CI log,
          or a 'diff'. Gates are not obvious ('TERM=dumb' under ssh,
          Windows without ANSI, NO_COLOR, CI).

DECISION  ALL gates must pass for colour to be on: isatty, NO_COLOR
          unset, CI unset, TERM set and not 'dumb', not Windows-without-
          ANSI. '--colour' ('force_f') beats every gate, NO_COLOR
          included. '--no-colour' ('veto_f') refuses. Both together:
          REFUSED. Decision taken once at 'main', handed down as a
          flag; no line re-sniffs. The test verifies the DECISION
          (escape presence) not the escape spelling.

COST      '--colour' beside '--no-colour' is refused, not silently
          chosen; the caller must be unambiguous.

GRAVEYARD Escapes-as-literals test idea -- the test would be tautological
          (it only exercises what the test itself writes). The decision
          is the right thing to test.

----------------------------------------------------------------------------
D-3 / E-1  THE EXIT STATUS LAW
----------------------------------------------------------------------------
PROBLEM   Five faces, five sets of magic integers. A caller checking
          '$?' against a literal had no guarantee two faces agreed.

DECISION  One enum, 'E_ExitCode' (services/_exit.py), IntEnum so
          'sys.exit()' takes a member unchanged:

              OK=0  FAULT=1  REFUSED=2  EMPTY=3  SIGPIPE=141

          Every face returns members and nothing else. The import is
          delayed past '_config' adoption for the three faces
          (compare, merge, report) that may be run by path without
          '-m': 'from ._exit import E_ExitCode' moved to after the
          'if __package__' guard so the relative import resolves in
          both invocation modes.

COST      The five faces needed surgical edits; their GOODs that print
          status numbers changed.

GRAVEYARD Bare integer returns -- every 'return 0/1/2' replaced.

----------------------------------------------------------------------------
D-4  THE DEFAULT TIER
----------------------------------------------------------------------------
PROBLEM   What should 'hwut.run' show without a flag?

DECISION  '--plain' is the default, statable redundantly; the name is
          explicit so a script can pin the rendering even if the
          default changes later.

COST      None.

----------------------------------------------------------------------------
D-5  THE TIER LADDER
----------------------------------------------------------------------------
PROBLEM   Some callers want every event (debugging, audit); others
          want the summary only (CI gate); others want nothing on
          stdout (pipe the exit status).

DECISION  Four tiers, each a strict subset of the one above:

              VERBOSE  every event, the swallowed ones included
              PLAIN    the flow, the roll-call, FAILURES (default)
              QUIET    no flow; the roll-call, FAULTS, FAILURES
              SILENT   nothing on stdout; faults to stderr, prefixed

          SILENT governs stdout only -- faults go to stderr with the
          same prefix and nickname as in the flow (the curl -sS law:
          '--silent' means 'quiet on stdout', not 'swallow errors').
          Tiers are mutually exclusive; two together: REFUSED.

COST      '--plain' beside another tier flag is REFUSED, not silently
          ignored; callers that state it redundantly must omit it when
          adding another tier flag.

GRAVEYARD Stderr-silent SILENT -- rejected; hides faults from the
          operator and violates the curl -sS precedent.

----------------------------------------------------------------------------
D-6  ONE ENGLISH DOOR
----------------------------------------------------------------------------
PROBLEM   Wire tokens (e.g. 'terminated-without-hwut-end') are
          machine words; prose renderings had appeared ad hoc in
          several places, giving the impression of misaligned
          components.

DECISION  'word.py' is the ONE place a token becomes English. Every
          rendering draws from 'phrase()'; the wire keeps its tokens.
          PHRASE_DB covers every verdict word and every E_TestRunResult
          value. A token not in the table prints with its hyphens
          opened -- the stability promise.

          One voice throughout: 'not supported here' is always 'not
          supported here', never sometimes 'unsupported'. No mental
          effort wondering whether two renderings name the same thing.

COST      A token the table does not know prints hyphens-opened, which
          may surprise a user on first sight; the fallback is explicit
          and consistent with the stability promise.

GRAVEYARD Token-in-flow / phrase-in-block split -- rejected; two
          vocabularies in one report are harder to read than one.

----------------------------------------------------------------------------
D-7  ONE HOME FOR RENDERING
----------------------------------------------------------------------------
PROBLEM   Human-facing output had three possible homes: 'compare/
          feeder' (which both computed a difference AND rendered it),
          'operations/interaction' (TuiDisplay), and 'display/'. Three
          homes is what makes a tree look like misaligned components --
          the very appearance D-6 was ruled to prevent.

DECISION  'compare' COMPUTES, 'display' RENDERS. Compare emits an
          association structure carrying tolerance marks; display turns
          it into text. One English door ('word.py') therefore covers
          artifact differences as well as the run report, and the
          renderer is reusable by 'accept' and 'merge' without either
          importing compare's presentation.

COST      Compare must emit structure where it previously emitted text;
          TuiDisplay's place (absorbed, kept as the interactive layer
          above the renderer, or retired) is NOT yet ruled.

GRAVEYARD (a) compare renders its own differences -- rejected; it puts
          a second vocabulary beside word.py with nothing keeping the
          two aligned.

----------------------------------------------------------------------------
D-8  THE FACE IS OPERATIONAL; DISPLAY OWNS THE RENDERING WORDS
----------------------------------------------------------------------------
PROBLEM   'orchestrator/run/' was verified free of rendering -- no
          print, no write, no format string, no display import. The
          FACE was not: 'services/run.py' held the tier table, the
          three rendering refusals, the tty sniff, the colour gates,
          the width policy and the ink construction. An operational
          face was deciding how things look.

DECISION  Three kinds of word, three parsers, each owned by whoever
          understands it:

              selection   the wish         plan/wish.py   parse_wish()
              execution   --jobs --no-store  the face
              rendering   tier, colour     display/console.py
                                              parse_rendering()

          'parse_rendering()' mirrors 'parse_wish()': it takes the
          words, yields what it understood and what it did not touch,
          and raises 'RenderingError' where the words cannot want
          anything. The face turns the raise into REFUSED, by name,
          with the usage line -- exactly as it already did for
          'WishError'.

          'console_view()' hands back a constructed view: tier, ink and
          width already decided. The face keeps ONE piece of rendering
          knowledge -- whether its own stdout is a terminal -- because
          it alone knows its sink ('write' given, or 'print').

          'orchestrator/run/' remains operational only. That is now a
          LAW in its README, not an accident.

          Side effect, and the reason this shape was chosen over a
          simple helper call: the REST split becomes STRUCTURAL rather
          than a rule in a document. Selection and execution are
          exposed; rendering never is. A type, not a convention.

COST      One more module in display/. The alternative (growing
          'plain.py') was rejected because a second view (html, pdf)
          will want its own option vocabulary and none of the console's
          tty gates; 'console.py' names the tier that owns them, so a
          future 'html.py' sits beside 'plain.py' as a peer.

VERIFIED  A pure refactor must move zero GOODs. All 14 display GOODs
          byte-exact after the move, the 'refused' choice -- which
          exercises all three rendering refusals -- included.


D-9  THE FLOW LINE CARRIES WHAT WAS ASKED FOR, AND NAMES WHAT RAN

PROBLEM   Every flow line opened with a clock and a job count nobody
          had asked for, carried its badge in brackets and its choice
          in braces, repeated the directory nickname on every line,
          and announced the beginning of tests that had already
          finished. Four columns of furniture around one fact.

DECISION  THE COLUMNS ARE ASKED FOR. '--show-timing' gives seconds
          (a suite is read in seconds; a column of zeroed hours says
          nothing); '--show-jobs' gives '|3|', no blank inside the
          bars. Absent both, the line carries the badge and the name.
          THE BADGES LOSE THEIR BRACKETS and keep their alignment,
          five characters wide. THE CHOICE LOSES ITS BRACES: it has a
          column of its own, and a column needs no punctuation to say
          where it begins.
          THE LINE ELIDES, as a sorted wishlist does (disc-8): ':/'
          where the directory repeats, ':/:' where the application
          does. Columns are FIXED, not grown as names arrive -- a
          width that widens mid-run moves every column under it.
          SESSION AND BUILD ARE SILENT unless '--show-details', the
          VERBOSE tier, or FAILURE: a failed precondition is a test
          result, never a silence.
          THE 'START' IS HELD '--start-delay' seconds, 2 by default.
          A test finishing inside the window never announces its
          beginning; one outliving it does, so a slow or hanging test
          is visible while it stands. The consumer WAKES on its own
          (run.py, TICK_SECONDS) -- a run that merely takes long
          emits no event, and a held line nobody released would never
          be seen.
          A BROKEN SPECIFICATION IS A FAILING TEST, emitted like a
          [MISDEP] node, counted in its directory and named in
          FAILURES -- the same law that makes a failed BUILD a test
          result rather than a suite-aborting precondition.
          THE VERDICT TAG CARRIES A GROUND, white on green and white
          on red; the phrase beside it does not. The tag is what an
          eye SCANS for, and a block of colour is found at a glance
          where a coloured word is read for.
          THE WIDTH is the terminal's, held between 40 and 120: a
          line drawn across a very wide terminal is a line the eye
          must travel, and the dots stop joining name to verdict.

COST      Every display GOOD moves. The default flow no longer shows
          when a test ran, which '--show-timing' restores.

VERIFIED  337 declared choices green across ten suite families.

GRAVEYARD A FIXED 2-SECOND DELAY, unstatable -- rejected: whether a
          line EXISTS would depend on the speed of the machine, which
          is the one thing a GOOD file may never hold.


D-10  THE RUN'S GOOD IS A DIGEST, AND THE DIGEST IS PART OF THE ORACLE

PROBLEM   'hwut.run' launches in parallel, so the ORDER of its flow
          is decided by a race. Order is not a fact about the
          subject, and a GOOD that records it records the morning.

DECISION  THE RUN SUITE RECORDS THROUGH A PYPE ('test-run.pype').
          The digest collects the flow, EXPANDS the elision, SORTS on
          (directory, application, choice), RE-ELIDES, and prints.
          EXPANSION COMES FIRST because a live line elides against
          ARRIVAL order: sorting lines still in that form would
          attach ':/: two' to whichever run sorted above it. And only
          then may it elide again -- ONLY A WRITER THAT SORTS MAY
          ELIDE (disc-8), which is why the live flow's elision and
          the digest's are different acts upon one notation.
          THE DIGEST MASKS what belongs to the machine and the
          morning: the timing column, the job column, machine paths,
          the wall clock, and the DOTTED FILL -- whose length is the
          width less the body, and whose body carries the live
          elision mark, so two runs of one suite differed in nothing
          but a count of dots.
          IT MASKS NOTHING ELSE. Masking the clock is
          deterministicalization; masking a verdict is blindness.
          THE PYPE IS THEREFORE PART OF THE ORACLE, and a defect in
          it is a defect in the oracle -- see
          adm/WORK/gathered/06-todo-2-pype-is-part-of-the-oracle.txt,
          which
          the developer's manual must carry.

COST      What a person reads in the run suite's GOOD is the digest's
          account, not literally what the terminal printed. The price
          of determinism under parallel launch.

VERIFIED  Byte-identical across '--jobs=1, 2, 4' and over three
          consecutive runs. Three defects were caught by that check
          and not by inspection: the varying dotted fill, a ':/:'
          that named nothing where a run had no choice, and 'DIR'
          lines printed on arrival while everything else was sorted.

GRAVEYARD MASKING IN TWO PLACES, shell and pype -- rejected as
          policy: one place, or the two disagree. The raw choices,
          which no digest reads, are masked in the suite; the
          digested ones in the pype; both swallow the same kinds of
          fact.

D-11  THE JUNCTION AND THE PLACE                                (2026-09-02)

PROBLEM   Every node of the DIRECTORIES tree ended in the same "o",
          whether it had run something or only led somewhere:
          "'---o region/potpourri" and "'---o solver/TEST" read alike,
          and the eye had to parse the name to tell a branch point
          from a test directory.

RULED     (Frank-Rene) drop the "o"; a node that only branches ends
          in ".".

DECISION  A PLACE ends in nothing, "'---  solver/TEST"; a JUNCTION
          ends in a dot, "'---. region/potpourri". Both six columns
          wide -- the indent below is four per level, and two widths
          would step the whole subtree sideways.

ALSO      'test-run.sh' now prints '<hwut-end>' as its last line.
          None of its sixteen GOODs carried the token, so
          'hwut.accept' refused every one of them and the suite could
          not be corrected -- three of them recorded 'STATUS: 120', a
          timed-out run accepted as the oracle, with nobody able to
          replace it. The token stands after 'esac', so the
          '--hwut-info' arm exits before reaching it. All sixteen
          re-recorded; no GOOD records a timeout any more.

          'jobs-budget' had looked like a timeout for days. It was
          'test-run--jobs-budget.pype' with its EXECUTE BIT LOST -- a
          mode a patch does not carry -- dying on 'Permission denied'
          every run. Restored, with five more pypes under 'test_
          writing_support/hwut_pype/examples/' that had lost theirs.

          Four 'test-plain.py' GOODs were stale since the display
          rework and read as "skip lines vanished". They had MOVED:
          the reason left the flow line for HINTS, as ruled. Every
          differing line named before blessing.

COST      display 20 ok / 4 fail -> 24 ok / 0 fail.

D-12  THE TREE'S INK                                            (2026-09-02)

RULED     (Frank-Rene) one blank after a leaf's connector; everything
          that is TREE in the directory's orange, a junction's name
          included; a leaf's name green up to and including the '/',
          the TEST directory itself red.

DECISION  As ruled, in 'plain.py's tree emitter:

              <208>   +--- <32>alpha/<196>TEST
              <208>   +---. <208>deep
              <208>   |   +--- <32>inner/<196>TEST

          Orange is 256-colour 208, the one colour a directory has
          wherever it is named (DIR band, HINTS head, now the tree).
          Red is 196, pinned, for the reason FAIL_CODE gives: a
          theme may paint palette slot 1 orange, and the place must
          not share a hue with the road. Green is base 32.

          A leaf's connector is five wide, a junction's six. A leaf
          hangs nothing below it, so its width is free; a junction's
          must not step the indent below it sideways. A place that
          ALSO branches keeps six.

          The fill and the tag are measured on the UNPAINTED text and
          painted after, so the dots land where they landed before.

CHECKED   'test-run.sh tree-ink' reduces the coloured tree to its SGR
          codes -- which colour paints which part, and nothing else --
          so the ruling has an oracle. The display's other GOODs are
          the plain rendering and cannot see ink.

COST      Eight GOODs re-recorded for the one blank ('tree-green',
          'tree-fail', 'busy', 'jobs-budget', 'labels', 'linear-raw',
          'short-form'; 'test-plain mixed'); the width was their only
          difference. display 24 ok / 0 fail -> 25 / 0.


D-13  THE SUITE IS RE-RECORDED TO O-24, AND ITS FIXTURE GOES
      THROUGH HWUT'S OWN FACE                                   (2026-09-19)
----------------------------------------------------------------------------
PROBLEM   display stood at 6 ok / 20 fail. Every GOOD of it was last
          recorded 2026-09-02 (D-11, D-12: '20 ok / 4 fail -> 24 ok
          / 0 fail'). O-24 landed 2026-09-07 and MOVED faults and
          notes out of the margin into the flow; nobody re-recorded
          here. The oracle was stale, the code right -- the opposite
          of what a red suite first suggests, and it was settled by
          DATING the rulings against the blessing, not by reading
          the code.

          Three further things stood in the way, none of them O-24:

          THE FIXTURE WROTE ITS NOMINALS BY HAND ('printf >
          GOOD/x.txt'), so the register had no entry for them and,
          since the register moved into the book (B-14), every run
          said 'REGISTERED ... accepted outside the book (E-41)' --
          79 lines. Worse, it says so on the FIRST run only, so the
          homogeneity law's own check ('digest identical to linear',
          O-15) read FALSE in four choices. Blessing that would have
          recorded a broken invariant as the oracle.

          THE DIGEST FILTER, which IS part of the oracle, masked the
          old job column '|n|' but not E-97's count tail '  ||n', and
          masked 'hh:mm:ss' but not the RESULTS footer's seconds.

          A GOOD HELD A WALL CLOCK: 'RESULTS: 1 ok, 0 fail, 0.00
          [sec]' -- a machine-chosen value in an oracle, which fails
          on every machine slower than the one that recorded it.

DECISION  RULED (Frank-Rene): the wall clock is a case for an
          equivalence pattern. 'test-run.sh' states
          'eq_pattern = [..., ", [0-9]+\.[0-9]+ \[sec\]"]'; the
          leading comma is not decoration -- without it the NUMERIC
          tolerance claims the digits first and the pattern never
          fires (measured). The digest filter masks the same footer
          to 's.ss [sec]' and swallows the count tail.

          THE FIXTURE RECORDS THROUGH 'hwut.accept --whole' (E-109),
          one helper 'app <dir> <name> <title> <prints> [<nominal>]'.
          A FAILING fixture is made the way failures are made:
          accepted once saying one thing, the application rewritten
          afterwards to say another. Three things this required, each
          measured: the fixture applications must print '<hwut-end>'
          or accept refuses them (R-70); the candidate accept
          provisioned must be removed after, or a rewrite within the
          same second reads as current by mtime and the run REPLAYS
          the recording instead of running ('beta/test-two' came up
          [OK] where the GOOD says [FAIL], and '--no-store' counted
          accept's candidate as its own); and 'fixture_tree_fail'
          must accept beta's second test ONCE, saying the nominal.

          SEVENTEEN GOODS RE-RECORDED, every differing line named
          before blessing and attributed to O-24, O-25, O-26, E-109
          or the RESULTS footer display grew after 2 September.

          'plain.py's prose is corrected: the constructor still
          described 'write_log' as where marginalia went, and
          'on_fault' still claimed to print the directory (the band
          says it). A doubled BAND comment -- two edits landing on
          one line -- is one comment now.

COST      'test-run.sh labels' STAYS RED, deliberately. Its diff is
          not O-24: a path-bearing word now ENTERS its directory
          (E-47), so the three-directory tree reads as one leaf and
          the glob warns under its entered spelling. That is the same
          unruled question as services E-95 -- a root walk with a
          case-level wish -- and blessing it would pin E-95's
          present behaviour as the oracle before it is ruled.

          'test-plain.py words' grew by one report phrase (O-25,
          'the nominal carries lines nobody has accepted'), which
          shifts every count and every fixture name in that page by
          one; the whole page moves for one word, and that is what
          a table read end to end costs.

D-14  '||n' IS COUNTED UPON PRINT, AND THE FILTER MUST KNOW EVERY
      BADGE                                                    (2026-09-20)
----------------------------------------------------------------------------
PROBLEM   TWO FAULTS, one ruling, and they were confused for each
          other for a whole session.

          THE NUMBER DESCRIBED THE WRONG THING. 'parallel_n' counts
          PROCESSES -- '++' at birth in 'on_run_begun', '--' at death
          in 'on_run_ended' -- while the trace shows ANNOUNCEMENTS. A
          run living less than START_DELAY_SECONDS is born, counted,
          and dies having never appeared as a 'START'. MEASURED:
          'peak standing at once: 9' against a screen holding two
          open 'START's. The number described 'ps'; the reader is
          looking at a screen. Beside it, '_count_tail' returned ''
          below 2, so the last two lines of EVERY directory printed
          no tail at all.

          THE DIGEST FILTER HAD GONE BLIND TO A BADGE. 'peak standing
          at once' is not read off '||n' -- 'test-run.pype' computes
          it from badges and MASKS the tail. Its vocabulary was
          ("START", "DONE", "SKIP", ...). When 'END  ' joined the flow
          badges nobody told the filter, so every 'END' line fell
          through 'badge not in BADGE_TUPLE' and was printed RAW,
          ABOVE the digest, unsorted -- and nothing decremented
          'standing'. Hence peak 9, and hence 'digest identical to
          linear: FALSE' in four choices: raw lines landed in one
          strategy's output and not in another's.

          O-15's homogeneity invariant was never broken. The oracle's
          filter was.

DECISION  RULED (Frank-Rene): '||n' IS THE NUMBER OF ANNOUNCEMENTS
          OPEN ON THIS SCREEN, COUNTED UPON PRINT, THIS LINE'S OWN
          INCLUDED.

              START   opens one; print after the rise
              END     closes the one it opened; print, THEN the fall
              DONE    opened none, so it opens one for the length of
                      its own line and closes it again: rise, print,
                      fall
              SKIP    never ran and is no working job; counts nothing

          Every line therefore prints at least 1. '||0' is impossible,
          '||1' is meaningful, and nothing is suppressed -- the old
          '< 2 -> ""' is why the last two lines of a directory were
          bare. THE TAIL IS DERIVABLE FROM THE PRINTED TRACE ALONE:
          the reader counts the open 'START's above and gets the
          number. 'START', 'END' and '||n' stand on ONE clock.

          THE TWO NUMBERS ARE KEPT APART, not merged. 'parallel_n'
          survives untouched and feeds the '|n|' column of
          '--show-jobs', which asks how hard the MACHINE worked;
          'screen_n' feeds the '||n' tail, which asks what the SCREEN
          shows. Both are legitimate questions and neither answers the
          other. The defect was one name serving two.

          'peak standing at once' ANSWERS THE SCREEN'S QUESTION, by
          the same law, in 'test-run.pype'. RULED: the budget is not
          what the page reports -- the observed parallelism is. That
          the two coincide at 2 and 1 is because the budget binds,
          and the GOOD's numbers stand byte-exact through the cut,
          which is the proof that the cure restored the old meaning
          rather than inventing a new one.

          A FILTER THAT IS PART OF THE ORACLE MUST KNOW EVERY BADGE
          THE DISPLAY CAN WRITE. An unknown badge is not ignored; it
          is mis-parsed, silently, into the wrong half of the page.
          This is D-10's law meeting its first real failure.

COST      'plain.py': 'screen_n' beside 'parallel_n', the '||n' rise
          and fall at the three write sites, '_count_tail' without
          its parameter and without its cutoff -- the 'parallel_n='
          plumbing through '_line' is gone, since a count taken at
          print needs nothing carried to it. 'test-run.pype': two
          badges added, the counting rewritten.

          EIGHTEEN PAGES RE-RECORDED. Classified mechanically first,
          every differing line against a stated rule (bin section 4's
          corollary): 92 '||n' tails, 74 tag column, 16 'DONE ' ->
          'END  ', 7 RESULTS seconds already tolerated by the suite's
          own 'eq_pattern'. NOTHING MATCHED NOTHING -- the six lines
          that matched nothing last session are the defect above, and
          they are gone. 'test-run.sh labels' stays red, blocked on
          E-95, and was not recorded.

          Census 823 ok / 30 fail -> 841 ok / 12 fail, the twelve
          being the thirty less the eighteen, every one pre-existing.

          LEFT STANDING, NOT RULED: a serial run now prints '||1' on
          every flow line, where before it printed no tail at all.
          That is what 'nothing is suppressed' costs, and whether a
          one-job run should carry the tail at all is a question
          nobody has been asked.

D-15  TWO MODES, AND THE BADGE SAYS WHICH ONE YOU ARE IN    (2026-09-20)
----------------------------------------------------------------------------
PROBLEM   Whether a run got a pair of lines or a single one was
          decided BY TIMING. A START was held for
          START_DELAY_SECONDS; a run that died inside the window
          never announced itself and closed with 'DONE ', one that
          outlived it closed with 'END  '. So the badge was an
          artefact of the machine's speed, a reader could not say why
          one line read 'DONE ' and the next 'END  ', and WHETHER A
          LINE EXISTED AT ALL depended on the morning -- the one
          thing a GOOD file may never hold. Every fixture that
          recorded a flow had to say '--start-delay=0' to escape it.

DECISION  RULED (Frank-Rene): TWO MODES, and the caller states which.

              NORMAL   every launch writes 'START', every termination
                       writes 'END  '. No 'DONE '. '||n' counted UPON
                       PRINT (D-14), this line's own run included.
              --brief  no launch announced. One 'DONE ' per run, at
                       its termination. '||n' from the LAUNCHER,
                       because nothing is announced and counting
                       announcements would say '1' on every line.

          IN BOTH, '||n' STANDS AT THE END OF THE LINE, where it
          always stood. Nothing was added to the closing blocks.

          '--brief' IS NOT A TIER. A tier says HOW MUCH is shown --
          details, provision nodes, faults. '--brief' says HOW A RUN
          IS ANNOUNCED. The two are orthogonal and a VERBOSE brief
          run is a meaningful thing to ask for, which a fifth tier
          could not spell.

          THE HOLD MACHINERY IS GONE: START_DELAY_SECONDS, 'held_db',
          'on_tick's release loop, '_release_held' and the
          '--start-delay=<seconds>' option. 'on_tick' stays, a no-op,
          because it is the receiver protocol's.

          THE BADGE ASKS ONE QUESTION: does a 'START' stand above?
          'announced_set' answers it, 'END  ' where yes, 'DONE '
          where no, 'SKIP ' where the node never ran. Under '--brief'
          the answer is always no, so the mode needs no branch of its
          own in the closing path -- it is simply 'never announce'.
          GRAVEYARD: E-97's 'a START is held until the run proves
          slow', and with it the reasoning that a count must be taken
          at the event because a held line is written late.

          ':' STANDS ONLY WHERE A CHOICE IDENTIFIES THE LINE. This
          law was stated in 'test-run.pype' from the day it was
          written and had never been given to the display. D-15 made
          the gap universal: every END repeats its own START's
          application, so in a suite of CHOICELESS runs every verdict
          line read ':' and named nothing. MEASURED, it broke a
          reader -- 'services test-base-questions.py' collects what a
          wish selected by finding a name beside a verdict, and found
          none, so '--fail selects what last failed' failed. One law,
          one place: the display keeps it now.

          A READER OF AN ELIDED FLOW MUST EXPAND BEFORE IT COMPARES.
          Where a CHOICE does stand, ':' is correct and the verdict
          line rightly does not repeat the file. 'services
          test-gate.py' scanned verdict lines for a file name and
          went red; the display was right and the reader was never
          expanding, which is the rule 'test-run.pype' keeps
          ('EXPANSION COMES FIRST'). The reader was taught to expand
          and ITS GOOD STOOD BYTE-EXACT -- the proof of which half
          was broken.

COST      'plain.py': 'brief_f' for 'start_delay', 'announced_set',
          'on_run_ended' split so the counts fall exactly once in a
          'finally' whatever path the line took. 'console.py':
          '--brief' for '--start-delay=<seconds>'.
          'test-plain.py badge' rewritten to show both modes;
          'test-run.sh jobs-budget' no longer pins a delay, because
          NORMAL guarantees by construction what the pin was for.
          'test-gate.py' expands elisions.

          TWENTY-ONE PAGES RE-RECORDED, classified mechanically
          first: 257 lines the START/END pair and its '||n', 43 the
          retired option and the usage line rewrapping round its
          absence, 2 the rewritten fixture's banners. NOTHING MATCHED
          NOTHING.

          Census 841 ok / 12 fail -> 841 ok / 12 fail, THE SAME
          TWELVE. A behaviour cut of this size that moves no
          pre-existing failure is the strongest evidence available
          that it moved only what it meant to.

          A NORMAL SUITE'S FLOW IS NOW TWICE AS LONG, and that lands
          on the default path. '--brief' is the old compactness, and
          it is opt-in.

D-16  A RAW FLOW OF TWO RUNS PINS ORDER AND SHAPE           (2026-09-22)
----------------------------------------------------------------------------
PROBLEM   'test-run.sh fail' blessed a raw flow of two runs with no
          ordering word, and three facts foreign to 'hwut.run' decided
          it. THE ORDER: the fixture is accepted through hwut's own face
          (bin 6), 'accept' PROVISIONS, and its runs leave
          'hwut-traces.csv' behind -- so the default longest-first order
          was two millisecond durations; measured 4 of 20 red on one
          core. THE SHAPE: the default budget is 'os.cpu_count()' of the
          recording machine; every GOOD and census on record came from
          a one-core sandbox; at eight cores the runs interleave with
          '||2', measured 3 of 3 red. THE END ORDER of overlapping runs,
          a race. 'labels' carried the first of these in its bare call:
          '--strategy=linear' keeps longest-first, so DIRECTORIES were
          ordered by accept's traces -- three orders in 12 runs, hidden
          because the page is red for E-95.

DECISION  RULED (Frank-Rene): a page that blesses a RAW flow of two or
          more runs says '--deterministic --jobs=1'. '--deterministic'
          fixes the ORDER; only '--jobs=1' fixes the SHAPE. Neither
          alone is the cure: '--deterministic' alone read 0 of 20 red
          on one core and was still a different page at two.
          GRAVEYARD: the digest pype for 'fail' -- it would hide the
          inline FAIL phrase the choice exists to show; deleting the
          fixture's traces after accept -- it cures the order only.

COST      Two face calls and the header's sentence in 'test-run.sh'. No
          GOOD moved: 'fail' stands byte-exact, 0 of 10 red at one core
          and at eight; 'labels' bare section now steady, the page still
          red for E-95 and its GOOD stale (D-14/D-15), not recorded.
          The same rule, applied beyond this page: 'services
          test-accept.sh interactive' greps verdicts from a raw run of
          three cases -- three outputs in three runs at eight cores,
          steady after; the page stays red for test_run_info.

D-17  THE USAGE BLOCK STANDS WHERE IT IS THE SUBJECT, ONCE     (2026-09-22)
----------------------------------------------------------------------------
PROBLEM   A refusal prints 'hwut.run's usage block after the REFUSED
          line, and 'test-run.sh' recorded it seven times: five in
          'refused', once each in 'timing' and 'strategy-refused'. One
          fact the tree states, seven copies, and every new option
          moved three pages -- two of which test something else.

DECISION  RULED (Frank-Rene): the block is recorded where it is the
          subject -- 'refused', with its FIRST refusal. Every other
          refusal on the page records the REFUSED line alone, through
          'refusal()', which is 'face()' with the block cut. GRAVEYARD:
          an eq_pattern over the block -- a pattern over what the tree
          fully decides, and it would hide the one copy that matters.
          A pype would serve as well; the cut is a single 'sed' and
          needs none.

COST      'refusal()' and five call sites in 'test-run.sh'. Three pages
          re-recorded, classified first: 60 lines removed, none added,
          every one a usage line.

D-18  A CLOSING LINE DOES NOT COUNT ITSELF                      (2026-09-23)
----------------------------------------------------------------------------
PROBLEM   D-14 counted '||n' UPON PRINT with the line's own run
          included, so an 'END' said how many stood open INCLUDING the
          one it was closing. The last line of a serial run read
          '||1' while nothing stood open any more, and '||0' was
          called impossible.

DECISION  RULED (Frank-Rene): 'END' and 'DONE' show n-1, never below
          zero. A closing line says WHAT OUTLIVES IT; 'START' still
          counts itself, having just opened. So a serial run now falls
          to '||0' on its last line, and the reader counting open
          STARTs above a closing line gets exactly its number.
          The decision is taken on the line's OWN badge, before the
          elision to a repeat, so a closing line that lost its badge
          to the line above is still counted as the closing line it is.

COST      One tuple ('CLOSING_BADGE_TUPLE') and one subtraction. Eleven
          nominals moved, classified first: every differing line is a
          closing line whose count fell by exactly one. D-14's "'||0'
          impossible" is superseded.

D-19  THE TERMINAL IS ASKED FOR ITS WIDTH                       (2026-09-23)
----------------------------------------------------------------------------
PROBLEM   'console_width' read 'COLUMNS' alone. Most shells do not
          EXPORT it, so a report on a 200-column screen drew 78.

DECISION  RULED (Frank-Rene): ask the terminal. 'COLUMNS' still wins
          where it stands and reads as a number -- a stated width is
          meant -- and otherwise 'shutil.get_terminal_size' asks the
          device. Held between 40 and 120 as before: the cap is the
          eye's, not the screen's. A DEVICE ANSWERING ZERO, or none to
          ask, falls back to the deterministic 78.

COST      A 'size_of' seam so the policy is testable without a
          terminal ('test-plain.py width', eight cases). NOT A TTY IS
          UNTOUCHED AT 78: every nominal is captured, so no GOOD moves
          and the determinism law is unaffected (O-30).

D-20  THE LINE IS AS WIDE AS THE TERMINAL SAYS                  (2026-09-23)
----------------------------------------------------------------------------
PROBLEM   E-97 put the '||n' badge after the verdict column and
          OUTSIDE the width math, so every flow line was drawn five
          columns wider than the width the terminal was asked for.
          MEASURED through a pty: 60 columns drew 65, 100 drew 105,
          200 drew 125 (the 120 cap plus the badge). On a terminal of
          exactly that width the tail wrapped.

DECISION  RULED (Frank-Rene): the console width IS the width printed.
          The badge is counted in the fill and in the padding of a
          line without a verdict. MEASURED after: 60 -> 60, 100 ->
          100, 200 -> 120.

COST      Eleven pages re-recorded, classified first: the dotted fill
          and the padding are five columns shorter, and nothing else
          on any line changed. D-18 ('a closing line does not count
          itself', '||0' on the last serial line) and D-19 (the
          terminal is asked, 'COLUMNS' wins where it stands, held
          between 40 and 120) were already built and stand untouched.
