SNAPSHOT -- A-13 production idiom + quantifier-router retraction
================================================================
Baseline: green=60 diff=0 err=0 skip=9

THE LAW (RATIONALE A-13): a rule names the product its customer expects on
success; each shape states the RANGE. SEQ->sequence; OR->one alternative,
expressive only with plain role-tagged arms (index = degenerate fallback);
STAR/PLUS->one list-node, children are the 0..n / 1..n matches, NO per-arity
product routing; OPT->present-transform only, ABSENT fixed to None (the syntax
must enforce its meaning, so the author cannot route absence). Failure is a
product (None), not an exception.

CHANGES
  core/parser_generator/ast_map_family.py
    - REMOVED E_Count, StarMap, PlusMap (the earlier arity-router step, retracted)
    - OptMap reshaped to ONE-ARMED: OptMap(present=PASS); absent -> None FIXED,
      no absent parameter (A-13 non-negotiable)
    - family is now two routers: OrMap + OptMap
    - preamble rewritten (WHY NO StarMap/PlusMap; WHY OptMap one-armed)
  parser/ast_map.py
    - _opt_by simplified to OptMap(lambda ...); shape-gate routers = (OrMap,OptMap)
    - docstrings updated (SEQ/STAR/PLUS take a plain factory)
  core/parser_generator/operator_interface.py  doc: drop StarMap mention
  parser/TEST/test-ast-map-router.py
    - run_leaves rewritten; ASSERTS absent->None regardless of present-leaf
    - StarMap case removed; imports/header updated
  parser/TEST/test-ast-signals.py  router-detection drops StarMap
  parser/RATIONALE.txt  + A-13 (production idiom)
  parser/DISCUSSIONS/disc-10  annotated: A-13 supersedes "one rule one type"
  GOOD: leaves + exemptions regenerated (test-text changes, value-exact)

LAWFUL-AS-IS (A-13 absolves them; NOT converted)
  _left_fold / _un_fold : SEQ customers folding a child list-node/opt against a
  head -> a member of the rule's bounded range. Never the violation.

STILL THE ONE RESIDUAL VIOLATION (-> pass 2; disc-10/11)
  _comparison's isinstance(left, ReferenceLeaf): content selection, no bounded
  range. The only reduce that must move to the semantic layer.
