==============================================================================
OPEN -- the by-subject register of everything not yet closed      (R-41)
==============================================================================
Consolidates disc-9, todo's F-entries, and the SEMANTICS deferral lists as
of R-41. Structure: one subject per section; within each, RULING NEEDED
(FR's word closes it) before BUILD (settled, awaiting implementation)
before DEFERRED-BY-DESIGN (bound to a later phase, marked in SEMANTICS).

(0) THE R-41 SWEEP  (settled entire; the next build station)
------------------------------------------------------------
Two attributable passes, each green before the next (sequencing (A),
atomic within each pass):
S1 SURFACE
  - Panel flat: 'in:' / 'out:' / 'signals:'; per-entry '[known:|had:]'
    markers, having implicit ('had:' optional visibility). Retires
    knows:/takes:/gives:/ticks: (R-41.1).
  - Site marking: 'known:' on binding targets; '=' relation-neutral
    (R-41.2). The acquaintance-take from a data access is a NEW
    statement kind.
  - Class-tail dissolution: has:/knows: blocks die; one body brace,
    member declarations + member works interleaving freely (R-41.3,
    interleave (A)). Factored-head parse per D-20 precedent (the
    'name :' head is shared -- LL(2) resolution planned).
  - Old-surface dissolution: behaviors, aspects, characters same --
    a behavior IS a class with reactive elements (R-41.10).
  - Terminals: 'give: <ports>' in works (names the out-ports; checked
    against panel); 'give:' UNLAWFUL in clockworks; clockworks tick and
    end by exit:; bare 'exit:' = the nothing-more egress (R-41.4/7).
S2 CONTROL
  - break:/continue: retired (supersedes R-14); dropto: the sole jump.
  - Placement laws (a) block-end label (b) after-brace label (c) never
    into a signal handler (d) no label inside a rule construct (R-41.8).
  - Catch region ':label: => { ... }' REMOVED from grammar+interpret
    (R-41.9 cancels R-39's second account, D-31).
  - for: ... else: -- plain block, orderly exhaustion only; dropping
    out skips it; label after the ENTIRE construct.
After both: fixtures rewritten, GOOD re-baselines audited, LANGUAGE
12.2/12.4/13.2/13.3/13.5 + SEMANTICS 23 re-speak, HAVE_KNOW_BE touch.

(1) WORK CONSTRUCT
------------------
RULING NEEDED
  - The built-in exhaustion variant's NAME in the puller's match domain
    ('finished' today; FR's words "nothing more"/"no more").
  - Bare 'exit:' in WORKS (a no-result egress; the 'inappropriate
    result' thread) or clockwork-only; if works: the built-in's name
    and pre-declared vs auto-injected.
  - Whether 'tick:' carries the out-list ('tick: value') under the
    names-its-ports rule (coordinator lean: yes) or stays bare.
  - The site-marker/panel AGREEMENT LAW (lean: checked both directions,
    like '+'/'-').
  - Bare 'exit:' concrete spelling ('exit:;' with the statement
    semicolon, or semicolon-free like give:/tick:) -- micro.
  - C-1 residual: the port-map OUTPUT association spelling; form
    RESERVED in 12.5 (touched by the in:/out: re-speak).
BUILD (settled)
  - elsewhile: (intra-instant, R-41.6 -- a frame-local loop, no bus
    boundary) and the 'wait' construct (D-2 family).
  - '==' common ground; specs and work references; indirect calls;
    dispatch tables.
DEFERRED-BY-DESIGN
  - Handler exhaustiveness and dead arms; the mandatory-else direction;
    same-type/discard; overload call-site checks beyond aware
    mutations; cross-module overload sets; named-port overload
    refinement (SEM 24/26). (Definite assignment of give:-ports moved
    into the sweep -- the port-list gives it its surface.)

(2) CLASS AND CUSTODY
---------------------
RULING NEEDED
  - R-31 interpretation flags ('+/-' definition-only; disposal needs no
    semi; inline member works stay lawful).
  - R-29 flags: the aggregation-literal member binder ':'.
  - The behavior PARENS LIST ('~behavior(power, boost=1)') --
    construction arguments; whether it adopts the panel form rides the
    construction chapter (R-41 flag).
BUILD (settled)
  - The aggregation literal 'sprite{pos: v, tex: give t}' -- settled in
    11.3, NO GRAMMAR YET.
  - 'is: obligation' marker.
  - The disposal DISPATCH at run time (destruct: currently traces and
    ends the having; the '-' work body does not yet run).
  - Character/aspect/behavior integration under the class concept:
    RULED IN PRINCIPLE (R-41.10); the dissolution rides the sweep;
    reactive-specific residue (activation, ~, is:-chains) keeps its
    chapter.
DEFERRED-BY-DESIGN (the type-flow phase)
  - SEMANTICS 27's three path checks (explicit classes: all-paths
    give-or-destruct; container remainder; aggregate members).
  - The custody ledger entire; the call-site 'give' marker; the
    give-half of SEMANTICS 30 (know-after-give); views.

(3) CONTAINERS (have/know)
--------------------------
RULING NEEDED
  - Insertion/removal verbs of the have-container (insert takes 'give
    x'; what removal reads as); whether dict/struct get the pair or
    list leads. (Iteration custody: the 'known:' site marker now
    spells the borrow -- R-41.2 largely answers the default; confirm.)
BUILD (settled)
  - 'have list'/'know list' parse (D-30); semantics beyond the parse --
    collective debt, 27(ii) linkage -- ride the type document.

(4) NOTHING AND KNOWNNESS
-------------------------
RULING NEEDED
  - The activation-scope fork (R-38 second flag): strict-everywhere vs
    type-gated; activation-by-mention is the coordinator's bridge.
  - The transposed-branches flag (R-38 first flag).
BUILD (settled)
  - The knowing model's runtime (known hubs: pointer + knower count;
    binds the EMITTER -- HAVE_KNOW_BE (3)).
DEFERRED-BY-DESIGN
  - Compound gate conditions; deeper divergence shapes; knownness of
    class members and reactive contexts (SEM 28).
  - SEM 30 branch JOINS (destruct in all arms => dead after the if).

(5) EPHEMERALS
--------------
BUILD (settled)
  - The event-typed panel entry 'event(...)' and its matcher (14.2).
DEFERRED-BY-DESIGN
  - D-4: the event/causality terminology sweep.

(6) CLOCKWORK
-------------
BUILD (settled)
  - Family-split terminals ride the sweep (no give:; tick: delivers;
    bare exit: exhausts; for-else -- R-41.4/7, S1/S2).
  - groove:; winddown:; multi-input pulls; pipelines.
  - Enforcement: single-puller; no-cycle (the 13.1 sentence is a
    coordinator addition -- RULING NEEDED on its wording).

(7) LABELS AND ROUTING
----------------------
  elseto: and the catch region CANCELLED (R-41.9). One label account
  remains: the dead address ':name:'. Placement laws and loop control
  ride the sweep (S2). Routing idiom: else:-arm copies payload out,
  dropto: a shared label.

(8) ENGINE BOUNDARIES (found en route; rule or accept)
------------------------------------------------------
  - Module-level causalities do not dispatch at run time (top-level
    'X => Y;' is structural only; top-level entities auto-activate).
  - Port-name shadowing of binding heads (e/b/a/c/s) is unruled.
  - F-10 residue: works in GUARDS ('when:' has no handler position);
    statement-form calls of signalling works.

(9) THE BACK HALF (Luau)
------------------------
  - link/pack/emit entire. Staged mappings: the generative clockwork
    walk ==> coroutine.yield (P-series); known hubs ==> the emitter's
    weak-reference structure (HAVE_KNOW_BE (3)); give:/exit: ==> the
    sum-return convention.

(10) PROCESS AND DOCUMENTS
--------------------------
  - werk-documents errata (FR's own; post-R-33..R-41 updates).
  - Explanation bin growth (candidates: signals/forced translation;
    clockwork's three waits; overloads; Nothing gates; the flat panel
    and site marking; dropto loop idioms).
  - The ~35-docstring core backlog (awaits FR scope call).
  - SESSION NOTE (R-41): an unattributed mid-session write to
    RATIONALE.txt was detected, diffed against the canonical dump,
    and reverted; the register keeps the fact on record.
