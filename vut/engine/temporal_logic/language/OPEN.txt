==============================================================================
OPEN -- the by-subject register of everything not yet closed      (R-41)
==============================================================================
Consolidates disc-9, todo's F-entries, and the SEMANTICS deferral lists as
of R-41. Structure: one subject per section; within each, RULING NEEDED
(FR's word closes it) before BUILD (settled, awaiting implementation)
before DEFERRED-BY-DESIGN (bound to a later phase, marked in SEMANTICS).

(0) THE R-41 SWEEP  (settled entire; the next build station)
------------------------------------------------------------
Two attributable passes, each green before the next (sequencing (A),
atomic within each pass):
S1 SURFACE  [BUILT -- 68/68 green; fixtures + EXPLANATION rewritten,
             GOOD re-baselines audited, LANGUAGE/SEMANTICS/HAVE_KNOW_BE
             re-spoken; S2 untouched]
  RULED (follow-up, Q&A session):
  - Homogeneity: ONE marker-admitting entry shape in every parens (kind
    parameters included; unmarked = had); signal payloads stay plain.
  - Behaviour body order -- SUPERSEDED by the reactor ruling: a
    behavior is ONLY causalities, so the order law is moot (retired).
    Cross-kind position in class/reactor bodies: unruled.
  - SEM 28 is ALWAYS ACTIVE, flow-sensitive: only UNGATED known access
    rejects; gated flow is fine. Mention-gating removed; the
    strict-vs-type-gated fork is CLOSED (strict, flow-sensitive).
  - Give leaves Nothing behind (LAW, ruled): the giver holds Nothing
    in the given thing's place; access to the given impossible; and
    NOTHING CAN NEVER BE GIVEN (a known is never had). Checks land
    with the written give sites when they land.
  - 'for: ... from: give <src>' RETIRED: from: takes the wound
    generator plainly ('generator = cw(args); for: v from: generator').
  - GATE EQUIVALENCE: 'm != Nothing' == 'm != 0' == bare 'm' (one
    gate; Nothing compares as 0, reads false); '=='-forms mirror.
  - AGREEMENT is ONE-WAY: a known out: entry MUST be received at a
    known:-marked site; a custody out: entry MAY be received known
    (unclaimed custody is had elsewhere or destructed; the hub hands
    readers Nothing).
  - COMMAND SPELLING: give/tick/signal/destruct/dropto are BARE words
    (R-43 renamed the ruled 'exit' to 'signal');
    EVERY simple statement ends with ';'. (break:/continue: untouched
    here -- they die in S2.) Bare 'give' being the statement, the
    call-site custody marker of disc-9 needs a NEW spelling -- open.
  - UNREACHABLE CODE IS AN ERROR: after give/signal/dropto, flow resumes
    only at an exit-label; give is always the last statement on its
    path (REJECT [STRUCTURE]).
  - GIVE-LIST ORDER: the written list MIRRORS the panel's declaration
    order exactly (REJECT [WORK]); one order everywhere.
  - THE REACTOR (major): character and aspect REMOVED; one reactive
    kind -- 'reactor' (state machine) / 'reactor++' (mode group), a
    CLASS with behavior content and an event recipient. A behavior is
    ONLY causalities (no members/works/params/instances), declared only
    inside a reactor; causalities exist ONLY inside behaviors inside
    reactors (top-level causalities died). 'A => Name' activates the
    behavior in THIS reactor; '=x=>' deactivates. State lives on the
    reactor; SELF is the bare-dot binding '.x' (classes and reactors);
    b/a/c/s bindings died.
  - THE PIPE MODEL -- RULED AND BUILT (the pipe pass): the ruling
    stands recorded as law in LANGUAGE 15 (channels and the panel;
    wiring is work; emit routing 'to'/fan/self; channel groups and the
    group law; delivery with the known-hub skip and the drop line;
    feeders) and SEMANTICS 31 (the four [PIPE] checks, wire-end checks
    PARTIAL). The bus, the interim activation scaffold, and the reactor
    activate/deactivate machinery are REMOVED; construction is standing
    ('reactor' first behavior, 'reactor++' all armed); behavior
    enter/leave trace lines survive. The harness feeder script("...")
    replays R-42 payload entries; runtime fixtures and the explanation
    demos are re-shaped into plants, GOODs re-audited. Fresh flags ride
    section (8): plain-arrow interim law, wire-outside-works rejection,
    duplicate wires/channels, the drain and feeder siblings, the
    SEMANTICS-1 reactor-row re-speak, destruct auto-cancel, harness
    verbs, the early-regex lexer tier, wire ends bare-locals-only.
  - THE SIGNAL STATEMENT -- RULED AND BUILT (R-43, the signal-flow
    pass): 'exit' renamed 'signal'; ONE statement, the position decides
    the channel spec -- behavior work code takes the optional
    'to <channel>' and is the EMISSION (equivalent to the arrow,
    suffix-less fans, flow continues); normal work code takes none and
    is the fault egress (LANGUAGE 12.4/15.3, SEMANTICS 23/31 (ii)).
    The word 'exit' left the language; the internal noun 'exit-label'
    (the ':name:' form -- its concrete syntax never spoke 'exit')
    survives, rename flagged in (8).
  - THE ARM/CAUSALITY MERGE -- RULED AND BUILT (R-44, R-45): the arm
    IS a causality in form ('(<event> | ~ANY) [when:] => <action>',
    e.<field> payload, parens-destructure and bare default arm
    RETIRED); dispatch FIRST-MATCH uniformly (behaviors converted from
    all-match; scope: per active behavior -- coordinator
    interpretation, flagged); '~ANY' in both worlds, 'e' Nothing under
    it; coverage-by-testimony the SEM-24 law (dead-arm-after-blanket
    checked; resolved-callee coverage deferred with the PARTIAL);
    signals ARE events (R-45): 'name : event(fields);' the
    freestanding definition, a signals: entry the same creature in
    panel position; explain-no-exceptions.vut carries the design
    point (the suite is 69 choices).
  - THREE ARROWS -- RULED AND BUILT (R-46): '=>' spawns event/work
    code; '=!=>' activates a behavior ('reactor': the transition, an
    atomic leave/enter; 'reactor++': the set grows); '=x=>'
    deactivates (reactor++ only for behaviors -- a state machine may
    never stand stateless: REJECT; the recurring handle stays
    cancellable in both modes -- coordinator interpretation, flagged);
    a behavior target under '=>' REJECTS; '=!=>'/'=x=>' take a bare
    target; the causality gains the SHRUG ('=> ;' -- reckoned,
    ignored, consumes under first-match); 'every:' ruled meaningless
    in else:/elsewhile: arms and in groove:. SEMANTICS 1 deactivable
    table re-spoken (the reactor row DIED -- retires the standing
    fork). Docs: two-'go'-causalities examples re-drawn as CHAINS
    ('go => blink to note =!=> GREEN;') -- the first-match idiom.
  - THE PHYSICAL UNIT TYPE -- RULED AND BUILT (R-50, the here-and-now
    round): literal '<number> [<unit>]'; type 'physical[<unit>]'; the
    unit a product/quotient ('*' AND whitespace multiply, '/' left-
    assoc, exponents '^int' / '^(p/q)' / Unicode superscripts);
    SURFACE -> CANONICAL rational-exponent vector over the seven SI
    bases -> DISPLAY as the quotient of nominator to denominator;
    derived names via UNITS.txt; algebra B -- the unit is CARRIED
    (derive on * /, same-vector law on + - and comparison, scalar
    scaling, dimensionless collapse, united P-5 zero, runtime
    'unit_mismatch' built-in fault beside div_by_zero); suite 70.
  - THE F-9 SLICE -- BUILT (R-52, no new rulings): the static unit
    net (inference + [UNIT] rejects, handler-suppressed per SEM-21);
    mandatory-handler and expression-position (12.6); testimony over
    resolved callees + primitive built-in arm law (SEM-24); signal/
    emission arity (R-45/R-47). Twelve reject lines of test coverage;
    all prior runtime GOODs byte-unchanged. Resolution PARTIAL:
    top-level single-segment callees.
  - EXPONENT FLOATS (Q9, ruled): the exponent marks float like the dot
    does -- '1e-6', '2.5e3', '1E+10' lex as floats; bare digits stay int.
  - Freshly OPEN by this ruling: abstract reactors / behavior
    inheritance (old ~/is: on behaviors died -- SEM 17 dormant);
    reactor construction/instances beyond the scope default (a reactor
    is a class: works construct it -- wiring to activation is the pipe
    model's question); behavior params (died with s.).
  - Panel flat: 'in:' / 'out:' / 'signals:'; per-entry '[known:|had:]'
    markers, having implicit ('had:' optional visibility). Retires
    knows:/takes:/gives:/ticks: (R-41.1).
  - Site marking: 'known:' on binding targets; '=' relation-neutral
    (R-41.2). The acquaintance-take from a data access is a NEW
    statement kind.
  - Class-tail dissolution: has:/knows: blocks die; one body brace,
    member declarations + member works interleaving freely (R-41.3,
    interleave (A)). Factored-head parse per D-20 precedent (the
    'name :' head is shared -- LL(2) resolution planned).
  - Old-surface dissolution (R-41.10) -- SUPERSEDED by the reactor
    ruling: aspect and character REMOVED; a behavior is ONLY
    causalities inside a reactor; the reactor is the class with
    behavior content.
  - Terminals: 'give <ports>;' in works (names the out-ports; checked
    against panel); 'give' UNLAWFUL in clockworks; clockworks tick and
    end by signal; bare 'signal;' = the nothing-more egress (R-41.4/7,
    command-spelling ruling; R-43 renamed 'exit' to 'signal').
S2 CONTROL
  - break:/continue: retired (supersedes R-14); dropto the sole jump.
  - Placement laws (a) block-end label (b) after-brace label (c) never
    into a signal handler (d) no label inside a rule construct (R-41.8).
  - Catch region ':label: => { ... }' REMOVED from grammar+interpret
    (R-41.9 cancels R-39's second account, D-31).
  - for: ... else: -- plain block, orderly exhaustion only; dropping
    out skips it; label after the ENTIRE construct.
After both: fixtures rewritten, GOOD re-baselines audited, LANGUAGE
12.2/12.4/13.2/13.3/13.5 + SEMANTICS 23 re-speak, HAVE_KNOW_BE touch.

(1) WORK CONSTRUCT
------------------
RULING NEEDED
  - The built-in exhaustion variant's NAME in the puller's match domain
    ('finished' today; FR's words "nothing more"/"no more").
  - Bare 'signal;' in WORKS (a no-result egress; the 'inappropriate
    result' thread) or clockwork-only; if works: the built-in's name
    and pre-declared vs auto-injected. (R-43: in BEHAVIOR work code a
    bare 'signal;' REJECTS -- the emission names its event.)
  - Whether 'tick' carries the out-list ('tick value;') under the
    names-its-ports rule (coordinator lean: yes) or stays bare.
  - The site-marker/panel AGREEMENT LAW -- RULED AND BUILT (Q&A
    session): known/had are part of the TYPE; the receiving side must
    receive into the appropriate one, BOTH directions (REJECT [TYPE]),
    checked where the callee resolves (PARTIAL: top-level works and
    selector-resolved overloads; the rest joins the type document).
    Corollary ruled and built: NOTHING CAN NEVER BE GIVEN -- 'give' on
    a known rejects (a known is never had, so never given).
  - Bare-egress concrete spelling -- SETTLED by the command-spelling
    ruling, renamed by R-43: 'signal;' (every simple statement
    ends ';').
  - C-1 residual: the port-map OUTPUT association spelling; form
    RESERVED in 12.5 (touched by the in:/out: re-speak).
BUILD (settled)
  - elsewhile: (intra-instant, R-41.6 -- a frame-local loop, no bus
    boundary) and the 'wait' construct (D-2 family).
  - '==' common ground; specs and work references; indirect calls;
    dispatch tables.
DEFERRED-BY-DESIGN
  - Handler exhaustiveness and dead arms; the mandatory-else direction;
    same-type/discard; overload call-site checks beyond aware
    mutations; cross-module overload sets; named-port overload
    refinement (SEM 24/26). (Definite assignment of give-ports moved
    into the sweep -- the port-list gives it its surface.)

(2) CLASS AND CUSTODY
---------------------
RULING NEEDED
  - FR THOUGHT WINDOW (pipe session; wording to be confirmed into
    RATIONALE when the give sites land): class, reactor, and clockwork
    objects shall NOT give away what they have as PLAIN MEMBERS;
    containers (list, set, dict) SHALL give their elements. An object
    that creates things stores them into a member CONTAINER -- the
    dynamic place, if configured so. Adds clarity to the object model:
    plain members are structurally fixed, containers are the custody
    exits.
  - R-31 interpretation flags ('+/-' definition-only; disposal needs no
    semi; inline member works stay lawful).
  - R-29 flags: the aggregation-literal member binder ':'.
  - The behavior PARENS LIST -- DIED with the reactor ruling
    (behaviors take no parameters); reactor construction rides the
    class model (works) and the pipe question.
BUILD (settled)
  - The aggregation literal 'sprite{pos: v, tex: give t}' -- settled in
    11.3, NO GRAMMAR YET.
  - 'is: obligation' marker.
  - The disposal DISPATCH at run time (destruct currently traces and
    ends the having; the '-' work body does not yet run).
  - Character/aspect/behavior integration under the class concept:
    RULED IN PRINCIPLE (R-41.10); the dissolution rides the sweep;
    reactive-specific residue (activation, ~, is:-chains) keeps its
    chapter.
DEFERRED-BY-DESIGN (the type-flow phase)
  - SEMANTICS 27's three path checks (explicit classes: all-paths
    give-or-destruct; container remainder; aggregate members).
  - The custody ledger entire; the call-site 'give' marker; the
    give-half of SEMANTICS 30 (know-after-give); views.

(3) CONTAINERS (have/know)
--------------------------
RULING NEEDED
  - Insertion/removal verbs of the have-container (insert takes 'give
    x'; what removal reads as); whether dict/struct get the pair or
    list leads. (Iteration custody: the 'known:' site marker now
    spells the borrow -- R-41.2 largely answers the default; confirm.)
BUILD (settled)
  - 'have list'/'know list' parse (D-30); semantics beyond the parse --
    collective debt, 27(ii) linkage -- ride the type document.

(4) NOTHING AND KNOWNNESS
-------------------------
RULING NEEDED
  - The activation-scope fork (R-38 second flag): strict-everywhere vs
    type-gated; activation-by-mention is the coordinator's bridge.
  - The transposed-branches flag (R-38 first flag).
BUILD (settled)
  - The knowing model's runtime (known hubs: pointer + knower count;
    binds the EMITTER -- HAVE_KNOW_BE (3)).
DEFERRED-BY-DESIGN
  - Compound gate conditions; deeper divergence shapes; knownness of
    class members and reactive contexts (SEM 28).
  - SEM 30 branch JOINS (destruct in all arms => dead after the if).

(5) EPHEMERALS
--------------
BUILD (settled)
  - The event-typed panel entry 'event(...)' and its matcher (14.2).
DEFERRED-BY-DESIGN
  - D-4: the event/causality terminology sweep.

(6) CLOCKWORK
-------------
BUILD (settled)
  - Family-split terminals ride the sweep (no give in clockworks;
    tick delivers; bare signal exhausts; for-else -- R-41.4/7, S1/S2,
    R-43).
  - groove:; winddown:; multi-input pulls; pipelines.
  - Enforcement: single-puller; no-cycle (the 13.1 sentence is a
    coordinator addition -- RULING NEEDED on its wording).

(7) LABELS AND ROUTING
----------------------
  elseto: and the catch region CANCELLED (R-41.9). One label account
  remains: the dead address ':name:'. Placement laws and loop control
  ride the sweep (S2). Routing idiom: else:-arm copies payload out,
  dropto to a shared label.

(8) ENGINE BOUNDARIES (found en route; rule or accept)
------------------------------------------------------
  - EARLY-REGEX LEXER TIER (pipe build): the ruled wire-arrow classes
    (r'--+>', r'--+\[') cannot lex in the last tier -- the captured '-'
    shadows them (first-match alternation). Built: T.regex(pattern,
    early=True) emits in a tier 3b BEFORE the bare keywords; the
    bare-':' last-tier design untouched; existing corpus lexes
    identically. Accept or re-rule.
  - PIPE FLAGS (pipe build, ruling needed -- one at a time):
    * FEEDER EVENT PAYLOAD syntax -- RULED (R-42): the D-28
      named-argument form per entry, ';'-separated, numeric and string
      literals admitted; built. The fixture re-shape is unblocked.
    * PLAIN-ARROW channel: interim law built -- the plain wire rides
      the source's SINGLE out: channel (several or none: REJECT);
      a feeder source rides its one provided out channel, the
      destination's single in: names the subscriber side.
    * WIRE ENDS are bare locals (grammar): a dotted end (member-held
      instance) would break the LL(2) budget against the mutation
      head; deferred.
    * Wire statements outside works: grammar admits (one statement
      world), elaborate REJECTS riding the ruling's words (WIRING IS
      WORK); confirm or widen.
    * Duplicate wires; duplicate channel names within one panel; a
      name standing in both in: and out: -- no law invented, silently
      admitted today.
    * THE DRAIN kind (in-channels only): unbuilt; the trace is the
      built-in drain. Feeder siblings (keyboard/file/socket/timer):
      stubs, unbuilt.
    * HARNESS VERBS (coordinator scaffolding, not language): the
      plant runs by Machine.run('<work>'); Machine.step() replays ONE
      feeder round (the timing interleave of the lifecycle scenario);
      Machine.play() steps to exhaustion. Trace vocabulary: make/wind/
      wire/feed/drop, 'event <label>.<chan> <name>' routed delivery,
      'emit <label>.<chan>|<label>.* <name>', 'event <label>.. <name>'
      the self channel. FR sees the surface in the staged GOODs.
    * '=> <reactor-def>' / '=x=> <reactor-def>': ANSWERED by R-46 --
      the deactivable table re-spoken (SEMANTICS 1): the reactor row
      DIED; '=x=>' stands down behaviors (reactor++ only) and
      recurring handles. Retired.
    * (destruct auto-cancel of recurring emissions: DIED with R-49 --
      no recurrences exist to cancel.)
  - SIGNAL-FLOW FLAGS (R-43 build, rule or accept):
    * The internal noun 'exit-label' (grammar rule, node class, doc
      prose): rename or keep -- the ':name:' surface never spoke
      'exit'; nothing user-facing changes either way.
    * R-48/R-49 CLOSED IN-SESSION: Q-A -- '=x=>' was always about
      BEHAVIORS (FR); Q-B -- the '~off' thought RETRACTED
      ('=!=>'/'=x=>' carry the on/off story; a dark reactor++ is
      lawful, events drop); Q-C -- DISSOLVED: R-49 removed the
      recurrence syntax in total ('every:'/'as:'/handles/'=x=>
      <handle>') -- lack of orthogonality; the timer concern is a
      plant-side FEEDER's (held discussion: the timer feeder,
      strawman 't = timer(period = 0.5, event = "poll"); t ----> s;').
    * Event definitions (R-45/R-47): structure checks on emission/
      catch with F-9 as documented -- now including the RESPELLED
      signals: entry mismatching its definition ('signals: blink(gap)'
      against 'event(v, kind)'): unify, reject, or shadow-with-own-
      shape. (The name-only entry itself is RULED AND BUILT, R-47:
      adoption + shadow; namespaced adoption also joins F-9.)
    * R-50/R-51 residue with F-9: the full STATIC unit flow
      (expressions over declared members, argument and give seams --
      today: name check static, algebra carried at run time in the
      REFERENCE interpreter, member-write seam runtime-checked).
      R-51 raises the stakes deliberately: the unit is TYPE; the Luau
      emitter erases what the static net proves and tags variants --
      so F-9's typing reach (typed ports/locals? inference?) now
      also DECIDES the emitter's erasure reach. Magnitude PREFIXES
      and scale factors stay OUT OF SCOPE by ruling (dimension only;
      'g' aliases the kg dimension); 'sqrt'/root builtins to exercise
      the rational exponents when a numeric-function surface lands;
      the EMITTER obligations (erase/tag/boundary-check) queue for
      the Luau emitter session.
    * '~EXIT' the lifecycle pseudo-event (distinct token '~EXIT'):
      untouched by R-43 -- confirm the tilde word stands.
  - Module-level causalities do not dispatch at run time (top-level
    'X => Y;' is structural only; top-level entities auto-activate).
  - Port-name shadowing of binding heads (e/b/a/c/s) is unruled.
  - F-10 residue: works in GUARDS ('when:' has no handler position);
    statement-form calls of signalling works.

(9) THE BACK HALF (Luau)
------------------------
  - link/pack/emit entire. Staged mappings: the generative clockwork
    walk ==> coroutine.yield (P-series); known hubs ==> the emitter's
    weak-reference structure (HAVE_KNOW_BE (3)); give/signal ==> the
    sum-return convention.

(10) PROCESS AND DOCUMENTS
--------------------------
  - werk-documents errata (FR's own; post-R-33..R-41 updates).
  - Explanation bin growth (candidates: signals/forced translation;
    clockwork's three waits; overloads; Nothing gates; the flat panel
    and site marking; dropto loop idioms).
  - The ~35-docstring core backlog (awaits FR scope call).
  - SESSION NOTE (R-41): an unattributed mid-session write to
    RATIONALE.txt was detected, diffed against the canonical dump,
    and reverted; the register keeps the fact on record.
