aspect: poller {
    clockwork: tick {
        => sample();
        => ~NONE;
        groove: {
            button { => ack(); }
            beat: 3 { => pulse(); }
            ~ELSE { => rest(); }
        }
    }
}
