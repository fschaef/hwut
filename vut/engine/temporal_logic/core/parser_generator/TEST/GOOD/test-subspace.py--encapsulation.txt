
--- members used only from within -> gate PASSES ---
one-level: PASS (TOP 'cond' reaches its members; no outside leak)
nested:    PASS

--- an outside rule referencing a private member -> SubspaceError ---
  rule 'top' (subspace <root>) references 'factor', a private member of subspace cond

--- a sibling subspace reaching into another's members -> SubspaceError ---
  rule 'b' (subspace <root>) references 'ai', a private member of subspace a

--- a reference OUT to a public rule -> allowed ---
outward reference to a public rule: PASS

--- structural faults: missing TOP, name collision ---
  subspace 'cond' has no TOP key (its own pattern)
  rule 'x' defined twice (flat names are global; a subspace member may not collide with another rule)
