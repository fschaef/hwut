%% TODO: Reference -- solving Zeno paradox of state transition
%%       Relational Ontology of Time (Leibniz) vs. Possibility of real time

\documentclass[11pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{cite}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{enumitem}

% Geometry settings
\geometry{margin=1in}

% Title formatting
\title{\textbf{Prolegomena to Any Future Temporal Logic}: \\ \large The Transcendental Architecture of the Discrete Machine}
\author{\textit{Dr.-Ing. Frank-René Schäfer, Independent Researcher}}
\date{\today}

% Custom environments for formalisms
\newtheoremstyle{formalism}% name
  {10pt}%      Space above
  {10pt}%      Space below
  {\itshape}%  Body font
  {}%          Indent amount
  {\bfseries}% Theorem head font
  {.}%         Punctuation after theorem head
  {.5em}%      Space after theorem head
  {}%          Theorem head spec (can be left empty, meaning 'normal')

\theoremstyle{formalism}
\newtheorem{formalism}{Definition}

\begin{document}

\maketitle

\begin{abstract} 


\end{abstract}

\section*{Nomenclature}

To ensure ontological precision, the following symbols are defined as the
invariant vocabulary of the Subject:

\begin{description}[style=multiline, leftmargin=3cm, font=\bfseries]

    \item[$\mathcal{S}$] \textbf{The Subject.} The transcendental entity
        defined by the tuple $\langle \mathcal{M}, \mathcal{I}, \Psi, \Pi
        \rangle$. That is, the manifold $\mathcal{M}$, the implication set $\mathcal{I}$,
        the schema of understanding $\Psi$, and the will $\Pi$ (Proairesis).
    
    \item[$\mathbb{E}$] \textbf{Universe of Discourse.} The set of all possible
        Phenomenal Events.
    
    \item[$\mathbb{A}$] \textbf{Universe of Assertions.} The set of all
        possible Constituted Truths (Statements). With the subspaces 
        $\mathbb{A}^\top$ of evident assertions, $\mathbb{A}^\diamond$ of
        unrefuted assertions, and $\mathbb{A}^\bottom$ of refuted 
        assertions. 
    
    \item[$\mathcal{M}$] \textbf{The Manifold.} The logical space where events
        of \textit{The Now} accumulate. 
    
    \item[$N^0_k$] \textbf{The Now.} The saturated set of events existing in the
        Manifold at logical time step $k$. ($N_k \subset \mathbb{E}$).
    
    \item[$\mathcal{I}$] \textbf{The Implication Set.} The set of positive,
        monotonic rules that drive the saturation of the Manifold ($A \implies
        B$).
    
    \item[$\sigma$] \textbf{The Synthetic Interstice.} The
        orthogonal dimension of logical time where $\mathcal{I}$ is applied
        until a fixed point is reached.
    
    \item[$H_k$] \textbf{History.} The immutable sequence of recorded Nows.
        $H_k = \langle N_0, \dots, N_{k-1} \rangle$.
    
    \item[$\Psi$] \textbf{The Schema of Understanding.} The Hermeneutic
        function that maps History to State ($\Psi: H_k \to S_k$).
    
    \item[$S_k$] \textbf{The State.} The set of active Assertions held as true
        by the Subject ($S_k \subset \mathbb{A}$).
    
    \item[$\Pi$] \textbf{The Will (Proairesis).} The function that generates 
        Petitions to the Noumena to cause specific assertions in some future State.

\end{description}

\section{Metaphysics}

A fundamental premise of this text is that the dynamic reality underlying the
perceived universe is generated by logical operations on a metaphysical
substance that is neither constrained by physical locality nor bound by the
consumption of energy.

This phenomenon becomes evident when considering `properties' of reality that 
concern objects entangled without the physical constraints of location. Let $A$ 
and $B$ be two `objects' for which any kind of common property $x$ can be 
established. Let $\mathcal{X}$ be the set of objects with property $x$, 
consisting solely of $\{A, B\}$. Then each one, $A$ and $B$, has the property 
of being \textit{One-of-Two} in $\mathcal{X}$.

If $A$ loses property $x$, then $B$ \textit{instantaneously} loses the 
property of being \textit{One-of-Two}, even if $A$ and $B$ are located at 
opposite edges of the cosmological horizon. The same can be said about the 
truth of the \texttt{AND} operation, namely ``$A$ and $B$ have $x$'', which 
loses its truth instantaneously. Also, the \texttt{OR} operation loses or 
gains its truth instantaneously, depending on scenarios for $A$ and $B$. 
\textit{Implication}, \texttt{AND}, and \texttt{OR} clearly operate on 
properties without consumption of energy and without the constraints of the 
speed of light.

To deny the ontological reality of a logical bond between $A$ and $B$ as a mere
`mental construct' is to undermine the very foundation of mathematical realism.
If set membership or equality do not possess ontological validity independent
of the observer's mind, then the capacity of mathematics to describe the
physical universe becomes inexplicable~\cite{Wigner1960}. It would imply that
the universe has no inherent logical structure---rendering scientific inquiry
itself impossible.

This text attempts to identify the mathematical mechanics at the heart of our
reality---mechanics which evade physical observation, but reveal themselves
through logical necessity.

\section{The Noumena and the Cognitive Subject}

We begin with the axiom of \textit{Finite Receptivity}. There is the {\it
Cognitive Subject\/} and the {\it Noumena\/}~\cite{Kant1781} which is the
``Outside'' of the Subject---the unknowable, un-modeled, un-conceptualizable
Absolute Reality. As Kant rigorously demonstrated, the thing-in-itself, the
Noumena, remains forever beyond the grasp of the cognitive subject. 

This paper delineates a mathematical framework to describe the metaphysical
process underlying a {\it Cognitive Subject\/}'s interaction with absolute
reality. We discuss the concept of a gap in time, the {\it Synthetic
Interstice\/} where reality manifests and a derive a structure for {\it
History}, i.e.\ the past. Based on the fundamental faculties of {\it
Equivalence\/} and {\it Distinction\/} as well as {\it Causing Existence\/} and
{\it Fixation\/} (memorization), a stable mathematical frame is derived as a
`host' for a consciousness to experience reality.

\section{The Manifold}

In the following, we assume the existence of a {\it metaphysical engine\/}
$\mathcal{M}$, we call the {\it Manifold\/}---adopting Kant's
terminology~\cite{Kant1781}. The Manifold operates during a {\it Synthetic
Interstice.\/} ($\sigma$), i.e.\ a gap in time. Its basic characteristic is
purely dynamic, i.e.\ no fixation, no writing, happens in that interstice. Once
a {\it fixed point\/} is reached, the result of the operations are
fixated/written to an immutable `History'.

In the Synthetic Interstice, ephemeral {\it Events\/} pop into existence. {\it
Original Events\/} are `caused' by the Noumena and {\it Implied Events\/} are
`caused' by the rules of nature~\footnote{Here `nature' shall designate the
structure that exists without the creational intervention of the Cognitive
Subject.}. Events are the smallest entity of information that allows for the
perception of {\it Identity}, i.e.\ the smallest unit that allows to
distinguish an $a$ from a $b$. Events solely exist in the gap in time, the
Synthetic Interstice---thus have no temporal expansion. Causation and
implication of events are an expression of an inherited property of existence
for a newly existing event.\ 

The Synthetic Interstice, the gap in time, is where any dynamic perceived by
the Cognitive Subject originates from. It is the closest interface to absolute
reality. It is where reality manifests. When its atemporal operations reach a
fixed point, the fixation/writing of the resulting Events marks the end of the
Synthetic Interstice and the beginning of a new. The distinction between the
purely dynamic, non-writing, nature of the Synthetic Interstice and the
written, immutable, nature of History has a significant impact on the
operational logic of the Manifold.

The `{\it fixation\/} of events' into History marks the end of a Synthetic
Interstice. During the purely dynamic Synthetic Interstice, no `write operations'
happen. The operations during the Synthetic Interstice rely solely on the `{\it
truth of being and becoming\/}'. In contrast, History expresses the truth if `{\it
has-been\/}'. The fixation marks the boundary between the dynamic present of
the Synthetic Interstice and the immutable History. This has significant impact
on the logic that may be applied during the Synthetic Interstice.

The following discussion applies a definition of sets which is grounded in
the set-theoretic foundations of Cantor~\cite{Cantor1895} and the logical
principles of Leibniz~\cite{Leibniz1714}. A set, in this sense, is not a
`snapshot of a fenced number of elements'. Rather, its existence is bound to
a generative logical process given by:

\begin{enumerate}

    \item A {\it Virtual Seed\/} $s_0$ possessing a specific 
          {\it core characteristic\/}---and no other discernable $\delta$. 
          It implements the essence of an {\it empty set\/} for a 
          given core characteristic.

          Since the virtual seed $s_0$ has no discernable $\delta$ it
          intrinsically distinguishes itself from any possible `real' member
          candidate with a `real' $\delta$.

          {\it Ontological Precedence\/} dictates that the \textit{Virtual Seed} 
          cannot rely on predicates that are not yet established.

    \item A {\it One-More\/} operation. This operation is manifested as the perception 
          of {\it Equivalence\/} of a perceived object $s_i$ with respect to the core 
          characteristic of $s_0$ and which is {\it distinct\/} by a secondary 
          characteristic $\delta$ from any other object $s_\omega \in \mathbb{S}$.

    \item A set is defined as the fixed point where the number of eligible 
          objects is exhausted.  

\end{enumerate}

Notably, we established the concept of a {\it Set\/} in terms of {\it
Equivalence}, {\it Distinction\/} and {\it Implication}. Further, this definition
of a set does rely on purely dynamic generation rather than on a `fixation' (as a
`snapshot of member elements'). This indepence of `fixation' is crucial for the
operation of the Synthetic Interstice.

Furthermore, this generative definition structurally prevents Russell's
antimony~\cite{Russell} of the ``set of sets that does not contain itself as an
element''. The set would require the definition of a virtual seed $s_0$ as
``does not contain itself''. This predicate, however, is a reference to
something that is not yet ontologically established, namely the set itself. The
antimony cannot arise, because Russel's set is not a set at all in the
aforementioned sense.

The aforementioned definition allows operations of the Synthetic Interstice to
operate freely, without producing contradictions. With this introduction, we can
setup a first formalism.

\begin{formalism}[Event]
    An \textbf{Event} $e \in \mathbb{E}$ is a discrete, distinguishable, purely
    ephemeral entity existing solely in the Synthetic Interstice, i.e.\ it has
    no temporal expansion. An event carries the characteristic of {\it being true\/}
    by means of its sheer existence and a {\it Non-Absolute-Truth\/} discernible 
    $\delta$.
    
    By means of the faculty of {\it Differentiation\/} the $\delta$ characteristic
    allows for the property of {\it Identity}.

    \begin{equation}
        e := \langle \top, \delta \rangle
    \end{equation}

    \begin{itemize}
        \item $\top$ (\textbf{Veritas}): The \textit{Universal Characteristic} of 
        Existence (Truth). This is the shared property that allows membership in the set.
        \item $\delta$ (\textbf{discernible}): The \textit{Particular Characteristic} of 
        Non-Absolute-Truth (Identity). Following Leibniz's \textit{Identity of Indiscernibles}, 
        $\delta$ is the unique property distinguishing $e$ from all other $e' \in \mathbb{E}$, 
        enabling the ``One-More'' operation.
    \end{itemize}
\end{formalism}

With the co-existence of events with an {\it Identity\/} given by $\delta$, the
concept of a perceivable {\it Multitude\/} can be established. We chose the
symbol $\delta$ in reference to Leibniz~\cite{Leibniz1714}'s {\it Identity of
Indiscernibles}. Multitude provides eligible elements of the set of events
produced by the Synthetic Interstice.

\begin{formalism}[The Synthetic Interstice]

    The \textbf{Synthetic Interstice} $\sigma_k$ represents the operational
    ``gap in time'' between recorded moments of History. It is the domain of
    \textit{Completing}.It is defined by the iterative, monotonic saturation of
    the set $N^0_k$ via an Implication Set $\mathcal{I}$:

    \begin{equation}
        \sigma_k : N^{(i)} \xrightarrow{\mathcal{I}} N^{(i+1)} \quad \text{until} \quad N^{(i+1)} \equiv N^{(i)}
    \end{equation}
    Then, the $N^{(i)}$ defines the fixed point $N^0_k$.

    It is defined by
    \begin{itemize}

        \item \textbf{Instantaneous Ephemerality:} It produces ephemeral 
           events ($e \in \mathbb{E}$) that possess Existence but are not yet 
           fixated into History ($H_k$).

        \item \textbf{Historic Construct:} It applies History as an immutable 
           antecedent in Implications.

        \item \textbf{Dynamic Construct:} It applies Events as non-negatable as
           antecedent in Implications.

        \item \textbf{Convergence:} The interstice persists until the application 
           of $\mathcal{I}$ reaches a fixed point (saturation) $N^0_k$.
    \end{itemize}
\end{formalism}

In the following text, possible operations of the Synthetic Interstice are
derived and it is shown how they align with the ability to reach a fixed point,
without any further constraint.\ 

When the set of $N^0_k$ events in the Synthetic Interstice converged to a fixed
point, they are written to History and become {\it Atomic Facts}. Let
$\mathbb{A}$ be the space of assertions---to be discussed later in detail.

\begin{formalism}[Atomic Fact]

    An \textbf{Atomic Fact} $a \in \mathbb{A}^\top$ is given as

    \begin{equation}
        a := \langle \bot, \delta \rangle
    \end{equation}

    It is the immutable record of an Event. The `writing' of the event corresponds 
    to the ontological shift from the core property of ``\textit{Existence} is 
    equivalent to its Truth'' to ``\textit{Has-Become} is equivalent to its Truth''.
    
    The formal structure of the Atomic Fact is defined by the fixation $\mathit{F}$:
    \begin{equation}
        \mathitt{F}: \langle \top, \delta \rangle \rightarrow \langle \bot, \delta \rangle
    \end{equation}

    That is, the Universal Characteristic of Becoming ($\top$) is replaced by the 
    characteristic of \textbf{Fixed Truth} ($\bot$, i.e.\ Has-Become). The Particular 
    Characteristic, the \textbf{Identity Provider} $\delta$ is preserved. 

\end{formalism}

The \textit{Atomic Fact} is the most primitive trace of an event, carrying
the potential to be perceived by a cognitive Subject beyond the gap in time---
beyond the Synthetic Interstice. The faculty of Equivalence and Differentiation
of $\top$ and $\bot$ establishes the faculty of perceiving the `Now' as 
a becoming existence and the `Past' as an existence that has become.

\begin{formalism}[History]

    \textbf{History} $H_k$ is the immutable, totally ordered sequence of
    sets of {\it atomic facts\/} $N_k = \mathit{F}(N^0_k)$ recorded up 
    to step $k$.

    \begin{equation}
        H_k := \langle N_0, N_1, \dots, N_{k-1} \rangle
    \end{equation}

    History is the result of `fixation' upon then completion of the Synthetic
    Interstice. 

\end{formalism}

The Synthetic Interstice is place of connection to {\it Ontological Reality\/}
(the Noumena)---the source of pure perception. History in the place of
relative (subjective) {\it Epistemic Truth\/}---the sole source of {\it True
Knowing}.

While being relative (subjective) History still is fundamentally true. The
Synthetic Interstice holds Truth in the sense of dynamically {\it Being\/} by
means of events that possess Truth by existence. History holds Truth in the
sense of immutably {\it Has-Been\/} by means of atomic facts, which are pure
handles, i.e.\ traces of events. 

History is subjective, but is beyond the reach of a Cognitive Subject's faculty
to `forge'. It is different for every Subject, but nevertheless inherently
true. Its differences in History is `factually' the sole potential for a
Subject to perceive {\it Identity}. In physical terms, difference in History
may express as differences in locality, or shape of the Cognitive Subject's host.

\begin{formalism}[The Manifold]

    The \textbf{Manifold} $\mathcal{M}$ is the metaphysical workspace. It
    consists, first, of a {\it Synthetic Interstice}, i.e.\ a gap in time
    characterized by pure dynamic, without any `writing'. Second, it consists
    of a History which is constantly generated by fixated results of the {\it
    Synthetic Interstice}.

    $\mathcal{M}$ is the metaphysical engine that executes the cycles of the
    Synthetic Interstice $\sigma$, constantly producing sets of events $N^0_k =
    \mathit{F}(N_k)$ and fixating them in History $H_k$. The end of one
    interstice marks the beginning of the next.
    
    All entities in the Manifold carry a component of {\it Truth}. An carries
    truth by its sheer existence, i.e. {\it truly being}. An atomic fact
    carries the Truth of {\it truly have been}.

\end{formalism}

\section{Inequality and Complementation}

We posit that Negation not as a singular atomic operation. Instead we
distinguish between two constituting faculties: \textbf{Inequality} and
\textbf{Complementation}. 

\begin{formalism}[Inequality (The Negation of Identity)]
    Inequality is the characteristic of \textbf{Distinction} between two positively 
    existing entities. It relies on the existence of a discerning attribute $delta$
    of both. 
    
    \begin{equation}
        A \neq B \iff \delta(A) \neq \delta(B)
    \end{equation}
\end{formalism}

\begin{formalism}[Complementation (The Negation of Exclusion)]
    Complementation is the logical construction of a ``Not-Concept''. 
    Complementation is established through inequality of a candidate
    with any other element in a completed \textbf{Universe} $U$.
    (Notably, we do not use the term subtraction in at this point in time).
    
    \begin{equation}
        \neg e := U \setminus \{e\}
    \end{equation}
    
    Complementation can only be established if and only if $U$ 
    is a defined/bounded \textbf{Set}. 
\end{formalism}

We posit that the set of potentially arising events $\mathbb{E}$, stemming
directly from the Noumena, corresponds to what Cantor termed the
\textbf{Absolute Infinite} ($\Omega$)~\cite{Cantor1899}. Because the Noumena is
the `most potent source of reality', strictly un-modeled and un-limited, it
cannot be encapsulated into a single, bounded container. The set of possible
events $\mathbb{E}$ is a {\it Proper Class\/} according Von
Neumann–Bernays–Gödel (NBG) set theory~\cite{VonNeumann1925}.

As $\mathbb{E}$ is not bounded, it cannot establish a Universe as required for
the complement. There cannot be a {\tt NOT} operation on events in the
Synthetic Interstice. However, since History is by definition the {\it fixation\/}
of evolved `Nows' $N_k$, the History $H_k$ is bounded. It can act as a Universe
for Completion and any attribute of History can be processed in terms of its
{\it negation}.

In summary, we established the concept of {\it Negation\/} by means of {\it Identity}.
Further, we constraint, that the Synthetic Interstice may not operate on negated
events, but on negated attributes of History.

\section{Operations at the Synthetic Interstice}

We concluded before, that the operations of the Synthetic Interstice
necessarily exclude the {\tt NOT} operation on events in contrast to attributes
of History. With {\tt AND} as the manifestation of co-existence. Co-existence
of events is crucial for the Synthetic Interstice, otherwise there would simply
be no dynamic at all. {\tt OR} can be established by alternative implications,
i.e.

\begin{equation}
    (A \lor B) \implies C \quad \iff \quad \{ (A \implies C), (B \implies C) \} \subseteq \mathcal{I},
\end{equation}

thus, avoiding a definition with the negation of the antecedent ($A\lor B \iff
\not A \land B$ cannot be used). We are now able to define a grammar of rules
in the Synthetic Interstice using the \textbf{Backus-Naur Form
(BNF)}~\cite{Backus1959}.

\begin{formalism}[Syntax of the Synthetic Interstice]

Let $\mathcal{I}$ over the powerset of events $\mathcal{P}(\mathbb{E})$. Let
the production rule symbol be $::=$ and alternative options be $|$. The
operators {\tt AND}, {\tt OR}, {\tt NOT} operate in their established
mathematical sense. Operator {\tt WHERE} acts as a constraint condition
with the left hand value being constraint by the right hand value being
true.

\begin{displaymath}
\renewcommand{\arraystretch}{1.3} % Adds vertical space between rows
\setlength{\arraycolsep}{3pt}     % Adjusts horizontal space between columns
\begin{array}{l c l}              % Left | Center | Left
    \text{Implication} & ::= & \text{Premise} \implies e \in \mathbb{E} \\
    \text{Premise}     & ::= & \text{Condition on Event} \\
                       & \mid & \text{Premise} \textnormal{\texttt{ AND }} \mid \textnormal{\texttt{ OR }} \text{Premise} \\
                       & \mid & \text{Premise} \textnormal{\texttt{ WHERE }} \text{Condition on History} \\
    \text{Condition on Event}   & ::= & e \in \mathbb{E} \\
    \text{Condition on History} & ::= & \text{Boolean logic on } H_k \\
                                & \mid & \textnormal{\texttt{NOT }} \text{Condition on History} \\
                                & \mid & \text{Condition on History} \textnormal{\texttt{ AND }} \mid \textnormal{\texttt{ OR }} \text{Condition on History} \\
    & & 
\end{array}
\end{displaymath}
    The result of the {\it Implication\/} is the existence of the event $e$ in $N_k$.
\end{formalism}

\begin{formalism}[Set of Implications $\mathcal{I}$]
    An implication $i$ in the Synthetic Interstice is a logical structure that can 
    be expressed in the form of the BNF Syntax of Implication. The set of 
    implications $\mathcal{I}$ is the set of active implications operating during 
    the Synthetic Interstice.
\end{formalism}

Each implication only adds events into existence, it never un-exists them. For
any two consecutive sets of events $N^{(i)}, N^{(i+1)} \subseteq \mathbb{E}$, it holds
that $\mathcal{I}(N^{(i)}) \subseteq \mathcal{I}(N^{(i+1)})$.

With the given BNF form of Implications, an implication becomes {\it
idempotent\/} as soon as it has caused its event. An event is unique and cannot
be removed. The History $H_k$ is immutable and therefore acts as a {\it static
constraint}, unable to induce oscillation. Consequently, with a finite set of
implications, the operations inevitably terminate. Formally, since
$\mathcal{I}$ is monotonic, Tarski's Theorem~\cite{Tarski1955} guarantees the
existence of a **Least Fixed Point** regardless of cardinality.

\begin{proof}[Universal Convergence via Monotonicity]

    \textbf{1. The Bounded Case (Local Convergence):}

    Let $\mathbb{E}' \subset \mathbb{E}$ be any bounded subset of events
    relevant to the current context (e.g., the set of all physically possible
    implications for a finite input). Since $\mathbb{E}'$ is a Set, its
    powerset $\mathcal{P}(\mathbb{E}')$ forms a Complete Lattice. Because the
    Implication Function $\mathcal{I}$ is \textbf{Monotonic} (it preserves the
    order $\subseteq$), \textbf{Tarski's Fixed Point Theorem} guarantees that
    the sequence $N^{(i)}$ defined on $\mathbb{E}'$ converges to a Least Fixed
    Point (LFP).

    \textbf{2. The Absolute Case (Structural Non-Oscillation):} Now consider
    the unbounded Universe $\mathbb{E}$ (the Proper Class). Oscillation in a
    logical system is defined as the sequence entering a cycle: $A \to B \to
    \dots \to A$. This structurally requires the removal of elements (i.e.,
    $N^{(k+1)} \nsupseteq N^{(k)}$) to return to a previous, smaller state.
    
    However, the Synthetic Interstice operates under strict \textbf{Positive
    Logic}. The operation is \textbf{Inflationary}:

    \begin{equation}
        N^{(k)} \subseteq N^{(k)} \cup \mathcal{I}(N^{(k)}) = N^{(k+1)}
    \end{equation}
    
    Since elements are never removed, the sequence is a \textbf{Strictly
    Ascending Chain}. An ascending chain can never oscillate; it can only:

    \begin{enumerate}

        \item \textbf{Halt:} Reach a state where $\mathcal{I}(N^{(k)})
        \subseteq N^{(k)}$ (The Fixed Point).

        \item \textbf{Transfinite Saturation:} Continue indefinitely to the
        limit ordinal $\omega$. In this case, the union of the chain
        $N^{(\infty)} = \bigcup N^{(i)}$ constitutes the Fixed Point.

    \end{enumerate}

    Therefore, even in the Absolute $\mathbb{E}$, logical oscillation is
    structurally impossible. The ``Now'' must crystallize.

\end{proof}

Consequently, the restriction of an assumed finite set of events and a finite
set of implications can be dropped. We have no reason to believe that the
Manifold with the Synthetic Interstice---being atemporal---is limited within
its capacity to operate. To the contrary, the attribute of \textit{limitedness}
appears to be intrinsic to the {\it domain of conceptualization},
rather than to the immediate interface with the un-modeled Noumena.

The {\it avoidance of circularity\/} is an ontological necessity for the
Synthetic Interstice to reach a fixed point. If circularity was possible, the
process of evolving sets of events would potentially `hang' in oscillation, and
no subsequent instant time could ever emerge, not even after an infinite number
of operations. On the other hand, this is avoided by what has been derived for
the operations on the Synthetic Interstice, namely the impossibility of a {\tt
NOT} operation on events. Without the converging operations during the interstice,
no association of events could be established and perceived existence would be
impossible. 

The Synthetic Interstice in time is an \textbf{Ontological Necessity} to
separate the writing to History from the development of a fixed point. It
separates the dynamic aspect of being ($\top$) from the aspect of potential
knowing ($\bot$). The section specified a consistent grammar for the Synthetic
Interstice to establish a source of {\it becoming\/} for History.

\section{Sequentiality and Enumeration}

The faculty of `writing' to history introduced a new discernible, namely by
means of the ontological truth states $\top$ vs $\bot$ of events $e = \langle
\top, \delta \rangle$ against atomic facts $a = \lange \bot, \delta \rangle$.
The differentiation of $\top$ and $\bot$, i.e.\ the differentiation of the
$\top$ in an $e_0 = \langle \top, \delta_1 \rangle$ against an atomic fact $f_1
= \lange \bot, \delta_0 \rangle$, establishes the faculty of perceiving an 
`after', and an according assertion of $\delta_1$ after $\delta_0$. 

\begin{formalism}[Sequential Fact]

    A \textbf{Sequential Fact} $s\in\mathbb{A}^\top$ is an immutable assertion
    of order between two discernibles $\delta_1$ and $\delta_0$ relating to
    an event in the Synthetic Interstice $e_0 = \langle \top, \delta_1 \rangle$ 
    and an atomic fact in History atomic fact $f_1 = \lange \bot, \delta_0 \rangle$
    defined as:
    
    \begin{equation}
        s := (\delta_1 \succ \delta_0) \in \mathbb{A}^\top
    \end{equation}
        
\end{formalism}

For each atomic fact $a$ in History with a $\delta_0$, the sheer existence of
an event $e$ with a $\delta_1$ in the Synthetic Interstice implies an event of
$\langle \top, \delta_1 \succ \delta_0\rangle$. Through fixation of those
events, the sequentiality of History becomes perceivable as `flow'. The
differentiation between $\top$ and $\bot$ establishes the most primitive
perception of {\it Time\/} and a dimensionality of one.

Notably, $\langle \top, \delta_1 \succ \delta_0\rangle$ is an event itself that
causes events of succession in relation to all facts in History. The set of
these implied succession events naturally converges, once all of History is
implied. But, as we saw before, even a convergence at infinity does not
contradict our framework. There is no necessity to impose History to be finite.

A special case is when an event and an atomic fact share the same discernible
$\delta$. This establishes an event of a Repetition $\langle \top, (\succ,
\delta) \rangle$ which corresponds to {\it Two-ness}. It also, established a
composition of $\succ$ and $\delta$ as a discernible $\tilde{\delta}=(\succ,
\delta)$. Now, as soon as $\delta$ appears again, another fact emerges with the same
discernible $(\succ, \delta)$. Again, through the faculty of Equivalence the
two discernibles are associated to establish a new atomic fact, namely $\langle
\bot, (\succ \succ, \delta) \rangle$ or {\it Three-ness}. 

Until the establishment of Three-ness, the composite discernible where
perceived as one $(\succ,\delta)$ to identify Equivalence. For Four-ness the
Three-ness of $\langle \bot, (\succ \succ, \delta) \rangle$ needs to be
associated with the new merged repetition $\langle \top, (\succ, \delta)
\rangle$. The faculty of Differentiation allows for the separation of the
characteristic $\delta$ out of $(\succ, \delta)$ and $(\succ \succ, \delta)$
before the perception of the equivalence in $\delta$ allows to establish
Four-ness $(\succ \succ \succ, \delta)$. With the faculty of separation of
the $\delta$ from the composite discernible and the faculty to perceive
equivalence of those discernibles, any further enumeration can be established.

\begin{formalism}[Numeric Fact]

	A \textbf{Numeric Fact} $n \in \mathbb{A}^\top$ is the immutable assertion
    of Cardinality established by recursive composition of Sequential Facts
    sharing an identical discernible $\delta$.
    
    Let $\succ$ denote the successor operator (the perception of ``after'').
    The Numeric Fact is defined recursively:
    \begin{enumerate}
        \item \textbf{Unity (1):} The perception of a unique $\delta$ in the Now ($\top$).
        \begin{equation}
            n_1 := \langle \top, \delta \rangle
        \end{equation}
        
        \item \textbf{Succession (k+1):} The perception of Equivalence between a current event 
        $\langle \top, \delta \rangle$ and a historical composite fact $n_k = \langle \bot, (\succ^k, \delta) \rangle$.
        This generates a new composite discernible:
        \begin{equation}
            n_{k+1} := \langle \top, (\succ \cdot \succ^k, \delta) \rangle
        \end{equation}
    \end{enumerate}

    Thus, Number is not an inherent property of the discernible $\delta$, but a 
    property of the \textbf{Repetition of Identity} across the boundary of 
    Time ($\top$ vs $\bot$). It is the measure of the depth of the recursive 
    stack of ``afters'' for a specific $\delta$.

\end{formalism}

The concepts of {\it Atomic Fact} $a$, {\it Sequential Fact/} $s$, and {\it
Numeric Fact\/} $n$ make up the domain of undeniably true assertions
$\mathbb{A}^\top$. Due to ontological necessities, they are as stable as the
underlying faculties of Equivalence, Differentiation, Fixation, and
Implication, on which they have been established. They implement the concept of
`stability' in contrast to the `caducity' of events the Synthetic Interstice.

\section{Belief of Causality}

With the faculty of perceiving Sequentiality, the Cognitive Subject is enabled
to the {\it Belief\/} in Causality as basis for understanding.

\begin{formalism}[Causal Assertions]

    \begin{description}
        \item[Causality ($A \implies B$)] 

		Posits that assertions $A$ and $B$ are tied stably in sequence. It 
        further asserts a `Truth' that links both, namely a `reason'.
        
        \item[Mono-Causality ($A \iff B$)] 

		Posits that the causality $A \implies B$ is the \textit{unique}
        generative path for $B$. That is, if $B$ is true, then $A$ must necessarily be
        true as well.
        
        \item[Co-Causality ($A_0 \implies B \land A_1 \implies B$)] 

          Posits that $A_0$ and $A_1$ are distinct but \textit{equivalent} sufficient 
          conditions for $B$. 
        
        \item[Mutually Exclusive]

        \textit{Function:} \textbf{Ambiguous.} The truth of $B$ proves the
        disjunction $(A_0 \lor A_1)$, but is \textit{insufficient} to determine
        the specific cause.

        \item[Exclusion ($A \implies \neg B$)] Posits that assertions $A$ and
        $B$ are mutually exclusive. They cannot coexist in the same valid
        State.
        
        \textit{Function:} \textbf{Regulatory.} This is the generator of
        \textbf{Logical Incoherence}. It allows the Subject to detect that its
        internal model ($\Psi$) contradicts the observed History ($H_k$).

    \end{description}
\end{formalism}

Causal assertions can potentially be falsified. However, they cannot be
verified---this would require to observe the complete History from the origin
to its end. Causal assertions are a fundamental necessity for a Will to
operate. The Will needs to know the necessary $B$ to achieve $A$.

Let the term `understanding' represent an activity towards producing assertions
of Truth, in contrast to the term `confabulation' as producing assertions
unrelated to Truth. The space of assertions of a cognitive Subject divides into
three subspaces namely:

\end{document}

%% \bibitem{Wigner1960}
%% Wigner, E. P. (1960). 
%% ``The unreasonable effectiveness of mathematics in the natural sciences''. 
%% \textit{Communications on Pure and Applied Mathematics}, 13(1), 1--14.
