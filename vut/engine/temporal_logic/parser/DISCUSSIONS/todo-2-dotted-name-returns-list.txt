SPDX-License: MIT; Project VUT; (C) Frank-Rene Schaefer
===============================================================================
todo-2  _build_dotted_name RETURNS A LIST, NOT A JOINED STRING
===============================================================================
SCOPE     parser. Decided; a deferred cross-cutting refactor.

STATE TODAY:
    _build_dotted_name joins its segments into a single string. The target is a
    segment LIST -- ["A", "B", "C"] -- so the semantic layer does not have to
    re-split a name the parser already had in pieces. Joining leaves parsing
    residue in the AST and forces the resolver to undo it.

TO IMPLEMENT:
    Return list[str]. Update every consumer of the dotted-name value (the
    factories and any AST field typed as the joined string) to carry the list.
    Cross-cutting because the value threads through several node families.
