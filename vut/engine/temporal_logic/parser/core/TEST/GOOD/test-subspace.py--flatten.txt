
--- a one-level subspace lowers to the flat qualified-named map ---
flat keys: ['cond', 'cond/factor', 'cond/op', 'cond/term', 'top']

--- scope_of is each rule's body-resolution scope ---
  cond         cond
  cond/factor  cond
  cond/op      cond
  cond/term    cond
  top          <root>

--- TOP became the rule's own pattern, keyed by the bare subspace name ---
cond pattern: ('<term>', Terminal(string:@and), '<term>')
