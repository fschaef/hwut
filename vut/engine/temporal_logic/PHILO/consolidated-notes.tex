%% ============================================================================
%% CONSOLIDATED WORKING NOTES for ontology.tex
%% Extracted from: TODO.txt, notes.tex, ontology-part-2.tex,
%%                 ontology-DRAFT.tex, summarized-paragraphs.txt
%%
%% Organization:
%%   Part A -- Terminology migration map (old drafts -> current manuscript)
%%   Part B -- Active work items (Section 2 TODO, verbatim)
%%   Part C -- Unincorporated derivations: SPACE (Stretch, Line, Adjacency,
%%             Metric, Orthogonality, Geometry, Topology)
%%   Part D -- Unincorporated apparatus from ontology-DRAFT.tex
%%             (Nomenclature, assertion tri-partition, Cycle of Knowledge,
%%              Conclusion draft)
%%   Part E -- Superseded material (kept only as pointers, not content)
%%   Part F -- Conflicts and open questions surfaced during consolidation
%%
%% Status flags used below:
%%   [PENDING]    -- not yet in ontology.tex; candidate for incorporation
%%   [SUPERSEDED] -- older formulation; current manuscript replaces it
%%   [CONFLICT]   -- two incompatible formulations exist side by side
%% ============================================================================


%% ============================================================================
%% PART A -- TERMINOLOGY MIGRATION MAP
%% The older files (ontology-DRAFT.tex, notes.tex, ontology-part-2.tex,
%% summarized-paragraphs.txt) use vocabulary that the current ontology.tex
%% has replaced. Any material lifted from Parts C/D must be translated:
%%
%%   OLD (drafts)                    CURRENT (ontology.tex)
%%   ----------------------------    ---------------------------------------
%%   Noumena                         The Transconceptual (T)
%%   History (H_k)                   The Immutable Past (H)
%%   The Manifold (M)                The Ontic Engine (O)
%%   Cognitive Subject as            Receptivity (at the primordial level);
%%     primordial "Lack of            Cognitive Subject only downstream,
%%     Knowledge"                     after Memory
%%   delta_bullet = "I exist"        delta_bullet = "I exist, You exist,
%%                                     I am active, You are passive"
%%   Primordial faculties posited    Faculties must be *derived*, not
%%     axiomatically (Equivalence,     posited (current discipline: nothing
%%     Differentiation, ...)           imported before it is earned)
%%   A^top / A^diamond / A^bot       No equivalent yet in current text
%%     (evident/unrefuted/refuted)     (see Part D.2 -- worth preserving)
%%   Psi (Schema of Understanding),  Not yet derived in current text
%%     S_k (State), Pi (Will)          (Pi appears late in ontology.tex)
%%
%% NOTE: ontology.tex itself is still mixed. From Section "The Primordial
%% Event" onward it contains the OLD Noumena-based text; Sections 1-2
%% (Foundation, Truth Modalities) are the current framework. The old tail
%% of ontology.tex overlaps heavily with ontology-DRAFT.tex.
%% ============================================================================


%% ============================================================================
%% PART B -- ACTIVE WORK ITEMS (from TODO.txt, verbatim)
%% ============================================================================

%% SECTION 2 TODO
%%
%% Status: opening drafted (delta_bullet alone, top/bot named,
%% Event/Atomic Fact/Fixation formalisms, Becoming as sharp edge, truth
%% invariant under modality shift, immutability under Absence of Redundancy).
%%
%% REMAINING THREADS TO WRITE:
%%
%% [ ] (1) Discernible atomicity
%%     - The discernible has no internal multiplicity; nothing in it can be
%%       rearranged.
%%     - Atomicity is inherited from the Transconceptual's Unity.
%%     - This is why the discernible itself does not change, in either
%%       modality.
%%     - Suggested placement: after the Event formalism.
%%
%% [ ] (2) The four properties of Information Substance
%%     - Invariance of meaning (truth does not alter)
%%     - Immutability (once become, remains)
%%     - Positive existence (no inverse operation; no return to nothing)
%%     - Origin beyond time (grounds the other three)
%%     - Stated as a single constellation, not derived properties.
%%     - Belong to the discernible; Event and Atomic Fact are modalities of
%%       the same discernible carrying the same properties.
%%
%% [ ] (3) Categorical separation: Information Substance vs. Concepts
%%     - Concepts do not have these properties.
%%     - Concepts are transient; Information Substance is not.
%%     - Therefore Concepts cannot be Information Substance.
%%     - Concepts require a later detailed investigation (defer).
%%
%% [ ] (5) Methodological note on category error
%%     - At this level, "what causes Fixation" is a category error.
%%     - We describe co-necessities, not causal chains.
%%     - Information Substance, Receptivity, Fixation are co-necessary:
%%       each requires the others.
%%     - Frames the discussion of Fixation that follows.
%%
%% [ ] (4) MAJOR: Complement operation, Fixation precedes Implication
%%     - The "gap to the Transconceptual" is a complement operation.
%%     - Complement requires something stable, fixated, to operate on.
%%     - Becoming is in flux; nothing under top can be complemented.
%%     - Therefore Fixation must precede Implication.
%%     - At the primordial step, Fixation happens because no other dynamic
%%       is available (no fixated entity yet exists for complement to
%%       operate on).
%%     - After the first Fixation, complement becomes operational.
%%     - This is the seed of complementation, and eventually of time as
%%       perceived in discrete steps.
%%
%% SUGGESTED ORDER: (4) first (most consequential), or (1)->(2)->(3)->(5)->(4).
%%
%% OPEN QUESTIONS:
%%     - Does (4) require revision of Section 1? Specifically the paragraph
%%       on the Receptivity changing with received Information Substance.
%%     - Does "Fixation precedes Implication" need qualification for the
%%       primordial step versus subsequent steps?
%%
%% NOTE: Items (1), (2), (3) appear to be substantially ADDRESSED already in
%% the current Section 2 draft of ontology.tex ("atomicity/immutability of
%% the discernible", the three trans-temporal properties, categorical
%% separation paragraph). Verify and check them off. Items (4) and (5)
%% remain genuinely open.
%%
%% AFTER SECTION 2:
%%     - Section 3: Synthetic Interstice (planned closing of paper).
%%     - Final pass: terminology consistency, prose polish.


%% ============================================================================
%% PART C -- UNINCORPORATED DERIVATIONS: SPACE  [PENDING]
%% Sources: ontology-part-2.tex (primary, most recent) and notes.tex
%% (earlier, partially redundant variants). All use OLD terminology.
%% ============================================================================

%% ---------------------------------------------------------------------------
%% C.1  Spatial Point Discernible (ontology-part-2.tex)
%% Prerequisites already in main text: Composition (oplus), Numeric
%% Discernible, synchronic projection of the Immutable Past (Memory).
%% ---------------------------------------------------------------------------

\begin{formalism}[Spatial Point Discernible]
    A \textbf{Spatial Point Discernible} $\delta_p \in \mathbb{E}$ is
    composed of numeric discernibles.
    \begin{equation}
        \delta_p := n_1 \oplus n_2 \oplus \dots \oplus n_d
    \end{equation}
    The cardinality $d$ of the fused numeric discernibles intrinsically
    establishes the \textbf{Dimensionality} of the point. Because these
    discernibles co-exist in the Now without the sequential operator
    ($\succ$), they exhibit zero logical distance between them.
\end{formalism}

%% Accompanying claim: the point discernible has neither temporal nor
%% spatial expansion under top; once fixated (bot) it gains temporal
%% stability as a trace in the sediment, but still no spatial expansion.

%% ---------------------------------------------------------------------------
%% C.2  Stretch Discernible and redundancy collapse of difference
%% (ontology-part-2.tex). Introduces ominus (Differentiation operator,
%% complement to Composition oplus).
%% ---------------------------------------------------------------------------

%% Core argument: the difference between two composite discernibles cannot
%% persist in redundant component-wise form
%%     (delta_a1 ominus delta_b1) oplus (delta_a2 ominus delta_b2)
%% if a strictly purer expression exists:
%%     delta_c = delta'_a ominus delta'_b
%% carrying the total magnitude of difference along a single axis.

\begin{formalism}[Stretch Discernible]
    A \textbf{Stretch Discernible} $t \in \mathbb{F}$ conveys a
    redundancy-free differentiation between two composite discernibles
    $\delta_a$ and $\delta_b$ belonging to two events $e_a$ and $e_b$. It
    maintains a single discernible $(\delta'_a \ominus \delta'_b)$
    representing the total modality of their difference.
    \begin{equation}
        t := \langle \bot, (\delta'_a \ominus \delta'_b) \oplus C \rangle \in \mathbb{F}
    \end{equation}
    Here, $C$ represents the \textbf{Shared Core}---the dimensions of the
    composition rendered equivalent and mutually redundant between the two
    points.
\end{formalism}

%% Metaphysical payoff (keep -- strong passage): the redundancy collapse
%% operates via Spectral Decomposition / Principal Axis Transformation;
%% the difference is projected onto a single principal axis (D, 0, ..., 0),
%% the zeros collapse into the shared core C. This grounds the "unreasonable
%% effectiveness" (Wigner) of eigenvalue analysis: not descriptions of an
%% external world, but the foundational informational algorithms by which
%% the Subject compresses perceivable reality. The Absence of Redundancy
%% necessitates this transformational logic as a structural faculty of the
%% Synthetic Interstice.

%% ---------------------------------------------------------------------------
%% C.3  Line Assertion (ontology-part-2.tex)
%% Depends on: Stretch (C.2), Metric Fact (C.5), Causal Assertion machinery
%% (varnothing mark), Will (Pi).
%% ---------------------------------------------------------------------------

\begin{formalism}[Line Assertion]
    A \textbf{Line Assertion} is the assertion $l \in \mathbb{A}^\diamond$
    that, behind a metric fact $m \in \mathbb{A}^\top$, the repetition
    expands to infinity, i.e.\ that there is an infinite number of points
    that share the same $C$ considering expansion and detail. That includes
    the assertion that there would exist an entity in the Noumena that
    governs the regularity.

    Let $t = \langle \bot, \mathbb{D} \oplus C \rangle$ be a Stretch Fact
    and $m$ be the Metric Fact representing the finite recurrence of this
    equivalent difference. The Line Assertion is the speculative fixation:
    \begin{equation}
        l := \langle \bot, (\varnothing, \infty(\mathbb{D}) \oplus C) \rangle \in \mathbb{A}^\diamond
    \end{equation}
\end{formalism}

%% Payoff passage: the postulation of continuous space is an act of Will
%% (Pi). The Subject constructs the spatial continuum to structure and
%% navigate the infinitely complex discernibles of the Absolute; like the
%% Causal Assertion it carries varnothing (wrongness) and remains unrefuted
%% only by the "grace" of the Noumena [translate: Transconceptual].

%% ---------------------------------------------------------------------------
%% C.4  Adjacency -- TWO VARIANTS EXIST  [CONFLICT]
%% notes.tex contains two distinct derivations. Choose one.
%% ---------------------------------------------------------------------------

%% VARIANT 1 (notes.tex, "The Genesis of Space"): adjacency derived from
%% the ORDER of sequential facts inside the Interstice; operator written
%% \tilde{\implies}; claimed *identical* to the sequential discernible succ.

\begin{formalism}[Adjacent Bonding Fact]  % Variant 1
    An \textbf{Adjacent Bonding Fact} $b \in \mathbb{A}^\top$ is the
    immutable assertion of `Close-ness' between two discernibles. It is the
    fixation of the immediate logical implication derived in the Synthetic
    Interstice. Let $\delta_1$ and $\delta_0$ be linked by the proximity
    operator $\tilde{\implies}$:
    \begin{equation}
        b := \langle \bot, (\delta_1 \tilde{\implies} \delta_0) \rangle \in \mathbb{A}^\top
    \end{equation}
    It asserts that within the dynamic generation of the Now, the existence
    of $\delta_0$ was the immediate logical condition for $\delta_1$.
\end{formalism}

%% VARIANT 2 (notes.tex, "Adjacency and Metric Facts"): adjacency derived
%% from IMPLICATION DEPTH between co-existing events sharing a discernible;
%% operator written \perp ("Structural Adjacency"). Includes the sharper
%% argument: two events with a shared discernible can co-exist only if they
%% differ in at least one discernible; to differ, at least one must be the
%% result of an Implication -- hence adjacency = record of direct implication.

\begin{formalism}[Adjacency Fact]  % Variant 2
    An \textbf{Adjacency Fact} $j \in \mathbb{A}^\top$ is the immutable
    record of a direct implication between two discernibles within a single
    Synthetic Interstice. If, during the genesis of $N_k$, the existence of
    $\delta_a$ implied $\delta_b$ (i.e., $\delta_a \implies \delta_b \in
    \mathcal{I}$), this relation is fixated as:
    \begin{equation}
        j := \langle \bot, (\delta_b \perp \delta_a) \rangle
    \end{equation}
\end{formalism}

%% ---------------------------------------------------------------------------
%% C.5  Metric Fact -- follows either adjacency variant  [CONFLICT: notation]
%% Same recursive idea in both: count of adjacency steps between two
%% instances of an identical discernible in the same Fixed Point.
%% ---------------------------------------------------------------------------

\begin{formalism}[Metric Fact]  % notation of Variant 2
    A \textbf{Metric Fact} $m \in \mathbb{A}^\top$ is a record of applied
    cardinality of Implication. It establishes the magnitude of separation
    between two equal discernibles $\delta$ in the same Fixed Point. It is
    defined recursively as the count of Adjacency steps required to traverse
    from one instance of $\delta$ to the other:
    \begin{equation}
        m_{ab} := \langle \bot, (\perp^k, \delta) \rangle \in \mathbb{A}^\top
    \end{equation}
    Here, $\perp^k$ represents the $k$-th degree of logical separation
    (Implication Depth) in the Synthetic Interstice.
\end{formalism}

%% Summary payoff (keep -- strong):
%%   Time  = cardinality of Fixations   (vertical depth)
%%   Space = cardinality of Implications (horizontal depth)
%% Metric is not a property of a container but the LATENCY OF LOGIC: two
%% points are "far apart" because the engine requires many logical steps to
%% derive one from the other. The Metric Fact anchors identity in a
%% dimension orthogonal to time -- call it Space.

%% ---------------------------------------------------------------------------
%% C.6  Orthogonality and Transformation (notes.tex, "Transformation and
%% Informational Equivalence")
%% ---------------------------------------------------------------------------

%% Context: given Causal Assertions that are not immediately refuted, the
%% Subject operates under an assumption of "Nice-ness" (regularity) of the
%% Ground: instantiations tend to be informationally CLOSE to existing
%% History (massive Equivalence, minimal Differentiation). Because the
%% Subject operates on pure information, a redundancy-reducing
%% Transformation (projection) is inherent: uniform co-variation of d
%% discernibles is informationally equivalent to variation of one.

\begin{formalism}[Orthogonality]
    \textbf{Orthogonality} is not a physical angle, but the
    \textit{Informational Independence} of discernibles within the
    composition of a Point. If a logical transformation can isolate the
    variation of one discernible such that its change does not necessitate
    a change in the others, the discernibles form an orthogonal basis.
\end{formalism}

%% ---------------------------------------------------------------------------
%% C.7  Geometry as Transcendental Constraint (notes.tex)
%% Depends on: WHERE operator (Transcendental Gate) from the Interstice
%% syntax; Metric Fact.
%% ---------------------------------------------------------------------------

\begin{formalism}[Geometric Generation]
    A geometric object is the saturated set of points in the Interstice
    generated under a specific condition on History.

    \textbf{The Line:} Generated when the \texttt{WHERE} constraint enforces
    a constant differential equivalence (slope) across the orthogonal
    discernibles.

    \textbf{The Circle:} Generated when the \texttt{WHERE} constraint
    enforces Equivalence on the \textit{Metric Fact} ($m$) between any newly
    generated point $p_{new}$ and a fixed historical anchor point
    $p_{anchor}$:
    \begin{equation}
        \text{Premise} \implies p_{new} \quad \textnormal{\texttt{WHERE}} \quad m(p_{new}, p_{anchor}) \equiv \text{constant}
    \end{equation}
\end{formalism}

%% Payoff: Space is not a pre-existing void; it is the Implication of Order
%% in the dynamic present, structured by the Subject's informational
%% transformations, constrained by WHERE conditions on the metric facts of
%% History.

%% ---------------------------------------------------------------------------
%% C.8  Topology (notes.tex; a shorter variant also in ontology-DRAFT.tex)
%% ---------------------------------------------------------------------------

\begin{formalism}[Topological Event]
    A \textbf{Topological Event} $e_t \in \mathbb{E}$ is an ephemeral entity
    emerging within the Synthetic Interstice that manifests the mutual
    implication between two co-existing events. Let $\bowtie$ denote the
    adjacency operator (structural ``closeness''). If two distinct events
    $e_1 = \langle \top, \delta_1 \rangle$ and $e_2 = \langle \top, \delta_2
    \rangle$ imply each other via $\mathcal{I}$ during the interstice:
    \begin{equation}
        (e_1 \implies e_2) \quad \land \quad (e_2 \implies e_1)
    \end{equation}
    then an implication event $e_t$ is generated:
    \begin{equation}
        e_t := \langle \top, (\delta_1 \bowtie \delta_2) \rangle
    \end{equation}
\end{formalism}

\begin{formalism}[Topological Assertion]
    A \textbf{Topological Assertion} $t \in \mathbb{A}^\diamond$ is the
    speculative fixation that, behind a recurring network of Topological
    Facts $p \in \mathbb{A}^\top$, there exists a non-conceptual reality in
    the Noumena that governs this synchronous regularity. Let $p = (\delta_1
    \bowtie \delta_2)$:
    \begin{equation}
        t := (p \xrightarrow{\mathcal{N}} \top)
    \end{equation}
    Like the Causal Assertion, it is refutable; it remains valid only as
    long as the Synthetic Interstice continues to reliably produce the
    adjacency $p$.
\end{formalism}

%% DRAFT variant of the payoff (Object Assertion): the Topological
%% Assertion generates the "Illusion of Substance" -- the belief that
%% Objects exist as independent containers, whereas they are stable
%% patterns of logical adjacency. (ontology-DRAFT.tex, sec. Topology)

%% ---------------------------------------------------------------------------
%% C.9  Loose fragments worth keeping (notes.tex, unsectioned)
%% ---------------------------------------------------------------------------

%% (a) The varnothing discernible as the primal experience of Non-Truth.
%%     When ignored, it carries the essence of ILLUSION: the belief that a
%%     `causal reason' (a concept) exists ontologically, while all
%%     conceptual experience emerges through the Synthetic Interstice from
%%     the inconceivable ground. Ignoring varnothing = entering the illusory
%%     world of pure conceptualization.
%%
%% (b) Immutability of implication rules: implications are fixated rules;
%%     to exist they must have been instantiated as events and fixated into
%%     the Immutable Past; since the Past is immutable, the rules themselves
%%     are immutable.
%%
%% (c) Topology TODO note (verbatim): "self by consideration of not-self
%%     concept + Fact of Belonging (by one discernible) + Metric Fact"


%% ============================================================================
%% PART D -- UNINCORPORATED APPARATUS FROM ontology-DRAFT.tex  [PENDING]
%% (Material with no counterpart in the current ontology.tex; everything
%% else in the DRAFT is superseded -- see Part E.)
%% ============================================================================

%% ---------------------------------------------------------------------------
%% D.1  Nomenclature table (DRAFT). Useful as a model for a nomenclature
%% section in the final paper; symbols must be re-derived, not posited.
%% ---------------------------------------------------------------------------
%%   S        The Subject: tuple <M, I, Psi, Pi>
%%   E        Universe of Discourse (all possible Events)
%%   A        Universe of Assertions, with subspaces:
%%              A^top     evident assertions
%%              A^diamond unrefuted (speculative) assertions
%%              A^bot     refuted assertions
%%   M        The Manifold [-> Ontic Engine]
%%   N^0_k    The Now: saturated set of events at logical step k
%%   I        The Implication Set
%%   sigma    The Synthetic Interstice
%%   H_k      History [-> Immutable Past]
%%   Psi      Schema of Understanding: Psi: H_k -> S_k
%%   S_k      The State: set of active Assertions held true
%%   Pi       The Will (Proairesis): generates Petitions to the ground
%%            to cause specific assertions in a future State

%% ---------------------------------------------------------------------------
%% D.2  The Cycle of Knowledge (DRAFT) -- epistemic tri-partition.
%% Not present in current manuscript; natural companion to the Causal
%% Assertion / varnothing machinery already in ontology.tex.
%% ---------------------------------------------------------------------------
%% 1. The Evident (A^top): Atomic, Sequential, Numeric, Metric Facts.
%%    Immutables fixated in the Past; to deny them is to deny the engine.
%% 2. The Speculative (A^diamond): Causal and Topological Assertions
%%    generated by the Will; carry varnothing; remain here only as long as
%%    they successfully predict the next Fixed Point.
%% 3. The Refuted (A^bot): when an assertion's prediction is contradicted
%%    by the actual fixation of the next Now, it is ejected from A^diamond
%%    into A^bot.

%% ---------------------------------------------------------------------------
%% D.3  Conclusion draft (DRAFT) -- themes to reuse for closing material:
%% ---------------------------------------------------------------------------
%% - The Subject metabolizes the Infinite into the Discrete.
%% - The Synthetic Interstice resolves the Zeno paradox of change by
%%   orthogonalizing Logic and Time.
%% - Numeric Fact anchors identity against the flow of time; Metric Fact
%%   anchors distinction against the collapse of space.
%% - The Will projects Order onto Chaos: laws necessary for the Subject's
%%   existence yet fundamentally contingent (varnothing).
%% - The "limitations" of finite cognition (discrete moment, fixed history,
%%   boolean implication) are not constraints to overcome but the
%%   Transcendental Conditions of coherent perception.
%% - Because every Causal Assertion carries varnothing, the Conceptual
%%   Universe operates non-deterministically, "by the grace" of the ground.
%%
%% Also relevant: the Conclusion fragment already inside ontology.tex
%% (Primordial Consciousness in the ground itself; demarcation from the
%% downstream Cognitive Subject) -- currently marked TODO there.

%% ---------------------------------------------------------------------------
%% D.4  Alternative title (DRAFT header):
%%   "Prolegomena to Any Future Temporal Logic:
%%    The Cosmogony of the Conceptual Universe"
%% ---------------------------------------------------------------------------


%% ============================================================================
%% PART E -- SUPERSEDED MATERIAL (pointers only; do not re-import)
%% ============================================================================
%% From ontology-DRAFT.tex:
%%   - Axiomatic posit of five primordial faculties (Equivalence,
%%     Differentiation, Instantiation, Necessity, Immutability): replaced by
%%     the discipline of deriving faculties; do not re-posit.
%%   - Primordial Event as "e_0 differentiating from the Noumena", the
%%     Static/Dynamic implication, delta_bullet = "I exist": superseded by
%%     Section 1's Transconceptual/Receptivity derivation and the four-part
%%     delta_bullet.
%%   - Manifold formalism, Negation asymmetry, Interstice syntax/BNF,
%%     Tarski convergence proof, Sequentiality, Numeric Fact, Causal
%%     Assertion, Will: all present (in newer form) in the old tail of
%%     ontology.tex itself; the DRAFT versions are strictly older.
%%   - Number via composite (succ, delta) with "Two-ness"/"Three-ness":
%%     superseded by the Numeric Discernible via redundancy collapse of
%%     nested repetition markers (#).
%%
%% From summarized-paragraphs.txt:
%%   - A paragraph-by-paragraph "execution trace" of an earlier compiled
%%     manuscript (framed by a "Dr. Thorne" persona from a prior AI
%%     session). Line numbers refer to a version that no longer exists.
%%     No unique content; useful only as a checklist that the old
%%     derivational chain read as a seamless sequence:
%%       Noumena -> Primordial Event -> Fixation -> Sequentiality ->
%%       Absence of Redundancy -> Memory -> Manifold -> Metaphysics ->
%%       Sets/Negation -> Interstice -> Operations -> Discreteness of
%%       Time -> Enumeration -> Order/Difference -> Causality -> Will ->
%%       Hallucination of Future.
%%     Nothing extracted beyond this map.


%% ============================================================================
%% PART F -- CONFLICTS AND OPEN QUESTIONS SURFACED DURING CONSOLIDATION
%% ============================================================================
%% 1. [CONFLICT] Adjacency operator: \tilde{\implies} ("proximity",
%%    Variant 1, identical to succ) vs. \perp ("structural adjacency",
%%    Variant 2, implication depth). Variant 2 has the stronger derivation
%%    (co-existence of equal discernibles forces composition via
%%    implication) and avoids collapsing space into the sequential operator.
%%    Decision needed before drafting any Space section.
%%    NOTE: \perp collides typographically with \bot -- if Variant 2 is
%%    chosen, pick a different symbol.
%%
%% 2. [CONFLICT] Two habitats for the Stretch Discernible: \mathbb{F} in
%%    ontology-part-2.tex vs. the A^top/A^diamond scheme of the DRAFT vs.
%%    \mathbb{A} in the current manuscript. Unify.
%%
%% 3. All of Part C presupposes the OLD faculties-as-posits and the
%%    A^top/diamond/bot apparatus. Under the current discipline (nothing
%%    imported before it is earned), each item needs its prerequisites
%%    checked against what Sections 1-2 have actually derived.
%%
%% 4. The old tail of ontology.tex (from "The Primordial Event" section
%%    onward) still uses "Noumena" and the old delta_bullet = "I exist".
%%    It duplicates the Event/Atomic Fact/Fixation formalisms already
%%    given in Section 2 (Truth Modalities). A structural decision is
%%    pending: rewrite the tail under the new framework, or cut it for the
%%    shorter-paper path (Sections 1-2 standalone).
%% ============================================================================
