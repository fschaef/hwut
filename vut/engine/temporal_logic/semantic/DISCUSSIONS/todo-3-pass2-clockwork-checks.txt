SPDX-License: MIT; Project VUT; (C) Frank-Rene Schaefer
===============================================================================
todo-3  PASS-2 CLOCKWORK CHECKS  F-12 .. F-14
===============================================================================
SCOPE     semantic (pass-2). The obligations are specified; implement them.

TO IMPLEMENT (from the parser handoff, now folded into RATIONALE):
    - F-12  CLOCK BINDING, FENCE, 'cw' SCOPE. A clockwork's 'on:' target is a
            <cause> (trigger + optional guard). The signature parameters ARE
            its instance members, read through 'cw.'. Enforce the fence (writes
            are clockwork-bound) and that 'cw.' resolves only inside the
            clockwork's own body.
    - F-13  STEPS: emission kinds, wait binding, cascade edges. Classify each
            step's emission (paced vs instant), bind 'wait:'/'select:' targets,
            and record the cascade edges the steps introduce.
    - F-14  The optional role-hint consult for the step vocabulary.
