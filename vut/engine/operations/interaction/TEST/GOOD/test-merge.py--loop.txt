INSPECT: looping  -> commit, 'alpha\nbeta\ndelta\ngamma\n'
         stuck    -> cancel, None after 1 round(s)
         patient  -> commit after 21 round(s)
         capped   -> cancel after 5 round(s) (cap 5)
  OK  : open and close happen ONCE -- the loop is inside the session
  OK  : the loop ends on COMMIT, carrying the final artifact
  OK  : a REALIGN that changed nothing is refused as a CANCEL
  OK  : and it is refused AT ONCE -- no second round is computed
  OK  : twenty progressing rounds are not cut off: only the bug is
  OK  : an OSCILLATING driver -- always progressing, never deciding -- is ended by the cap, as a CANCEL
  OK  : and exactly at the cap, not one round beyond it
SUCCESS: the merge loop loops, and stops for a reason.
<hwut-end>
