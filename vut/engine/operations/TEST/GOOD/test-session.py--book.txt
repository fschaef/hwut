INSPECT: operations booked = {'demo.py': {'<none>': ['Accept', 'Run']}}
         Run entry  = {'verdict': True, 'report': 'ok', 'canonicaliser': {'stdout': ['cat']}}
  OK  : one entry per operation, both kept
  OK  : the canonicaliser is recorded: a record is HISTORY, and a later Replay must be able to tell it was freed differently
  OK  : the instant, the host and the attribution are NOT in the book: a run makes them again, so they are observations, and stand in the local database (E-20, E-22)
SUCCESS: what happened is booked, and never appended to.
<hwut-end>
