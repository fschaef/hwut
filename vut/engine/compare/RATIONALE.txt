
C-1   THE CONTRACT -- A SHARED NAME BELONGS WHERE ALL ITS READERS
      SIT ABOVE IT

PROBLEM   'engine/compare' held seven MUTUAL import pairs among five
          sub-components and four of the tree's ten module-level
          cycles -- the one place in the tree with no readable
          direction. Measured ('adm/import_graph.py --shared'), four
          of those pairs rested on a SINGLE NAME travelling backwards
          against three to eight going forwards.

DECISION  'engine/compare/contract' holds the vocabulary in which the
          parties of a comparison name the same things: 'E_Verdict',
          'E_Chunk', 'E_ToleranceId', 'E_EditId' and the semantics
          that give them meaning; 'AnalogyDb' and 'FrozenAnalogyDb',
          the shapes an analogy is carried in.

          IT IMPORTS NOTHING OF THIS TREE, and that is the property
          which MAKES it a contract: every party sits above it, so no
          party can be made to wait on another.

          WHAT BELONGS: SHAPES. An enum has no behaviour at all and
          exists precisely so two parties may say the same word; a
          request shape is what one party asks of another; an error
          kind is part of an interface.
          WHAT DOES NOT: anything whose move would move BEHAVIOUR.
          THE TEST -- would moving it require moving behaviour? 'Line'
          has twenty methods and imports 'core'; 'PatternFinder'
          computes; 'Store' holds state. Those are shared because a
          REAL DEPENDENCY exists, and the layering must express it,
          never hide it behind a shared package.

COST      Thirty-six modules re-pointed. The door re-exports and says
          so with '__all__' -- a re-export is not an unused import,
          and saying so to the reader matters more than saying it to
          the tool.

VERIFIED  'reading -> engine' fell from 8 names to 1; 'region ->
          engine' from 6 to 1. Every shared name of 'compare' now
          reads 'from contract' except the three that carry
          behaviour. 301 declared choices unmoved.

WHAT IT DID NOT DO, and this is the honest half: THE SEVEN MUTUAL
          PAIRS STAND, and the module-cycle count is still ten -- the
          'enums <-> semantics' pair simply moved INTO the contract
          with them. What holds the remaining pairs shut is exactly
          the three names the test above forbids moving. A contract
          removes the cycles that a MISPLACED SHAPE caused; it cannot
          remove the ones a real mutual dependency causes, and
          'reading <-> region' looks like one: a reading contains
          regions, a region is made of readings.

          THAT REMAINDER IS NOW A QUESTION FOR ARCHITECTURE, not for
          a tool: state the mutual recursion as a design, or break it
          by moving behaviour. This entry does neither.

GRAVEYARD MOVING 'Line' AND 'PatternFinder' TOO -- rejected: both
          carry behaviour, and a 'contract' holding machinery is a
          package named for a property it does not have.

C-2   THE SEAL -- 'NOTHING LEAVES' IS CHECKABLE, AND SO IT IS CHECKED

PROBLEM   C-1 stated that a contract imports nothing of this tree, and
          nothing could test it: 'adm/LAYERING.txt' is written at
          depth 2, where the whole of 'engine/compare' is one
          component and everything inside it is invisible. A law
          nobody can check is a law that decays.

DECISION  'SEALED <glob>' in the declaration, JUDGED AT MODULE DEPTH.
          A direction is about a PAIR and needs both named; a seal is
          about ONE COMPONENT and needs no depth at all, so it holds
          at whatever depth the rest is read with.

          VERIFIED BY BREAKING IT: one import added to
          'contract/enums.py' reaching for 'region/registry.py', and
          the check at DEPTH 2 -- where it cannot see inside 'compare'
          at all -- names the module and its target and answers 1.

COST      A second line shape in the declaration, and a second kind of
          reading. The refusal message names both, so a malformed line
          says which shapes exist.

C-3   THE ORIGIN OF A LIFT DOES NOT STAY BEHIND            (2026-09-01)

PROBLEM   C-1 lifted the four shared names -- 'enums', 'semantics',
          'analogy_db', 'frozen_analogy_db' -- into 'contract/'. The
          copies in 'engine/' were never removed. Nothing imported
          them (the two that imported at all imported each other),
          so 'import_graph.py' could not see them: a reachability
          tool is blind to what nothing reaches. They had drifted:
          'engine/enums.py' lacked 'RegionSyntaxError'. Fourteen
          docstrings and documents still pointed at 'engine/...' as
          the single source, naming a file that was the stale one.
          Found by clone detection (PMD CPD, identifiers ignored),
          which sees what the import graph cannot.

DECISION  The four files in 'engine/' are deleted. Every pointer
          'engine/<name>.py' in code comments, README, ARCHITECTURE,
          SEMANTICS and IMPLEMENTATION-happy-constraints now reads
          'contract/<name>.py'. The DOC history files are left as
          they were: they record what was, not what is.

COST      None in behaviour: the compare tree answers 119 ok, 6 fail
          before and after, the same six (region framing, memory
          cap -- both open elsewhere). 'import_graph.py --check'
          answers the same twelve findings before and after.

GRAVEYARD Re-exporting from 'engine/' for compatibility -- rejected:
          there was nobody to be compatible with.

C-4   REGION FRAMING IS SWITCHABLE, LIKE EVERY OTHER FEATURE (2026-09-02)

PROBLEM   '##! <handler>' and '####' were read in EVERY text, always.
          Every other lexical feature has a flag -- 'analogy_f',
          'ignored_line_f', 'whitespace_f', 'backslash_f' -- and this
          one had none. A text that TALKS ABOUT framing therefore
          could not be read at all: this tree's own
          'test-equivalence-vs-association.py' prints a side-by-side
          table whose CELLS hold '##! potpourri', and the reader saw
          a shebang with a parameter '|'. Four choices of the compare
          suite failed on it, carried as open; 'hwut.run.play' on any of
          them ended in a traceback.

DECISION  'ConfigurationPatternFinder.regions_f: bool = True'. False:
          the markers are NO LONGER REGIONS -- they fall to the
          ordinary rules, where '##! ...' meets the ignored-line
          marker '##' and reads as a comment; with comments off too,
          as content. Switching regions off REMOVES A CLASS; it
          invents none.

          THE NAME, in two turns: 'region_f' was refused as too weak,
          'regions_enabled' proposed and built, then ruled back to
          'regions_f' -- the plural says what is switched and the
          '_f' keeps the habit of its five neighbours
          ('analogy_f', 'ignored_line_f', 'whitespace_f',
          'backslash_f', 'strip_whitespace_f'). One suffix for one
          kind of member is worth more than one member's clarity.

          JUDGED IN ONE PLACE: 'reading/line_scanner.classify()', the
          one function where a line becomes a class. Nothing
          downstream can see a region the reader was told not to
          read, so no second gate exists to disagree with the first.

          THE HWUT WORD IS 'tolerance.regions', beside
          'tolerance.whitespace' and 'tolerance.slash', stated per
          choice or per application:

              # @hwut { title = "..."  tolerance { regions = false } }

          Related in 'relation.py' to 'regions_f', so
          'hwut.show' prints its default like every other parameter
          and 'test-defaults.py' checks it.

COST      One member in compare's declaration, its mirror in
          'owner_faces_provisional.ConfigCompare', one leaf on
          'Tolerance', one line in the validator's vocabulary, one
          in the adapter's mapping. The hwut word is 'tolerance.
          regions' -- the word drops the suffix, as
          'tolerance.whitespace' does for 'whitespace_f'. Two tests of this tree state
          'regions = false' and stop failing:
          'test-equivalence-vs-association.py' (3 choices) and
          'reading/test-chunk_pipe.py' (1). Compare: 119 ok / 6 fail
          -> 123 ok / 2 fail, the two remaining being the memory-cap
          kills (disc-6 r-3), untouched.

GRAVEYARD Escaping the marker in the text -- rejected: the text is
          the subject, and a framework that requires the author to
          alter their program's output to be testable has failed at
          the one thing it is for.
          Auto-detection ("this looks like content") -- rejected: a
          reader that guesses would silently reinterpret a region
          somebody meant, which is the very thing 'RegionSyntaxError'
          exists to make loud.

C-5   THE ONE FAULT A CALLER MUST CATCH, CAUGHT ON EVERY ROAD (2026-09-02)

PROBLEM   'RegionSyntaxError' stands in the contract BECAUSE THE
          CALLER CATCHES IT -- its own docstring says so, and
          'consume/equivalence_check.py' states the law with the
          reason: "a broken region framing is a VERDICT WITH A
          REASON, not an exception". Of the three roads into
          compare's door, only that one obeyed. 'consume/
          difference_display.py' and 'services/compare.reading_view'
          let it through, so 'hwut.run.play' and 'hwut.compare' answered
          a text they could not read with a Python traceback -- which
          tells the person the framework broke when it was their text
          that could not be read.

DECISION  Each ROAD catches once, in the operation that owns it, and
          says it in that road's own vocabulary:
              VERDICT   consume/equivalence_check   (unchanged)
              DISPLAY   consume/difference_display  -> the report
                        'region-syntax-error', diagnosed by name
              READING   services/compare.reading_view -> a REFUSED
                        line naming the line, and a FAULT exit; the
                        one place for both faces that show a reading
                        ('hwut.compare' and 'hwut.run.play')
          Not four faces each rolling their own: three roads, three
          catches.

COST      'reading_view' answers a bool where it answered None, and
          takes a 'write'. Both its callers read it: play and compare
          return FAULT where the reading could not be shown, rather
          than OK with a refusal printed in the middle.

C-6   A STRESS TEST SAYS WHAT IT COSTS                          (2026-09-02)

PROBLEM   'solver/TEST/test-csp_backtracking_mrv.py' was killed on two
          of its four choices -- 'massive-pipe' and 'soduko' -- for
          exceeding the 512 MB default memory cap. The two build the
          whole pairing space in memory; that is what they are for.
          Open since disc-6 r-3.

DECISION  The application states 'caps { memory_mb = 2048 }'. Raised,
          not removed: a cap nobody can exceed catches nothing, and
          2048 still catches a regression that doubles the space
          again. Measured UNCONSTRAINED, the two need 1069 MB.

FOUND     THE KILL MESSAGE'S 'peak' IS NOT THE REQUIREMENT. It is
          where the process stood when it was killed, so it tracks
          the cap: 518 MB reported at cap 512, 1048 MB at cap 1024,
          for the same run. Two caps were set on that reading before
          the pattern was visible. Only an unconstrained measurement
          answers "how much does it need"; the message is worth a
          word saying so, and 'procsitter' is the place for it.

COST      compare answers 131 ok / 0 fail, where it answered 129 / 2.

C-7   E-9's DELETION, EXECUTED                                   (2026-09-03)

PROBLEM   E-9 (2026-08-26) retired 'tool/hwut_compare.py': "the tool's
          suite and four GOOD files go with it." The deletion was
          decided and never carried out -- the same shape as C-3's
          origin-of-a-lift, a ruling standing unexecuted in the tree.
          'tool/hwut.compare' (the shim), 'tool/migrate_region_
          syntax.py' (E-9: "stands alone and untested" -- the one-shot
          '||||' migration, and zero occurrences of '||||' remain
          anywhere outside 'tool/' itself) and the retired suite's
          four GOODs stood alongside it, inert but not free: the
          census walked 'tool/TEST' and reported it '[OK] 0/0', a
          green line for a directory that tests nothing.

DECISION  'engine/compare/tool/' deleted whole -- seven files: the
          shim, the tool, the migration script, the suite, its four
          GOODs. 'MANUAL.txt' no longer instructs a reader to run a
          script that is gone; 'ARCHITECTURE.txt's directory listing
          no longer names a directory that does not exist.
          'DOC/SEMANTICS.txt's D-19 entry and 7.6 section, which
          record the migration as HISTORY rather than instruct
          anyone to run it, are left standing (C-3: a DOC history
          records what was, not what is).

CHECKED   Every reference outside 'tool/' read before deleting:
          'services/RATIONALE.txt' (E-9's own text), 'services/
          adm/WORK/gathered/0k-disc-3-compare-confs.txt' (already past tense:
          "took to its grave"), the three documentation files above.
          None is code; nothing imports or executes any of the seven.

COST      None. compare's census is unaffected; 'import_graph.py
          --check' answers the same twelve findings before and
          after.

C-8   A SLOTTED STRUCT REFUSES A MISSPELT FIELD                 (2026-09-04)
------------------------------------------------------------------------------
RECORDED 2026-09-04, on striking 'operations/DISCUSSIONS' todo-12, which
raised this and is now built. The number is the next free one in this
component's series; the ruling itself is older than the entry.
PROBLEM   'Configuration' was slotted and refused a misspelt field on
          itself. 'ConfigurationPatternFinder' was NOT, so

              c.pattern_finder.numeric_tolerence_ratio = 0.01

          was ACCEPTED and 'numeric_tolerance_ratio' stayed 0. The
          author believes a tolerance is in force; it is not. Under
          'absent and empty never collapse' this is the worst shape a
          defect can take: the test then passes or fails for a reason
          NOBODY WROTE DOWN, and no error is raised anywhere to say so.
          It is silent, and it is silent in the direction of a false
          pass.
DECISION  EVERY CONFIGURATION STRUCT IS SLOTTED.
          '@dataclass(slots=True)' on 'ConfigurationPatternFinder' as
          on 'Configuration', so a misspelt attribute raises at the
          point of the mistake rather than being kept as a new
          attribute nothing reads. A test choice demonstrates the
          refusal ('TEST/test-configuration.py', choice 'slots'), so
          the law is checked and not merely stated.
          Nothing changes for the callers: 'TestChoiceConfiguration'
          holds this struct verbatim either way.
COST      A field cannot be added to a configuration by assignment from
          outside; it must be declared. That is the point, and it is
          paid once per new field.
GRAVEYARD A validator that walks the struct after construction and
          reports unknown names: it runs LATER than the mistake, needs
          a list of legal names beside the class (a second place to be
          wrong), and catches nothing the language catches for free.


C-9   THE 'unaccepted' REGION: CONTENT NOBODY HAS JUDGED           (2026-09-09)
------------------------------------------------------------------------------
PROBLEM   A partial acceptance -- some lines of a candidate taken into
          the nominal, the rest left as found -- had no way to say
          which lines were left. A nominal is read as a whole; a line
          nobody decided about passes or fails on its content like any
          other, and a green run then proves less than it says.

DECISION  A REGION KIND, registered beside the others:

              ##! unaccepted
              <lines nobody has looked at>
              ####

          Its rule is the OPPOSITE of 'ignore': equivalence is False
          wherever either side holds a line, and the association shows
          every pair as DIFFERING -- a run meeting the region fails
          until someone decides. An EMPTY region on both sides is
          equivalent: nothing undecided stands in it, and the lawyer,
          who sees no pair, must agree with the judge.

          THE '!' IS LOAD-BEARING. '## unaccepted' is an IGNORED
          commentary line and passes silently; '##!' is REGION_BEGIN,
          recognised before the ignored-line rule
          ('reading/line_scanner.py'). No new grammar: a name in the
          registry, and the framing the language already had.

          WHICH SIDE: a nominal's. A subject carrying the framing is a
          test application that printed it itself.

OWED, NOT HERE: the verdict WORD. A run failing on an unaccepted
          region is not a regression and must not read as one; it
          wants its own word beside FAIL and STAINED, so the census
          can count it apart and the closing hint can name the way
          forward ('hwut.accept'). That is the display's and the
          book's, and comes with the partial acceptance that writes
          the region (services E-58).

COST      One more handler name in every 'known handlers:' message.

GRAVEYARD A new token ('<undefined>') -- rejected: every reader would
          learn a word meaning "we did not look"; the region framing
          already means "read these lines under this rule", and the
          rule here is "fail".


C-10  THE CLOSING TOKEN IS A CHUNK OF ITS OWN                       (2026-09-10)
----------------------------------------------------------------------------
PROBLEM   E-60's mirror puts '<hwut-end>' outside every region, and R-70
          needs it last. Fused into the block above it on the subject
          side, it faced a token standing alone on the nominal side --
          and neither could pair. Measured: both read as one-sided.

DECISION  compare's reader splits a trailing '<hwut-end>' into a chunk
          of its own, FOR EVERY STREAM. The display names its section
          'CLOSING-TOKEN' and sets it apart, at the foot: the screen
          reminds whoever reads it of a characteristic of an hwut test
          -- a stream ends with its own testimony that it completed.
          R-70 enforces it; the display teaches it.

          AN 'unaccepted' NOMINAL CHUNK PAIRS WITH ANY SUBJECT CHUNK.
          Every other handler says HOW a stretch is read, and two
          readings cannot both claim one line; 'unaccepted' says only
          WHETHER anybody looked, a statement about the nominal's
          history. The zip pairs them and 'region/unaccepted/
          associate.py' -- written for a subject side it never received
          -- zips the lines as SUBSTITUTE. Without this the two streams
          DOUBLE on screen.

          THE POST-TAKE SHAPE -- one subject chunk facing 'region |
          plain | region', which every take leaves behind -- is SPLIT
          to face the run, anchored by CONTENT: a taken line IS its
          subject line, byte for byte, so a plain nominal chunk is found
          in the subject exactly and monotone, and a region receives the
          subject lines between its neighbours' anchors. The zip keeps
          ONE CHUNK of nominal look-ahead for it. The cut fires ONLY
          beside a region: plain facing plain with no region in sight
          pairs as it always has, so compare's association is never
          second-guessed where nobody took anything.

COST      compare's own GOODs do not move for the token (measured: no
          fixture carries it). Fifteen downstream GOODs move, each by
          the one section line. The look-ahead changes WHEN the nominal
          is read, so 'test-associate-race.py's three arrival traces
          move -- their yielded pairs byte-identical -- and that test,
          older than R-70, now ends in the token it never printed.

GRAVEYARD Peeling the token in the session only -- rejected: the mirror
          stays off by one chunk. Tolerating a trailing extra nominal
          chunk in the zip -- rejected: measured to flush as
          BAD_SUBJECT_HAS. Pairing by line offset after a take --
          rejected: the first N != M take breaks it for good. Cutting a
          plain chunk wherever its lines are found -- rejected, measured:
          it re-associated ordinary plain/plain pairs in four standing
          suites where no region stood.

C-11  A NUMBER IS ALWAYS A NUMBER                                  (2026-09-15)
----------------------------------------------------------------------------
PROBLEM   The NUMERIC pattern was registered only where
          'numeric_tolerance_ratio' was non-zero. MEASURED: at the
          default 0, '23' was a STRING element, so '1.0' differed from
          '1' and '0.000' from '0.0' -- the ratio decided WHETHER a
          number was one, not how far two may differ. The reading could
          not mark a number either: the element kind the display wears
          IS the lexed kind, and there was none to wear.

          AND: '4.5s' lexed as NUMERIC '4' + STRING '.5s' -- the
          trailing '(?!\w)' made the pattern backtrack into half a
          number.

DECISION  NUMBERS ARE ALWAYS LEXED. The ratio is the band and nothing
          else: 0 means 'equal VALUES' -- '1.0' is '1', '1e3' is '1000',
          '007' is '7' -- and a number against a word is a type
          difference.

          THE NUMBER IS ATOMIC: taken whole or not at all. Glued to a
          word it is text, as 'x86' always was.

          The default ratio stays 0. Whether it should be 0.01 is a
          separate question, not ruled here.

COST      MEASURED on the whole tree: sixteen recorded pages moved --
          three readings gained '{...}', 'test-ui structure' reads
          '(NUM)' for '(STR)', 'test-association-cost' counts other
          search nodes (the knee stays between lines 30 and 32), three
          'test-pattern_finder' pages print the pattern, and six
          'test-main' pages gained the '<hwut-end>' that suite never
          printed (R-70), as 'test-pattern_finder' did. 'test-main
          numbers' pins the verdicts.

GRAVEYARD 'None' for 'not a number', 0 for 'exact' -- rejected: there is
          no case where a number should be read as text. A reading-only
          re-lex for colour -- rejected: the colour would say 'number'
          where compare reads text.

C-12  THE SIGN IS THE NUMBER'S                                     (2026-09-16)
----------------------------------------------------------------------------
PROBLEM   MEASURED: '-3.5' lexed as one number, '+3.5' as STRING '+' and
          NUMERIC '3.5' -- so '+3' against '3' compared a '+' as text,
          and the display coloured the digits without their sign. And a
          sign right after a digit ('1+2') was never a sign at all.

DECISION  Either sign belongs to the number: where it stands free, and
          RIGHT AFTER A DIGIT -- '(?<=\d)[-+] | (?<!\w)[-+]?'. Output
          prints values, not arithmetic: '1+2' reads 1, +2; '2026-09-15'
          reads 2026, -09, -15; 'pages 1-20' reads 1, -20. After a
          LETTER a sign is text ('a-1'), and digits never start after a
          word character ('x86' stays text).

COST      MEASURED on the whole tree: no verdict moved anywhere. At ratio
          0 equal text stays equal, and -20 against -21 differs as 20
          against 21 did; what changes is the colouring and the status
          bar's wording on ranges and dates. 'test-main numbers' gained
          five verdicts and the lexed edge cases; three
          'test-pattern_finder' pages print the pattern.

GRAVEYARD A sign only where it stands free (bundle 19) -- replaced by
          ruling: it left the '+' of '1+2' as uncoloured text.

C-13  NO TRANSPOSITION INSIDE A LINE                               (2026-09-24)
----------------------------------------------------------------------------
PROBLEM   MEASURED: 'hwut.run.diff test-import_graph.sh vut' never returned.
          The element search of ONE line pair swapped a later subject
          element forward wherever it matched (TRANSPOSE), and kept the
          permuted line in its merge key. Two paths permuting
          differently never merge, so the search grew exponentially:
          7 words 875 509 expansions, 8 words beyond 30 s. Path-like
          lines ('/', '.') offer a candidate at nearly every position.

DECISION  RULED (Frank-Rene): the element search reorders nothing.
          Swapped elements read as DELETE plus INSERT. A swap never
          made a line equivalent -- TRANSPOSE was no GOOD edit -- so no
          verdict depends on it; it changed only the cost that pairs
          lines, the 'out of order' mark, and an analogy only a swap
          could bind.

COST      MEASURED: the pair above associates in about 2 s.
          'test-association-cost long_lines' no longer explodes, and
          'fixture' measures the whole real pair without reaching the
          stopping rule -- though the step from line 30 to 32 still
          multiplies the nodes by 12.5 (32 155 nodes). 'test-line'
          and 'test-line-2' read DELETE/INSERT where they read
          TRANSPOSE, at the higher cost, and gained the '<hwut-end>'
          they never printed (R-70). No verdict moved.
          The vocabulary went with it: 'E_EditId.TRANSPOSE',
          'cost_TRANSPOSE', the edit's 'transpose_ai', and the display's
          'out of order' marks.

GRAVEYARD Transposition inside a window |ai - si| <= K -- rejected: it
          keeps a threshold and a mark nobody reads, for a line that is
          DIFFERENT either way.

C-14  THE AUTHOR'S PATTERN BEFORE NUMBERS AND BLANKS              (2026-09-24)
----------------------------------------------------------------------------
PROBLEM   MEASURED: an 'eq_pattern' that begins at a free-standing
          number never matched. The reading is one alternation, and it
          took the earliest alternative at a position -- NUMERIC and
          SEPARATOR stood before the author's patterns. '[0-9]+ x'
          never took '9 x' against '6 x'; 'n[0-9]+ x' did. A count, a
          date ('2026-09-24' reads three numbers, C-12) could not be
          tolerated by the means every page uses.

DECISION  RULED (Frank-Rene): the author's patterns are registered
          BEFORE NUMERIC and SEPARATOR, after the constraint binding and
          the analogy. An 'eq_pattern' is the most specific word there
          is, as the binding is before the analogy. Where no pattern
          claims the text, a number is still always a number (C-11).

COST      MEASURED: three 'test-pattern_finder' pages print the
          registration order and moved by it; no verdict moved on the
          whole tree.

GRAVEYARD Rewriting the output in the harness so a pattern need not
          start at a number -- rejected: the page stops showing the
          tool's own words, and every later author meets the same trap.
          A numeric ratio on the page -- rejected: a ratio wide enough
          for 9 against 186 tolerates every number on it.

C-15  THE LINE LEVEL IS ANCHORED FIRST                            (2026-09-25)
----------------------------------------------------------------------------
PROBLEM   The line level aligned a chunk pair by one search over every
          subject line against every nominal line. Its costs are flat
          and it reorders nothing: it is diff, and diff is anchored.

DECISION  RULED (intend 19, phase one; L-1, L-2): within ONE chunk pair,
          lines EQUIVALENT and UNIQUE on both sides pair up, and the
          longest chain of them in order on both sides is kept
          (patience, Cohen 2007); the begin and the end anchor
          virtually and every anchor grows over equal neighbours
          (Heckel 1978). The sections between anchors, and each anchor
          as a 1x1 section, go through the search IN ORDER, each from
          the analogy bindings its predecessor ended with. A chunk with
          no anchor is one section, the search as before.

          THE KEY IS THE EQUIVALENCE CLASS, NOT THE TEXT: separators
          one token, visible-nothing dropped, strings by content,
          numbers only where their tolerance is exact. A line holding an
          equivalence pattern, a tolerated number, an analogy or a
          binding never anchors -- no key names its class; nor does a
          line with no content at all (empty, whitespace, ignored by the
          comment markers), which equals every other such line: anchored,
          it only picks a partner at random (measured: 11 of phase one's
          12 disagreements with a known pairing). Keyed by
          text, a unique 'glad' paired with a unique 'glad' where the
          partner was an equivalent 'happy' (measured, 'test-
          consistency-fuzz.py consistency', case 312).

COST      MEASURED on the fixture pair: the line level expands 47 items
          for the whole pair; the pair aligns in 0.42 s, and
          'hwut.run.diff test-import_graph.sh vut' answers in 0.37 s.
          What remains is the ELEMENT level inside the one unanchored
          section: 30 to 32 lines take the element search from 1 083 to
          29 782 expansions (0.29 s) -- intend 19's second point. The
          judge and the association agree on 2 200 generated cases over
          seven seeds.

GRAVEYARD Anchoring by literal text -- rejected on the counterexample
          above. Anchoring over the whole stream -- rejected by L-1: it
          would bind a line in one region to a line in another.

C-16  A SECTION IS SEARCHED WITHIN A BUDGET; PHASE TWO BEYOND IT  (2026-09-25)
----------------------------------------------------------------------------
PROBLEM   MEASURED ('benchmark-anchoring.py --knee', 1 462 sections phase
          one left in 300 real GOOD revisions): the search's time is
          driven by the section's ELEMENT WORK W -- the elements of its
          subject lines times those of its nominal lines, which counts
          long lines where the pair count does not (a 2x2 section of
          3 080-element lines ran 3 s). The p90 bends at W = 8 192 (x5 per
          doubling after it); 16 sections ran past 3 s.

DECISION  RULED (Frank-Rene): the search stays where it is affordable, and
          its budget is COUNTED IN EXPANSIONS, not predicted and not
          timed -- counted, the result is the same on every machine.
          'diff_display_parameters.search_budget' 20 000: every section below the knee
          fits (the largest used 10 968); ~11 us an expansion. A section
          that spends it goes to PHASE TWO (intend 19, o-6, o-7): pairs
          clearly the closest anchor first (the runner-up, or DELETE +
          INSERT at 1.0, 'margin' 0.10 above), else the 'lowest_n' 4
          cheapest, non-crossing; the gaps are aligned the same way, from
          ONE budget pool per section. A pair is costed by the FUZZ RATIO
          averaged with 'context_k' 1 line either side; a pair whose own
          element alignment spends a budget shows as DELETE + INSERT.
          The four numbers are 'Configuration.diff_display_parameters' (o-4).

COST      MEASURED: no section reaches 3 s; all up to W = 524 287 within
          460 ms; the three largest (W up to 7.6 million) 0.6-1.7 s. On
          30 mutated pairs with known truth: search alone 159 wrong /
          187 missed pairings in 141 s; now 141 wrong / 335 missed in
          9.2 s. A missed pairing shows as a line removed and one added:
          coarser, never a false equivalence -- the verdict is the
          Judge's.

GRAVEYARD The ELEMENT cost as phase two's measure: 3.6 ms a pair against
          1 us, and no more accurate (k=1: 257/330 against 214/273).
          Context k=0: 774 misses against 273. A larger margin: fewer
          wrong 'clearly closest' anchors, more wrong cheapest ones --
          total 214 -> 301. A W threshold predicted from structure: the
          counted budget takes exactly the sections the search cannot
          finish, whatever makes them hard.

