## The three invocation forms must agree byte-for-byte.
stdin  == file argument: True
wrapper == file argument: FALSE

## Suppression: --no-color and NO_COLOR both yield escape-free
## output (a piped stdout is already non-tty, so the flag is
## what carries the intent).
--no-color escape-free: True
NO_COLOR   escape-free: True
both plain outputs equal: True
